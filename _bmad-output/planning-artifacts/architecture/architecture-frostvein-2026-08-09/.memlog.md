---
scope: frostvein Milestone 2 — Bevy client
purpose: build-substrate + updated docs/architecture.md companion
altitude: feature (M2), inheriting the M1 feature spine
updated: 2026-08-09T13:41
---

- (constraint) PARENT SPINE BINDING, READ-ONLY: architecture-frostvein-2026-08-01/ARCHITECTURE-SPINE.md (final). AD-1 pure sim-core; AD-2 fixed-timestep decoupled loop; AD-3 NDJSON/TCP protocol v0; AD-4 clients render a world not a game; AD-5 plain A*; AD-6 wire types only in protocol, enums never strings; AD-7 structural determinism; AD-8 deltas = dirty tiles + full-resend-is-authoritative-replacement; AD-9 sim-assigned stable u32 ids; AD-10 world-mutating commands via queue at loop boundary, control commands direct; AD-11 SaveState; AD-12 one job market. Plus all Consistency Conventions. Not re-derived; new ADs may not weaken them.
- (constraint) PRD binding inputs: prd-frostvein-2026-08-09 FR27-FR37, NFR5-NFR8; NFR6 measured feel bar is BLOCKING in this pass; NFR8 gate probe sibling; sign-off gate both halves; cap 10-14 stories with cut order (lanterns, then parity narrows); procedural-first art; one zoom continuum; z-levels survive into 3D.
- (constraint) M1 spine's Deferred 'Raycast 3D view' entry is STALE (superseded by the 2026-08-08 pivot + FR24 withdrawal); flag a parent-spine update rather than silently contradicting it. Also stale there: 'Unreal client' mention in save-format deferred line.
- (event) Run started. Coaching path; deliverables = spine + docs/architecture.md update. Open worklist: bevy client crate paradigm + delta consumption, NFR6 number, NFR8 probe, renderer evidence discipline, FR27-30 vocab invariants, z-slice/world-edge spine-vs-story, stack verification (bevy version, WSL2/WSLg display envelope).
- (decision) CLIENT PARADIGM (Wolf): mirror-then-project. The Bevy client keeps a plain non-ECS world mirror holding wire truth only; snapshot/delta application (AD-8 semantics incl. absence-is-deletion) lives ONLY there. Bevy systems diff mirror vs spawned render entities and reconcile; the render side never touches protocol decoding. Prevents: AD-8 semantics scattered across ingestion systems; unassertable client state. Rejected: direct ECS ingestion (B), hybrid (C).
- (decision) INTERPOLATION IS PRESENTATION (invariant, extends NFR5 to time): the mirror holds only positions/state the wire sent (ticks N-1, N); the projection layer may blend for smooth motion but authoritative client state never contains an uninvented... never contains an invented position. Prevents client-side drift via extrapolation/prediction.
- (decision) EXTRACT THE MIRROR NOW (Wolf): fifth crate holding mirror + snapshot/delta application, depends on protocol only; BOTH tui and the Bevy client consume it. Second-concrete-case rule satisfied (tui ClientState + bevy mirror). TUI adoption surgery is an M2 story. AD-8's client-side semantics exist once; the TUI asserts through the same code the Bevy client renders from — the instrument argument closed.
- (decision) OPERATIONAL ENVELOPE (Wolf, with live evidence): M2's Bevy client runs from the WSL2 devpod via WSLg — verified on the dev machine: glxinfo shows direct rendering, D3D12 passthrough to RTX 4080 Laptop GPU, Mesa 25.3.5, GL 4.6, 11GB free texture memory. NFR6's bar is measured in THIS environment. Native Windows build: wanted eventually — DEFERRED with the constraint that nothing in M2's gui crate may preclude it (no unix-only assumptions in client code).
- (assumption) glxinfo proves OpenGL, not Vulkan; Bevy/wgpu prefers Vulkan (WSLg = Dozen driver, younger). First Bevy story must prove a window renders at speed on this box before anything builds on it — de-risk item, consistent with first-third-wow sequencing.
- (decision) NFR6 BAR SET (Wolf, explicit — the blocking item): sustained 60 fps at working zoom, >=30 fps at full vista, full 128x128x32 world + all dwarves + all lights, measured on the WSLg devpod via the client's own frame-time overlay (the overlay IS the instrument). Command acknowledgement keeps NFR2's ~200 ms. Not measured by feel.
- (decision) CRATE NAMES (Wolf): shared mirror crate = client-core (depends on protocol only); Bevy client crate = gui (parallel to tui, engine-agnostic name). Workspace becomes six crates: sim-core, protocol, client-core, simd, tui, gui.
- (decision) EVIDENCE LADDER (Wolf: 'should be ok'): (1) client-core proves world-correctness headless in CI — AD-8 semantics byte-exact, same code gui renders from; tui = live cross-check on a shared daemon; (2) gui logic (projection/reconcile, picking, camera, z-slice) tested headless via minimal plugins, no GPU in CI; (3) visual truth = gui --capture screenshot instrument with its own tests (exists, non-black, changes-when-world-changes, range-checks its target — exit 0 is not a result) but NEVER golden-imaged in CI (driver variance). Captures are the sign-off gate's artifact; rung 3's judge is Wolf's eye, structurally.
- (decision) WIRE MODEL FOR M2 CONTENT (Wolf): trees are TILES (Material variants, occupy voxels, block pathing via existing solidity, dirty-tile path covers future chopping); everything that glows is an ENTITY with a light field — torch/campfire entities, dwarf-with-lantern is the same concept (light: Some(Lantern)); static vs moving light = one field. Wire carries kind identifiers only — light appearance (RGB/radius/flicker) is a client-side data table extending the color-as-data convention (AD-4). Sky/aurora/snowfall wire-absent (NFR5 carve-out).
- (decision) NFR8 mechanics: gate.sh probe grows two siblings — gui and client-core each must show no sim-core edge in cargo tree. Mechanical, no coaching needed.
- (decision) Z-slice control and world-edge treatment stay STORY-LEVEL design-and-test questions (per PRD addendum); the spine binds only the outcomes the PRD already states. Deferred entries with triggers, not ADs.
- (event) Gate ran: lint clean; rubric CONDITIONALLY APPROVED; versions PASS; adversary NOT-STORY-PROOF (5 pairs); 2 reconciliations 5 gaps each. Full outputs in reviews/.
- (change) Gate fixes applied: (1) Inherited section rewritten — AD-6 amendment surfaced, graph supersession explicit, stale-parent list grown (mouse/touch entry, color-table raycast clause), conventions extended to six crates; (2) AD-14 scoped to world-projected vs client-local entity classes (aurora/overlay no longer break re-projection); (3) AD-15: snapshot clears previous-tick, rewind snaps never blends, dig chips added to carve-out; (4) AD-16 pins the exact sanctioned wire diff (Entity.light: Option<LightKind>, LightKind{Torch,Campfire,Lantern}, EntityKind{+Torch,+Campfire}, Material{+TreeTrunk,+TreeFoliage}), FR30 'shape' defined, snow caps = presentation, dig-a-tree drops no item [ASSUMPTION flagged]; (5) NEW AD-18 client-core contract — mirror shape owned there, previous=entities only, mandate not cap, rect rule binding + one normalization helper + simd validates; (6) AD-17: sign-off gate's opening half named, capture tests excluded from gate.sh (GPU venue); (7) conventions: coordinate transform pair (one world_to_render/render_to_world in gui, round-trip tested), FpsOverlayPlugin bevy_dev_tools caveat; (8) NFR2 client-agnostic restatement added; (9) F10 map row carries the parity backward half (tui glyphs); (10) seed carries sequencing facts (client-core first, envelope proof first); (11) tech-art guidelines ungated from asset-pipeline trigger — procedural-era half owed by first gui visual stories; (12) bevy_vox_scene re-verify note; bevy publish date corrected to 2026-06-19.
- (decision) Version lens confirmed: bevy 0.19.0 current (2026-06-19, same train as bevy_ecs 0.19.0, lockfile confirmed), MSRV 1.95 satisfied by repo 1.97.1, MinimalPlugins headless testing upstream-documented, Dozen risk real (envelope-proof story non-negotiable).
- (decision) DIG-A-TREE (Wolf, 2026-08-09): tile removed, drops NO item. Digging stays uniform; stone from mineral materials only; wood items deferred. Rejected: undiggable trees (noisy FR8 retries), stone-from-pine (absurd to the acceptance eye).
- (change) Parent M1 spine amended in place (Wolf's yes), dated 2026-08-09 notes: AD-6 tui-depends-on-nothing-else relaxed by AD-13; dependency graph superseded by M2 spine's; Color convention raycast clause; Deferred raycast entry (withdrawn, never-sprites clause overturned); Deferred mouse/touch entry (picking lives in gui); Unreal mention. Parent frontmatter updated: 2026-08-09.
- (change) docs/architecture.md updated: retitled (no longer phase-one), six-crate shape + diagram, client-core/gui table rows, new 'Milestone 2 — the Bevy client (AD-13…18, condensed)' section, conventions (rect binding + coordinate transform pair), stack (bevy 0.19.0 + bevy_dev_tools note), Deferred list refreshed (raycast removed with explanation, M2 deferrals added). Still a ten-minute read.
- (event) spine finalized
