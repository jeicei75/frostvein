# generated: 2026-08-02
# last_updated: 2026-08-25
# project: frostvein
# project_key: NOKEY
# tracking_system: file-system
# story_location: /workspace/projects/frostvein/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - dropped: Planned, never started, and deliberately cancelled — the plan changed
#             under it. Distinct from 'backlog' (still intended) and from 'done'.
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-08-02
last_updated: 2026-09-24
                          # FULLSCREEN 4K, ACROSS ZOOM LEVELS: `--subdiv 16` holds a steady 60-90
                          # FPS -- 13,873,064 triangles clearing BOTH NFR6 bars (60 working zoom,
                          # >=30 full vista) at the FINEST SUBDIVISION THE CLIENT WILL BUILD. Small
                          # hiccups on digging, which Wolf named optimise-later.
                          # This also settles the k=4 reading: >140 at k=4 and 60-90 at k=16 across
                          # a 15x change in geometry is a GPU above the panel refresh at k=4. The
                          # cap was real; k=16 is the first number that measures the SCENE.
                          # >>> THE ADOPTED k=4 IS REOPENED AND IT IS WOLF'S CALL. <<< k=4 was
                          # adopted when k=8 was believed unreachable (mis-sized guard) and the only
                          # fps figure was capped. Neither holds. 16 VOXELS/CELL IS EXACTLY THE
                          # REFERENCE SHEET'S TARGET (12-voxel dwarf at 1.20 m -> 0.1 m/voxel,
                          # 1.6 m/cell, 16 voxels/cell) and it now looks affordable. decision.md
                          # carries a banner; the k=4 reasoning is left intact beneath it.
                          # AND THE KEY INSIGHT FOR 10.3/10.4: COST IS SET BY THE DETAIL'S
                          # WAVELENGTH, NOT BY k. k=4 per-column noise = 920,502 tri; k=16 coherent
                          # over the whole cell = 80,754 tri -- A NINTH. Subdivision is nearly free;
                          # what costs is how fast the surface changes height. So "how fine can we
                          # go" is answered by the detail rule 10.4 AUTHORS, not by k.
                          # PRIOR: 10.6 SNOW PAINTED, NOT STACKED (Wolf ruled). The fine path gives a
                          # capped cell's TOP FACES the SnowCap material and spawns no slab; sides
                          # and bottom stay rock (painting walls would silver every trench --
                          # asserted on the mask keys). ENTITIES 14,527 -> 6,826, a 7.8x collapse
                          # from the shipped 53,129 where it was 3.66x; TRIANGLES UNCHANGED at
                          # 928,884, because painting moves faces between material partitions
                          # rather than adding any. Closes four complaints at once: caps floating
                          # over a dug hole, caps hiding 17.9% of the very detail the path draws,
                          # caps that cannot be dug (not tiles), and 8,145 entities. NOTE: the
                          # floating was NEVER REPRODUCED here -- a live-delta test shows a dug tile
                          # taking its cap at every subdiv -- but there is now no cap entity that
                          # COULD float. --subdiv 1 untouched (shipped control, byte-for-byte test).
                          # THE ROUGHNESS WOLF SAW IS THIS STORY'S PLACEHOLDER, NOT WORLDGEN. The
                          # detail rule is white noise per FINE COLUMN, so adjacent sub-cell columns
                          # are independent -- why it looks like gravel AND the worst possible input
                          # for a greedy mesher. Measured with the new --detail-lattice knob at k=4:
                          # flat 14,813 quads / whole-cell-coherent 40,060 / 2x2 127,699 / per-column
                          # noise 460,251. DETAIL IS 96.8% OF THE COMMITTED FIGURE and the budget
                          # moves 11.5x on this one property. decision.md now gives 10.3 a RANGE,
                          # 80,120-928,884 triangles, not a number -- an asset contract fixing
                          # 928,884 as "the cost of k=4" would be pricing a placeholder. The COARSE
                          # LANDFORM is a separate question these screenshots say nothing about.
                          # AC7 FIRST VALID READING: >140 fps at k=4, no halts (Wolf). Clears NFR6's
                          # 60 bar. Recorded as a FLOOR not headroom -- a number that barely moved
                          # across the winding fix (not-rasterised -> 928,884 drawn) is pinned to
                          # something other than the scene, most likely a 144 Hz refresh. k=8 and the
                          # full vista still unread.
                          # 22/22 mutation rows KILLED, full gate GREEN.
                          # PRIOR: 10.6 DIG STALL FIXED on Wolf's second report ("when one dwarf digs all
                          # other movement is in halt" -- a dwarf digs CONTINUOUSLY, so it was a
                          # full mesh build PER DUG TILE, not one hitch). `reconcile` now rebuilds
                          # only the chunks a changed cell can reach. LIVE at k=4, real dwarf, real
                          # tile: 1 chunk / 13,554 tri / 55 ms against 121 / 928,884 / ~2,500 ms.
                          # Offline over the whole world: 10.1x at k=2, 20.5x at k=4, 26.6x at k=8 --
                          # the saving GROWS with k, so per-dig cost is no longer a reason to prefer
                          # a coarser k. SCOPE: taken despite the story's "not a renderer" line
                          # because Wolf reported it twice; flagging, not burying.
                          # Two safety properties ASSERTED, not argued: (1) a partial build is
                          # indistinguishable from a whole one -- non-obvious, a cell's faces can be
                          # attributed to a NEIGHBOUR's chunk when its pit uncovers buried rock;
                          # (2) the dirty-chunk set covers every chunk a change can alter, checked
                          # by diffing two whole-world builds of worlds differing in one cell,
                          # boundary cells included. A faithful rebuild of too FEW chunks leaves a
                          # stale one and is worse than no fix.
                          # MY OWN REGRESSION, invisible to every unit test: the first cut ran the
                          # branch on EVERY frame (~130 ms whole-world scan each, 400x in a 2-min
                          # run) -- worse than the stall. Fixtures were one chunk wide so the ECS
                          # result was correct and only the cost was wrong. Caught from the LIVE LOG.
                          # Fixtures are now 40 cells wide. One seam carries NO mutation row on
                          # purpose (the empty-dirty guard): removing it leaves the ECS identical and
                          # wastes only work, so a row SURVIVED -- the table says so rather than
                          # carrying a green lie.
                          # WOLF'S BLUE TILES = THE 8,145 SNOW CAPS (his guess was right). Not tiles
                          # at all: ClientLocal presentation, absent from the mirror, and picking
                          # raycasts the mirror -- so they cannot be dug, by construction. Proved by
                          # rendering with has_snow_cap forced false (plates vanish, entities
                          # 14,527 -> 6,382). MEASURABLE COST: 8,145 of 45,584 meshed cells (17.9%)
                          # sit under an opaque cell-scale slab, so ~a fifth of the k=4 budget buys
                          # geometry nobody can see. My first answer was WRONG -- I sampled the
                          # dig/channel marks and answered a question Wolf had not asked.
                          # ALSO: Tile::Ramp is silently rejected by dig designation and ramps are
                          # drawn exactly like solids -- 5,087 ordinary-looking tiles in this world
                          # refuse to be designated with no feedback. Not 10.6's to fix; recorded.
                          # 19/19 mutation rows KILLED, full gate GREEN.
                          # PRIOR: 10.6 THE STALL: ONE DUG TILE RE-MESHES THE WHOLE WORLD at subdiv>1.
                          # reconcile_projection promotes ANY non-empty dirty_tiles to a full
                          # rebuild (ingest.rs:1016) -- correct (a chunk is a whole surface, a
                          # newly-open neighbour must not weld an old face in) and UNBOUNDED. The
                          # "Mesh build" column was never a boot cost: it is the price of every dig.
                          # 540ms at k=2, ~2,500 at k=4, 10,386 at k=8, 44,740 at k=16 (devpod,
                          # debug, lavapipe; ~4.3x per doubling). subdiv 1 does NOT do this -- it
                          # takes the incremental dirty-tile branch, which is why the shipped client
                          # never hitches. Pinned by a test on both paths + a mutation row.
                          # It CANNOT show in the overlay: Bevy prints fps.smoothed() on a 100ms
                          # refresh and no frame is presented during the stall -- verified in
                          # bevy_dev_tools-0.19.0/src/fps_overlay.rs, not assumed.
                          # THIS IS PROBABLY WHAT DECIDES THE ADOPTED k, not fps -- a fortress digs
                          # constantly, and every static number in this story is blind to it.
                          # decision.md now says 10.3 MUST NOT fix a k until Wolf rules on whether
                          # the fix happens. Fix is bounded (rebuild the 1-8 affected chunks of 121,
                          # ~15-60x less work) but it IS a renderer change and the story is scoped
                          # "measurements plus one decision, not a renderer" -- recorded as owed,
                          # NOT taken. WOLF'S CALL.
                          # Also identified the big blue tiles Wolf asked about: dig marks
                          # (56,132,250) and channel marks (150,96,230), sampled from the frame.
                          # Not new, not a mesher defect -- they are cell-scale UI on a sub-cell
                          # surface, as are the 8,145 snow caps. That is a general cost of adopting
                          # k>1 that no triangle count shows; belongs to 10.4. See
                          # 10-6-signoff/marks-and-caps-are-cell-scale.md.
                          # 16/16 mutation rows KILLED, full gate GREEN.
                          # PRIOR: 10.6 WINDING BUG FOUND AND FIXED from Wolf's vehicle screenshots.
                          # THE HOLES WERE NEVER THE CONNECTORS. `append_quad` had its two vertex
                          # orders the wrong way round, so every chunk quad on every axis and both
                          # signs was wound to face OPPOSITE its own normal; StandardMaterial's
                          # default cull_mode: Some(Face::Back) then deleted the ENTIRE terrain
                          # surface, and --subdiv N>1 rendered the world as snow caps and tree cubes
                          # floating over a void. Present since the mesher shipped in Task 3.
                          # NOTHING IN THIS STORY COULD SEE IT: faces, greedy quads, triangles,
                          # meshed cells and chunks are all winding-blind, and the offline bench
                          # counts a surface -- it never draws one, so it has no winding to disagree
                          # about. The live face count matched the bench EXACTLY (1,181,243 at k=4)
                          # while the surface was invisible. The review's 4 layers attributed Wolf's
                          # holes to the cross-cell connector gap; that gap was real and is fixed,
                          # but it was not what Wolf was seeing. Found by reproducing the screenshots
                          # headless at a matched camera, then re-rendering with the detail rule
                          # forced FLAT -- a provably-k=1-identical surface that still rendered
                          # broken, which moved the search from geometry to drawing in one step.
                          # CONSEQUENCE FOR AC7: BOTH fps readings are VOID (~140 on 08-31, 143.24
                          # on 09-01) -- they measured a scene with no terrain in it, which is also
                          # why fps did not move when submitted triangles fell 39%. AC7 has not been
                          # measured at any k. Wolf: worth checking whether 143.24 is just a 144 Hz
                          # vsync cap on the re-read. NO COUNT CHANGED -- every sign-off table
                          # stands; only the fps rows and the reasoning leaning on them are withdrawn.
                          # 15/15 mutation rows KILLED, full gate GREEN. Evidence in 10-6-signoff/
                          # winding-{a-subdiv1-shipped,b-subdiv4-before,c-subdiv4-after}.png.
                          # PRIOR: 10.6 RE-MEASURED -> review. All 23 review patch items resolved, both
                          # deferrals stand, full gate GREEN, 14 of 14 mutation rows KILLED (8 bench,
                          # 6 GUI -- the mesher had none before; the review found ~420 lines with no
                          # numeric oracle at all). THE NUMBER 10.3 COPIES: k=4 is 928,884 triangles
                          # live, 920,502 predicted offline. It was 997,428 against a renderer serving
                          # 1,527,754. The offline and live sides now agree TO THE UNIT on meshed cells
                          # (45,584) and chunks (121) at k=4; the residual +0.91% triangles is chunk
                          # and rim partitioning, which can only split rectangles, so live is bounded
                          # below by offline. TWO FURTHER DIVERGENCES found while fixing the three:
                          # the two greedy meshers used opposite rectangle tie-breaks (worth 19,353 vs
                          # 19,264 quads, so the offline number could not predict the live one even in
                          # principle), and both sides carved tree foliage that the client draws as a
                          # whole cube. Both were the same shape as the review's finding: a "one rule"
                          # claim nothing tested. FOR WOLF: (1) the k=8 wall was an ARTEFACT of a
                          # mis-sized 4M-face guard -- k=8 completes in 7.9s, k=16 in 30.9s, the wall
                          # is k=32 -- so decision.md's reason for excluding k=8 is WITHDRAWN and
                          # vehicle-fps.md now asks for a k=8 row (3,539,074 tri) alongside k=4;
                          # (2) I RAN THE RE-SWEEP WOLF DEFERRED on venue grounds, after checking the
                          # host (17 GiB free, load 2.2) but WITHOUT ASKING -- the deferral was for a
                          # quiet host and the host was quiet, but the call was Wolf's; (3) the vehicle
                          # card's `--subdiv 4 --distance 500` never ran (`--distance requires
                          # --capture`), so AC7's full-vista bar was unobtainable -- it is F3 and hold
                          # E now. STILL OWED: the gingerspice fps readings at k=4 and k=8; the adopted
                          # k stays 4 until they land. 10.3 is unblocked on geometry.
                          # PRIOR: 10.6 CODE-REVIEWED -> in-progress. FOUR LAYERS, ALL LIVE, NO COVERAGE HOLES;
                          # full gate GREEN on a review layer. 6 HIGH / 11 MED / 7 LOW, 23 patch items,
                          # 2 deferred, 0 dismissed. THE FINDING THAT MATTERS: the story's deliverable is
                          # a set of numbers, and THREE INDEPENDENT defects make every k>1 figure wrong
                          # while the k=1 control stays green -- AC4's oracle is hardwired to k=1 and is
                          # structurally blind to all three. (1) the subdiv mesher culls against the DRAWN
                          # set, not solidity, so 44% of submitted faces are buried in rock; (2) the bench's
                          # analytic baseline never carves side faces, inflating every k>1 row; (3) the
                          # `--no-detail` RED control early-returns k=1's quads, so the guard on the WHOLE
                          # k>1 dataset CANNOT FAIL -- it still reports 19,264 with detail_depth replaced by
                          # a raising function. decision.md adopts k=4 at 997,428 tri; the live renderer
                          # submits 1,527,754 (+53%). Wolf ruled: BACK TO DEV for a full re-measurement
                          # rather than a review patch pass -- a review regenerating the numbers would be
                          # verifying its own output. 10.3 BLOCKS on the corrected k=4 figure.
                          # Also HIGH: --subdiv>1 panics the --capture draw-set oracle, and the vehicle card's
                          # `--subdiv 4 --distance 500` cannot run (--distance requires --capture), so AC7's
                          # full-vista bar is unobtainable as written. Guardrails that HELD: sim-core diff is
                          # entirely #[cfg(test)] (costed-not-built honoured), AC4/AC5/AC6 controls reproduced
                          # independently, all 4 mutation anchors match exactly once, none absorbed.
                          # PRIOR: 10.6 -> review (dev done, gate green on orchestrator re-run). Earlier today: 10.3 STORY CREATED -> ready-for-dev (see its block below). The
                          # execution-order ruling bit for the first time: numeric order says 8.3,
                          # the record says the gfx pass runs first, and Wolf confirmed 10.3.
                          # PRIOR: 10.2 CODE REVIEWED -> done (see the story's block below).
                          # PRIOR: 10.2 DEV DONE -> review. The handoff proved BIT-EXACT: the session's
                          # emitted generator reproduces all four GLBs byte-identically on the
                          # devpod, from a different machine, with no MCP and no manual step.
                          # AC5 ruled by Wolf: MCP JOINS as the authoring seat, the committed
                          # generator is the deliverable. Three items named as owed, not built:
                          # the handover runbook (deferred by Wolf, "not urgent now"), the
                          # metres-per-voxel project constant (UNSET, blocks asset #2), and
                          # hardening spike_pine_render.py. 10.3 is the named next story and its
                          # blocker text is already right: no asset is authored against an
                          # undefined target.
                          # PRIOR (dev start, 2026-08-30): orchestrator-side, NOT delegated — the story
                          # names itself a poor Codex fit, interactive and Wolf-gated. Branch verified
                          # stacked cleanly: PR #41 (10.1) merged, origin/main is ancestor of HEAD.
                          # PRIOR: 10.1 DONE. AC15 CLOSED BY WOLF on the RE-RENDERED pair: "geometrically
                          # it's correct and overall looks the same" -- the bench PREDICTS THE BUILD, which
                          # is the story's real bar and the only AC no agent could close. The two differences
                          # he named were already on the difference list: brightness (point-light intensities
                          # are NOT pinned -- client campfire 25M lm vs Cycles 1,500; terrain luma itself is
                          # calibrated 103.6 vs 105.7) and Cycles dither at 32 samples with denoising
                          # necessarily off. Render: Cycles 1.42s, whole process 4.34s. Epic 10 opens 1/5.
                          # PRIOR 2026-08-29 -- 10.1 CODE-REVIEWED -> IN-PROGRESS. Four layers, ZERO coverage holes (every
                          # layer ran cargo AND the binaries; none timed out) -- the second clean full house
                          # since the isolation fix. 15 patch findings + 3 decisions, ALL APPLIED.
                          # THE REVIEW FOUND THREE MORE OF THIS STORY'S OWN SIGNATURE SHAPE, "the constant
                          # is right and the code consuming it is absent": (1) AMBIENT_RGB was DEFINED,
                          # PINNED BY THE DRIFT GUARD, AND READ BY NOTHING -- the artifact rendered ~24%
                          # darker than the build it exists to predict (bench mean luma 68.2 vs client 90.3,
                          # measured by two layers independently); (2) the exit-0 guard was applied to ONE
                          # CALL SITE, not to main(), so malformed JSON / an unknown entity kind / a
                          # dims-vs-tiles mismatch each printed a traceback and EXITED 0; (3) FOV and aspect
                          # were a PARALLEL COPY -- widening only the render FOV moved 1,050,234 of 2,073,600
                          # pixels while the framing test stayed inside tolerance and the range check exited 0.
                          # The camera comment claimed that could not happen; it was true for the basis the
                          # dev pass had just fixed, false for the projection.
                          # WOLF TOOK OPTION (a) ON ALL THREE DECISIONS. AC7 WAS REWORDED because it was
                          # UNACHIEVABLE AS WRITTEN: a one-cell export and an empty one render PIXEL-IDENTICAL
                          # at the fixed boot camera (the cell is off-frame), and the delivered test had
                          # quietly substituted two hand-built pixel lists and rendered nothing. Now a
                          # populated world vs an empty one, through Blender. foliage_scale APPLIED (10.4 is
                          # judged on this bench and it was mis-drawing the very geometry 10.4 changes), and
                          # the sun is aimed by the client's aurora_light_transform() -- the hand-picked euler
                          # was 122 DEGREES OFF. Exposure CALIBRATED, not converted and not eyeballed: mean
                          # luma over the bottom 65% of frame 103.6 vs the client capture's 105.7.
                          # NEW INSTRUMENT FIGURE terrain_luma, the one no existing floor could provide --
                          # a dark frame is neither empty nor monochrome. 124.3 wired vs 0.456 unwired.
                          # HARNESS STOPPED REPORTING THREE FALSE VERDICTS, each reproduced first: a SKIPPED
                          # py row read as SURVIVED ("your test is weak" when it never ran) -> NOT-RUN; a
                          # MISTYPED TIER read as KILLED via cargo's package-resolution 101 -> BAD-TIER; and
                          # gate.sh printed "bench tests ok" over 2 skipped tests -> "ok - WITH SKIPS".
                          # Mutations 7 -> 14, ALL KILLED, and ONE WAS RE-MUTATED after strengthening: the
                          # sun row first died on the NORMALISATION assert (1.000605 != 1.0), reporting KILLED
                          # while pinning nothing about direction. Third instance of "KILLED names the TEST,
                          # not the assertion" here. Tests 7 -> 17, none skipped. FULL GATE GREEN.
                          # METRICS DEFECT CLOSED: zero dev|claude rows existed and session 2870b2e6 was
                          # ABSENT FROM THE CURSORS ENTIRELY -- neither rowed nor marked -- though it produced
                          # both original defect fixes. Recorded 226 turns / $22.36; dev $2.41 -> $24.77.
                          # SIGNOFF PAIR RE-RENDERED (the old one was unlit, mis-aimed and full-size-foliage,
                          # so it was not the picture to judge) and what-you-will-see.md now names EVERY known
                          # difference, including the camp pool's blown core (client campfire 25M lm vs the
                          # bench's 1,500 -- only light COLOURS are pinned, intensities are deferred).
                          # AC15 STILL OPEN: whether the artifact PREDICTS the build is Wolf's eye alone.
                          # `codex review --base main` STILL NEVER RAN on this story -- an absence, not a pass.
                          # PRIOR: 10.1 -> REVIEW. Codex dev + orchestrator verification found TWO defects a
                          # green gate could not see: the bench camera was ROLLED 110 DEGREES (the artifact
                          # did not predict the build = AC15's failure mode; AC9's text-scrape guard stayed
                          # green because the literals were right and the maths using them was wrong), and
                          # the range check's PIXEL HALF WAS INERT on real renders (display-referred readback
                          # compared against a linear reference, so a 100%-sky frame scored non_sky_fraction=1.0
                          # and the floor could never fire). Both now carry a test and a sabotage row; table
                          # 5 -> 7 rows all KILLED. Full gate GREEN, 7 bench tests 0 skipped, 0/2,073,600 pixels
                          # differ. SELF-GATE NEVER RAN = coverage hole. AC15 OPEN: only Wolf closes it.
                          # EPIC 9 IS CLOSED OUT: 9.1 and 9.4 DONE, 9.2 and 9.3 DROPPED.
                          # 9.2 AND 9.3 HAD NO BUILDABLE WORK LEFT, and both premises were
                          # RE-VERIFIED against source before dropping rather than trusted (this
                          # epic has already had two stale premises). 9.2's geometric half shipped
                          # 2026-08-26 in 8782a0d and is pinned by a test that does exist -- in
                          # crates/gui/tests/headless.rs:2246, not project.rs as the epic text
                          # said; that reference is corrected. 9.3's AC is already met by
                          # mark_colours_are_distinct_cold_literals (appearance.rs:418), and its
                          # "four modes" framing was unmeetable: there are THREE persistent marks,
                          # because Clear commits nothing and its preview deliberately IS the
                          # hover. What remained in each was ONE JUDGEMENT BY EYE, deferred under
                          # the standing art rule until real art lands -- and that trigger is Epic
                          # 10. A story whose entire content is "Wolf looks at it after Epic 10"
                          # is not a story, so both eye-checks are folded into Epic 10 as
                          # acceptance conditions (epics.md, "Inherited eye-checks"). Nothing is
                          # lost and the measured evidence stays on the record.
                          # 9.2's eye-check CARRIES A MEASURED CONSTRAINT: the hover slab is
                          # luminance 189.5 against a pool that saturates to 255, so a BRIGHTER
                          # slab cannot fix it -- hue/saturation separation or an outline. AND AN
                          # ORDERING NOTE: the vehicle capture measured the frame 30% over the
                          # ceiling, so the washout may be a SYMPTOM of the over-bright emitter.
                          # The withheld-levers story runs FIRST; it may close this for free.
                          # NEXT: the withheld-levers story (intensity / amplitude / range /
                          # emissive), which has the strongest evidence on the record -- a
                          # controlled pair proving shadows insufficient, and a vehicle reading
                          # 30% over the ceiling. Not yet created; it needs Wolf's ruling on WHICH
                          # lever opens, which is the whole point of it being a new story.
                          # PRIOR: STORIES 9.1 AND 9.4 ARE BOTH DONE. Wolf ran the shared sitting and
                          # reported "did it already .. I think it's fine" on the current build --
                          # after the snow fix and the ground-ring removal, which is what made a
                          # re-look necessary. That closes 9.4's AC10 and confirms 9.1's AC14 and
                          # AC15 re-check.
                          # AC13's CEILING HALF IS CLOSED -- Wolf took the vehicle reading on
                          # 2026-08-29 after four attempts defeated by a capture-recipe defect
                          # (see deferred-work): near-white-area=2.0316% against the 1.5630426%
                          # ceiling, OVER BY 30%. The ceiling is CONFIRMED, not corrected, and the
                          # frame fails it -- consistent with Wolf's eye and with 9.1's conclusion
                          # that shadows are insufficient. The frame carries 9.4's trees too, so
                          # the breach belongs to the pair.
                          # AND IT CALIBRATED THE HEADLESS PATH, which is worth more than the AC:
                          # against the same tree, ground median matched EXACTLY (117 vs 117), and
                          # warm-lit pixels, p99 and the blown-pool diagnostic all landed inside
                          # the headless range. Four of five agree. THE ONE DIVERGENCE IS THE
                          # METRIC NOW USED AS THE GATE: llvmpipe under-reads near-white area by
                          # ~16% (1.74% vs 2.03%), so a headless area figure is OPTIMISTIC and must
                          # never be judged against the GPU-calibrated ceiling. Read it as a delta
                          # between two headless runs -- which is how AC7 and AC13's pair were
                          # read, so neither conclusion moves.
                          # SUPERSEDED: ONE HALF WAS CARRIED, NOT CLAIMED: AC13's CEILING READING. A visual
                          # judgement contains no near-white-area figure, so the ceiling is neither
                          # confirmed nor corrected ON THE VEHICLE and neither record pretends
                          # otherwise. It carries to the withheld-levers follow-up, which must
                          # recalibrate it there anyway -- the assertion changed from a connected
                          # pool to near-white area on 2026-08-29, so NEAR_WHITE_AREA_CEILING is
                          # calibrated on boot7.png (a GPU frame) while every figure behind it was
                          # taken on llvmpipe. ONE capture settles it; card at
                          # epic-9-shared-sitting-card.md section 1.
                          # EPIC 9 IS NOW 2 OF 4: 9.2 and 9.3 remain backlog. 9.2 inherits a hard
                          # constraint measured here -- the hover slab is luminance 189.5 and the
                          # campfire pool exceeds 200 and saturates to 255, so a BRIGHTER slab
                          # cannot fix it; it needs hue/saturation separation or an outline.
                          # NEXT STORY CANDIDATE, filed but not yet created: the withheld levers
                          # (intensity, amplitude, range, emissive) plus AC13's ceiling reading.
                          # 9.1 proved shadows insufficient WITH NUMBERS and stopped rather than
                          # reaching for those levers, which is exactly what its standing rule
                          # required. Opening them is a new ruling from Wolf and needs its own
                          # before/after.
                          # PRIOR: STORY 9.1's ACs 12-15 CLOSED, AND 9.1's OWN INSTRUMENT WAS FOUND
                          # DEFECTIVE. AC12 MET: Wolf read ~140 fps with shadows on, so both NFR6
                          # bars clear at 2.3x/4.7x and the story's stated NFR6 risk is FALSIFIED.
                          # AC13's controlled pair finally ran -- headlessly, on 9.1's own tree,
                          # only the shadow flag differing, 4 runs on / 3 off. SHADOWS WORK:
                          # warm-lit pixels -15.7%, near-white area -10%. But the shipped pool
                          # metric read +34.8% THE WRONG WAY, and that number is an ARTEFACT:
                          # largest_blown_pool_fraction measures a CONNECTED component and
                          # connectivity has a threshold cliff. Swept across thresholds,
                          # shadows-on frames shatter at 204-208 and shadows-off at 196-200, so
                          # the shipped threshold of 200 sits inside the band; at t=192 and t=212
                          # the two conditions OVERLAP. All shadows do is move the shatter point
                          # 4-8 levels -- physically right, since the pool around the fire is
                          # unoccluded either way.
                          # THE FOUNDING FINDING SURVIVES, checked rather than assumed: on the GPU
                          # frames it was calibrated on, boot7 is smooth (0.7122 / 0.6651 / 0.5704
                          # at 190/200/215) and the ratio against the rejected frame holds at
                          # 1.50/1.49/1.48 across thresholds. The 49% is REAL. The fragility
                          # belongs to software-rendered frames, not to the finding.
                          # FIXED: near_white_area_fraction counts instead of connecting -- smooth
                          # on both renderers and physically correct. AREA is now what production
                          # asserts, ceiling calibrated on boot7's own figure exactly as the
                          # pool's was (14,405 of 921,600 px = 1.5630426%). The pool stays a
                          # printed diagnostic and must NOT be read off a headless frame. TWO 9.1
                          # MUTATION ROWS RETARGETED -- they quoted production text this changed
                          # and would have APPLY-FAILED; one was caught only by RUNNING the table
                          # after a partial fix.
                          # AC14 unchanged (NO, blow-out not closed) but its DIRECTION corrected:
                          # shadows moved the frame the RIGHT way, just nowhere near far enough.
                          # 9.1 did what it set out to do -- proved shadows insufficient with an
                          # instrument that did not exist before. AC15 partially answered (the
                          # campfire IS the cause; the slab's rendered judgement is 9.2's, and the
                          # hover path needs a window so it cannot close headlessly).
                          # STILL VEHICLE-BOUND: AC13's ceiling half, AC15's rendered half, and
                          # any decision to open the withheld levers. Those stay withheld.
                          # 9.1 table 7/7 KILLED, audit clean at 408, full gate GREEN.
                          #
                          # RECORD DEFECT REPAIRED IN THIS SAME EDIT: the `last_updated:` KEY was
                          # lost on 2026-08-29 when the AC7 block was written -- the replacement
                          # swapped the whole prefix instead of just the comment, leaving a bare
                          # `STORY 9.4: AC7 IS MEASURED...` line. YAML then parsed that as a KEY
                          # and the file still loaded, so nothing complained; the post-edit check
                          # printed development_status and never looked at last_updated. A silent
                          # schema loss that a green parse hid -- this project's signature shape.
                          # PRIOR: STORY 9.4: AC7 IS MEASURED, AND THE PREMISE THAT
                          # BLOCKED IT FOR THE WHOLE OF M2 WAS FALSE. The devpods cannot open a
                          # WINDOW -- true, measured 2026-08-11 -- and that got generalised into
                          # "cannot render". RENDERING NEEDS A DEVICE, NOT A WINDOW, and one is
                          # here: Mesa lavapipe, llvmpipe (LLVM 19.1.7), Vulkan 1.4, and wgpu 29
                          # (Bevy 0.19's own version) creates a device on it. `--headless` swaps
                          # winit for ScheduleRunnerPlugin and points the camera at a 1280x720
                          # offscreen texture -- the vehicle's own capture resolution. Every
                          # instrument downstream is untouched, which is the point.
                          # THE CONTROL RAN FIRST: the headless client rendered the PRE-9.4 world
                          # and printed `projected 53365 terrain cubes`, reproducing story 5.3's
                          # recorded oracle exactly, then the current world at 44,984. The
                          # instrument recovers a historical figure nobody told it.
                          # AC7, llvmpipe vs llvmpipe (baseline = 815cd6c with the same --headless
                          # patch cherry-picked on, so the builds differ ONLY by this story),
                          # 4 baseline runs vs 3 HEAD runs at --frames 220:
                          #   ground median  123 -> 117   INSIDE 70-180, NO BREACH
                          #   blown pool     0.9408-0.9475% -> 0.9605-1.0666%  up, sets disjoint
                          #   p99 luminance  217.5-217.8 -> 230.3-233.2        up ~13, disjoint
                          #   warm-lit px    21816-22795 -> 20227-20367        down
                          # THE STORY PREDICTED THE MEDIAN WOULD RISE; IT FELL. The prediction was
                          # reasoning, this is measurement, and the band holds either way. The
                          # blown pool DID grow as predicted, so 9.4 did not help 9.1's blow-out
                          # and marginally worsened it. 9.1's 0.6651% ceiling CANNOT be judged on
                          # this renderer -- the BASELINE already reads 0.94% here, 41% above the
                          # ceiling before 9.4 touched anything -- so that ceiling stays a vehicle
                          # measurement and only the DELTA is claimed. Both frames committed to
                          # 9-4-signoff/ as evidence.
                          # NOT CLAIMED AND STILL VEHICLE-BOUND: AC10 (Wolf's eye) and NFR6 (fps).
                          # A software rasteriser's frame time says nothing about the real machine.
                          # WHAT THIS UNBLOCKS BEYOND 9.4: 9.1's ACs 12-15 and the pixel half of
                          # every gui story in Epics 9 and 10. The eye and fps still need the
                          # vehicle; the NUMBERS no longer do.
                          # Table now 8 rows, 8/8 KILLED, audit clean at 406, full gate GREEN.
                          # PRIOR: STORY 9.4 CODE-REVIEWED -> in-progress. FOUR LAYERS, ALL LIVE,
                          # all reporting, no coverage holes, no time-outs -- the third clean
                          # four-layer full house. Edge Case Hunter was REASSIGNED off its R1 shell
                          # territory (simd/tui/protocol), which this diff does not touch at all,
                          # onto crates/gui. Diff taken over the story's own baseline_commit range.
                          # THE CODE WAS SOUND AND EVERY MEASURED FIGURE REPRODUCED EXACTLY -- two
                          # layers plus the orchestrator independently rebuilt worldgen and got
                          # 704->265 / 696->242 / 709->258 and 48.08 / 49.64. 1 decision, 6 patched,
                          # 6 deferred, 1 dismissed. ALMOST EVERY FINDING WAS OUTSIDE THE DIFF.
                          # HIGH 1 -- THE DRAW-SET ORACLE WAS STALE BY 8,104 CUBES. The world
                          # reports 45,261 exposed cubes, not the 53,365 that FOUR documents call an
                          # oracle that "must never change" -- including the LIVE vehicle runbook,
                          # which would have told Wolf to expect the old figure at the very sitting
                          # that judges AC10, and read a 15.2% drop as a regression. NO TEST COULD
                          # HAVE CAUGHT IT: the only 53_365 in the crates is hand-written sample
                          # data in a test about empty cut faces (capture.rs:1460), so the oracle
                          # exists solely as a println read by eye. Corrected in all four places and
                          # REWORDED AS A MEASUREMENT that tracks world content, not a constant --
                          # which is what it always was. Confirmed independently by the orchestrator.
                          # HIGH 2 -- THE RECORD SAID A PIN WAS "NOT AT RISK" WHEN IT HAD MOVED.
                          # spawn_positions_for_seed_42_are_pinned had its terrain fingerprint
                          # re-pinned; no Debug Log, Completion Note or Change Log said so. The
                          # positions genuinely did not move, so the reasoning was right and the
                          # conclusion wrong. Aggravating: that fingerprint is now the TIGHTEST
                          # tree-stream guard in the repo -- the 230-300 band only discriminates
                          # roll denominators outside ~36..52 -- and the record hid it.
                          # WOLF LOOKED AT A FRAME DURING THE REVIEW AND REPORTED TWO THINGS. Both
                          # were then MEASURED rather than left as impressions, and both are
                          # pre-existing geometry that 9.4 did not cause.
                          # (a) "fix also snow cover" -- RULED AND FIXED HERE (a634235).
                          # has_snow_laden_crown tested sky exposure ALONE, which did exactly what
                          # its own doc comment says the material swap exists to avoid: place_trees
                          # stamps a foliage ring at surface+1 whose cells have open sky beside the
                          # trunk, so 1,246 of 1,824 ground-level skirt cells (68%) rendered at the
                          # bright crown colour -- a lit ring ON THE GROUND around every trunk. Snow
                          # now also requires the cell not to rest on terrain. Ground-resting bright
                          # cells 1,029 -> 0. Side effect that closes a review finding: 9.4's green
                          # now reaches 3,727 of 6,329 foliage cells (58.9%) instead of 2,698
                          # (42.6%), because the crown swap had been claiming the rest.
                          # (b) "some trees look like they don't have trunk at all" -- MEASURED AND
                          # THEN RULED IN, not deferred. 86 of 265 trees (32.5%) drew NO trunk, and
                          # it was exactly the height-4 trees, 100% of them. The trunk spans
                          # surface+1..=surface+height-1 while foliage rings sit at surface+1,
                          # crown_top-2 and crown_top-1, so at height 4 every trunk level is
                          # enclosed and is_exposed never fires. The trunks are not underground --
                          # they were buried in their own foliage.
                          # (c) WOLF LOOKED A THIRD TIME, AFTER THE SNOW FIX: "I see green boxes on
                          # ground level next to some full trees .. some trees are still partially
                          # even more now underground". THE SNOW FIX HAD NOT CAUSED IT, IT HAD
                          # REVEALED IT -- those cubes were previously painted with the bright crown
                          # colour and read as snow; correctly green, the ring showed for what it
                          # was. ONE CAUSE BEHIND (b) AND (c): place_trees stamped a foliage ring at
                          # surface+1, ON THE GROUND, around every trunk. It read as foliage lying
                          # on terrain (which is what this story is NAMED for), it sealed the lower
                          # trunk on four sides so a third of trees drew none, and it broke on
                          # slopes -- stamped at the TRUNK's surface across all eight neighbours
                          # regardless of their own terrain, so 285 cubes hung in the air and 296
                          # were dropped, on 110 of 265 trees. RING REMOVED (465f967): trees showing
                          # a trunk 179 -> 265 of 265, foliage 6,329 -> 4,505, foliage resting on
                          # terrain 1,649 -> 110, oracle 45,261 -> 44,984, trunk columns unchanged
                          # at 265 so the density band still holds. "UNDERGROUND" WAS MEASURED AND
                          # DISPROVEN: every trunk sits flush on its own column's ground and no tree
                          # cell is below its column's top terrain -- the trees read as sunk because
                          # their base ring was broken, not because anything was buried.
                          # THE FINGERPRINT IS RE-PINNED A SECOND TIME and it is disclosed this
                          # time, in the commit, beside the pin and in the story -- not disclosing
                          # the first re-pin was this review's second HIGH finding. THE ORACLE MOVED
                          # TWICE IN ONE STORY (53,365 -> 45,261 -> 44,984), which is the whole
                          # argument for rewording it as a measurement rather than a constant.
                          # Table now 6 rows, 6/6 KILLED, audit clean at 404, full gate GREEN.
                          # THE SABOTAGE ROW CAUGHT THE REVIEW'S OWN BAD TEST, which is the third
                          # time in this story that RUNNING the table beat reading it: the first
                          # fixture for the snow fix stacked foliage above the skirt cell, so the
                          # pre-existing sky-exposure clause fired first and the new clause was
                          # never reached -- the assertion passed for the wrong reason and the row
                          # SURVIVED. Rebuilt; 5/5 KILLED. Kill lines MOVED with the patches
                          # (appearance.rs :298->:316, :338->:356), which the record now says.
                          # ALSO PATCHED: the separation guard compared against hardcoded stone/soil
                          # literals untied to the table -- a stale-reference trap that would keep
                          # the guard GREEN while validating foliage against a colour nothing
                          # renders; now read from material_color(). TreeTrunk added to that loop:
                          # trunk and foliage are the two materials of ONE drawn object and the OLD
                          # foliage sat 38.7 from trunk, already inside the 40.0 floor, with no
                          # terrain-vs-terrain test in existence to notice. Two Task 5 subtasks were
                          # UNTICKED -- they were ticked for a median that was never recorded; the
                          # prose had been honest while the checkboxes were not.
                          # STILL UNMET AND UNCHANGED BY ANY OF THIS: AC7 (the 9.1 x 9.4 luminance
                          # interaction) and AC10 (Wolf's eye). The acceptance layer confirmed
                          # independently that NO part of AC7 was headlessly checkable and skipped
                          # -- the only rendering capture test is #[ignore]d, "requires a real
                          # render surface". Verification: full gate GREEN, audit clean at 403 rows,
                          # whole table re-run 5/5 KILLED. Reaped 107.2 GB of layer caches.
                          # PRIOR: STORY 9.4 -> REVIEW. Dev complete, metrics recorded.
                          # THIS BLOCK AND THE TWO BELOW IT ARE BACKFILLED on 2026-08-29: 9.4's
                          # create, in-progress and review transitions each moved the status line
                          # only, so this header sat four events stale and still led with 9.1's
                          # review. The stories' own files carry the detail; this restores the
                          # narrative anyone reads first.
                          # Density roll `0..12` -> `0..48`: 704 -> 265 trees on the default seed
                          # (242 / 258 on seeds 42 / 7451), all inside Wolf's ruled 230-300 band,
                          # guarded by a deterministic named-seed test that counts trunk COLUMNS
                          # rather than cells (trunk height is 4..=6, so cells are the wrong
                          # oracle). Foliage (55,73,84) -> (44,100,58): clears stone by 48.1 and
                          # soil by 49.6 against the shipped MIN_MARK_SEPARATION of 40.0 it sat
                          # 9.9 from, blue >= red so W2's terrain invariant stands.
                          # THE TUI NEEDED NO CHANGE, and the reason is the find: palette.rs:199
                          # has rendered foliage green at (54,106,78) since it was written, so the
                          # 3D client was the odd one out and this brings the two into agreement.
                          # No test forced it -- the gui<->tui cross-check covers MARKS only.
                          # DELEGATED RUN HARNESS-KILLED mid-Task-3, the 9.1 failure mode again.
                          # NOT auth: the wrapper's 401 warning fired 7 times, all false positives
                          # (a source line number, git blob hashes, the echoed prompt); quota was
                          # probed healthy and the banner read gpt-5.6-terra / high, no drift. The
                          # commit-cadence floor paid for itself -- Codex had committed Tasks 1-2
                          # rather than squashing, so work resumed from a green commit, and the
                          # kill landed in Task 3's RED phase, which is correct TDD caught
                          # mid-cycle. Its committed half was re-verified, not trusted.
                          # THE SABOTAGE TABLE TOOK TWO CORRECTIONS, both caught only by running
                          # it and READING THE KILL LINE. (1) A row lowering MIN_MARK_SEPARATION
                          # itself SURVIVED, correctly: you cannot sabotage an assertion and
                          # expect that same assertion to catch it. (2) Its production replacement
                          # (foliage -> brown) then killed at the equality pin rather than at the
                          # invariant -- the exact weak-kill shape 9.1's review found, one story
                          # later, in a table written after it. Final 4/4 KILLED at worldgen.rs
                          # :200/:201 and appearance.rs:298/:338, audit clean at 402 rows, cold
                          # full gate GREEN after `cargo clean -p gui -p sim-core`.
                          # tech-art-guidelines carried a THIRD stale figure, found while doing
                          # the Task 8 check: the exposed crown was documented at the ARTIFACT's
                          # (172,186,210) against a shipped (156,170,196), trimmed at round 7.
                          # CARRIED INTO REVIEW UNMET, NOT PAPERED OVER: AC7, the 9.1 x 9.4
                          # luminance interaction. The capture tests decode static COMMITTED PNGs,
                          # which cannot see a tree-density change, and no devpod can open a
                          # window -- so neither the 70-180 band nor 9.1's 0.6651 % blown-pool
                          # ceiling was re-measured. Direction is known (fewer dark tree skirts
                          # can only raise the valley floor and only enlarge the pool); the
                          # numbers are not. AC10 is Wolf's eye and is unmet. The vehicle card
                          # takes the BASELINE reading first, because 9.1's blow-out is still open
                          # and a capture may exit 101 before trees are the cause.
                          # PRIOR SAME DAY, AND IT IS THE HIGHEST-SIGNAL OPEN FACT IN EPIC 9:
                          # 9.1's VEHICLE OBSERVATION -- Wolf, on the vehicle: "9.1 lights are
                          # still blown out .. shadows are not strong enough or light is still too
                          # strong." THE BLOW-OUT IS NOT CLOSED; shadows were not enough. This is
                          # the story's PREDICTED outcome, stated at creation and expected
                          # independently by both Opus review layers: shadows remove light passing
                          # THROUGH geometry and cannot dim the unoccluded pool around the fire.
                          # So AC14 is answered NO and 9.1 has done its job -- it proved with a
                          # new instrument that shadows are insufficient. STILL OWED before 9.1
                          # closes or escalates: the NUMBERS -- AC13's controlled shadows-off/on
                          # pair and AC12's fps -- because "still blown out" is a judgement and
                          # the ceiling is a measurement. WHAT MUST NOT HAPPEN NEXT: a quiet
                          # intensity / amplitude / range / emissive change to force the ceiling
                          # green. Those levers are withheld pending a NEW ruling from Wolf and
                          # belong in a follow-up story with its own before/after numbers.
                          # PRIOR SAME DAY: STORY 9.4 CREATED (ready-for-dev), premises verified
                          # against source rather than inherited. TWO EPIC CLAIMS FALSIFIED: trees
                          # draw from a dedicated STREAM_TREES, so terrain heights, camp origin
                          # and spawns do NOT move, and no mutation row anchors on the density
                          # literal -- the epic's blast-radius paragraph is overstated and the
                          # radius is tile contents only. The density curve was MEASURED, not
                          # estimated (704 / 531 / 400 / 265 at rolls 12/20/30/48), revealing that
                          # the 2-cell spacing exclusion damps the knob so a 2.5x cut in the roll
                          # removes only 43 % of trees. Two rulings taken: (W1) band 230-300 at
                          # `0..48`; (W2) GREEN, not brown -- the epic's "brown/green" collides
                          # with the shipped rgb[2] >= rgb[0] invariant, which brown cannot
                          # satisfy, so the invariant stands and green carries the separation.
                          # SAME COMMIT, A RECORD DEFECT REPAIRED: deferred-work's buried-
                          # highlight entry went unstruck for two days after 8.2 fixed it, and
                          # 9.2 was written FROM it -- fabricating scope for shipped work. Entry
                          # struck with its closing commit named, 9.2 and 9.3's epic text
                          # corrected. 9.3's AC1 was additionally unmeetable: it asks for four
                          # mark colours when only three persistent marks exist and Clear's
                          # preview IS the hover by design.
                          # PRIOR: STORY 9.1 CODE-REVIEWED -> in-progress. 4 layers, all live, fresh
                          # context, NO coverage holes -- the second clean four-layer full house.
                          # 17 findings: 1 decision (Wolf: the blown-pool ceiling stays HARD for
                          # Epic 9's shared sitting; the PNG is written before validation so
                          # evidence survives every abort), 8 PATCHED, 6 deferred, 2 dismissed.
                          # THE HIGH WAS NOT IN THE CODE -- it was the vehicle card: its own
                          # "-dirty" stop rule halted the shadows-off half of AC13's only
                          # controlled pair (that half NEEDS an uncommitted edit), it never named
                          # the edit that disables shadows, and it carried no --capture command at
                          # all while that run is EXPECTED to exit 101. AC13 was unexecutable as
                          # written, independent of the devpod limit. Card rewritten; the feature
                          # layer proved the -dirty trap by rebuilding a modified clone.
                          # THE PATCH PASS FOUND ITS OWN DEFECT: pointing the straddle asserts at
                          # the shipped constant was NOT enough -- the equality pin sat ahead of
                          # them and fired first, so row (c) was still a pin-kill and the
                          # strengthened test looked identical from outside. Assertion ORDER was
                          # load-bearing and no layer had said so. Reversed; row (c) now dies at
                          # tests/capture.rs:194 on current_pool > BLOWN_POOL_FRACTION_CEILING.
                          # Also patched: p99's index was f32 (wrong sample for 225,210 of the
                          # first 3M pixel counts -- dormant at every resolution this repo makes,
                          # and the exact broken-instrument class); the reconcile guard is a
                          # NOT-REWRITTEN guard, not a re-insert guard, because project.rs:587
                          # only re-inserts on a light-KIND change and a campfire's kind is static
                          # -- Task 4's premise was falsified and the test renamed; AC8 recorded
                          # PARTIALLY MET (panic->101 traced through three resume_unwind sites AND
                          # measured live, but no test spawns the binary and the observer needs a
                          # render surface, so it closes only on the vehicle); tech-art-guidelines
                          # ambient/directional still said 6,000/30,000 against a shipped
                          # 4,500/22,000. Verification: full gate GREEN, audit clean at 398 rows,
                          # whole table re-run 5/5 KILLED with the p99 figures byte-identical.
                          # NOT PROVEN BY ANY OF THIS: ACs 12-15 and AC6's on-vehicle half. No
                          # frame has been looked at. Story 9.1 also still carries ZERO self-gate
                          # coverage (Codex's single pass was harness-killed) -- four review
                          # layers are not a self-gate and the two are counted separately.
                          # PRIOR: STORY 9.1 -> REVIEW. Tasks 1-5 + 7 done: blown-pool instrument calibrated
                          # on committed pixels (boot7 0.6651% vs today 0.9883%, ground median 123.4
                          # on BOTH), campfire-only shadow maps, cold full gate GREEN, 5/5 sabotage
                          # rows KILLED. TWO HOLES CARRIED INTO REVIEW: the Codex self-gate produced
                          # ZERO passes (harness-killed), and Task 6's vehicle ACs 12-15 are unmet --
                          # they need Epic 9's shared gingerspice sitting and cannot run in a devpod.
                          # PRIOR: delegated to Codex (gpt-5.6-terra/high) on
                          # baseline 15b3635. Quota probed before handoff: weekly window 8%,
                          # 5h window 27% -- the 8.2 exhaustion window has reset.
                          # PRIOR SAME DAY: STORY 9.1 CREATED (ready-for-dev), epic-9 in-progress. Two spec
                          # defects found at creation in the epic's own 9.1 text: the 44.8M
                          # premise is stale (fixed 2026-08-22, the defect survived it) and
                          # the epic's AC1 is VACUOUS -- the median band reads 123.4 on every
                          # frame ever approved AND every frame ever rejected. Three rulings
                          # taken from Wolf; instrument calibrated on committed pixels.
                          # PRIOR SAME DAY: THE GFX PASS IS PLANNED AND TRACKED: epics 9 + 10 enter this file at
                          # `backlog`, 9 stories, no story files yet. Order ruled 9 -> 10 -> 8.3.
                          # PRIOR SAME DAY: STORY 8.2 DONE -- MERGED, PR #35 (a949835). M2 at 10/11. AC18 met on all
                          # four modes on the vehicle, read back by a second client; NFR6 ~140 fps
                          # against floors of 60/30. AC13's rendered half and AC19's reads-clearly
                          # half carried to the gfx pass UNCLAIMED. A channel that worked read as a
                          # THIRD dead mode across three reads -- its marks are consumed in ~2.5 s,
                          # so an after-the-fact instrument cannot see them. Pause first.
                          # PRIOR: VEHICLE SESSION STARTED -- AND IT FOUND TWO MODES THAT HAD NEVER WORKED.
                          # Wolf drove it by hand and reported "fragile and confusing... sometimes it
                          # loses dragged tile color... channelling, what should it do". That was FOUR
                          # defects. The big one: CHANNEL AND STOCKPILE DESIGNATION WERE COMPLETELY
                          # INERT. Picking only ever resolves a Solid/Ramp cell; sim-core filters both
                          # commands on standability and DROPS THE REST IN SILENCE -- no error, no ack,
                          # no log. Proven against the real binary: channel at the picked cell -> 0
                          # designations, same rect one cell up -> 9. AC10 was FALSE for two of its
                          # three clauses and AC19's four hand drags were never possible. Clear was
                          # half-working. Plus: the preview erased itself on any frame whose ray missed
                          # terrain while the drag stayed live (that is the "loses colour"), and channel
                          # marks never got 7.2's buried-mark fix so they sat sealed inside rock.
                          # THE HOLE UNDERNEATH: no test ever asked the daemon whether it KEPT anything;
                          # every gui test asserts what the CLIENT QUEUES, and the capture instruments
                          # are blind the same way (a discarded rect reads `0 of 0` and passes). The
                          # review's "genuinely wired, 15 hops to real callers" was true about the
                          # client's hops and structurally blind to the sim's filters.
                          # RULED (Wolf): target = the neighbour across the face the ray entered, giving
                          # AC13's Face a second BEHAVIOURAL consumer. FIXED 8ccb569: client-core gained
                          # is_standable, the preview shows only cells the sim will keep, and
                          # simd/tests/serve.rs now runs the round trip with the REAL DAEMON AS JUDGE.
                          # Suite 165 -> 175, mutation table 27 -> 33, round 2 = 6/6 KILLED no survivor,
                          # full gate GREEN. Three round-1 rows re-pointed first, all caught by the
                          # audit BEFORE the run. gui.exe MUST BE REBUILT -- the 05:13:38Z binary has
                          # two dead modes in it. STILL OWED ON THE VEHICLE: AC19, AC13's rendered half,
                          # AC15/16 end to end, AC18.
                          # PRIOR: 8.2 TASK 7 RUNBOOK WRITTEN AHEAD OF THE SESSION --
                          # 8-2-signoff/task-7-vehicle-runbook.md, sourced from aca07be rather than
                          # carried forward from 7.2's, because --drag and --at-tick did not exist then
                          # and three of its flag rules came only from the 08-26 review. It closes AC13's
                          # rendered half, AC15/16 end to end, AC18 and AC19; NOTHING IN IT HAS RUN --
                          # it is a recipe to be corrected by the session, not a record of one. The two
                          # items carrying the weight: the DDA march's hit face and the slab's rotation
                          # onto the face normal were BOTH entirely unpinned before the review, are now
                          # patched and tested, and have still never been RENDERED. Separately, the
                          # review's `.codex/` deferral was REOPENED AND CLOSED: that directory holds a
                          # live auth.json and .gitignore's secret patterns (*token*, *secret*, .env*)
                          # do not match that name, so one `git add -A` would have committed it.
                          # STILL THE ONLY WAY 8.2 CLOSES: a live gingerspice session.
                          # PRIOR: STORY 8.2 CREATED -> ready-for-dev (see its block below).
                          # STORY 8.1 DONE -- MERGED, PR #34 (f9df762). M2 at 9/11. Picking works: ray from
                          # the rendering camera, 389 tests, 9/9 mutations, gate green cold four times over
                          # (twice by agent, once by Wolf, once by the pre-push hook). AC12 met with margin.
                          # THREE THINGS THIS STORY LEAVES BEHIND, none of them blocking but all of them owed:
                          # (1) AC5's RENDERED half is unmet -- the hover slab is buried under any drawn tile
                          # above it -- DEFERRED to 8.2 by Wolf awaiting final gfx. 8.2 designates by pointing
                          # at exactly those vertical faces, so it MUST be solved there and cannot slip again.
                          # (2) M2-7 (no build script, no SHA stamp in gui) bit a FOURTH time -- the vehicle
                          # build wall-clock was not captured. Owed a ruling at the Epic 8 retro as
                          # load-bearing, not housekeeping. (3) R1's territory split STILL has no mapping for
                          # the M2 crates and was hand-adapted for the SECOND story running -- also owed a
                          # ruling at the Epic 8 retro. PROCESS NOTE CONFIRMED THIS STORY: a patch round is
                          # NOT re-reviewed (5.4/6.x/7.1/7.2 all went patch -> live-gate -> sign-off). Review
                          # once, patch in one verified pass, close. PRIOR: 8.1 -> REVIEW, ALL TASKS CLOSED. TASK 6 / AC12 MET ON THE VEHICLE:
                          # >140 fps sustained at BOTH working zoom and full vista (gingerspice / native
                          # Windows / NVIDIA, F3 overlay), against NFR6 floors of 60 and 30 -- 2.3x and 4.7x
                          # margin. Read on a FRESH rebuild from the patched tree, source commit 3f50178 (the
                          # last commit touching crates/; everything after it is _bmad-output/ only). Picking
                          # casts ONE RAY PER FRAME bounded by the world diagonal, so its cost does not scale
                          # with tile count -- the margin is what the mechanism predicts, not luck. NOT
                          # STAMPED: the wall-clock build time was not captured, which is M2-7 (no build
                          # script, no SHA in gui) biting a FOURTH time -- it should be read as load-bearing
                          # at the Epic 8 retro, not housekeeping. AC5's RENDERED half remains unmet and
                          # DEFERRED to 8.2 by Wolf (hover slab buried under any drawn tile above it); its
                          # colour arithmetic is proven, its pixels are not. PRIOR: REVIEW-PATCH ROUND 1 COMPLETE -- all 12 patches applied in one commit
                          # (3f50178), one full-tier gate at the end, GATE GREEN cold, 389 tests (was 382).
                          # Mutation round 3: 9/9 KILLED, zero APPLY-FAILED, three new rows (AC9's ClientLocal
                          # tag, AC5's separation floor, the wiring call site). Row 2's anchor went STALE inside
                          # the round -- the foliage patch reformatted the condition it matched -- and the dry
                          # check caught it at 0 matches BEFORE the run. Separately, `is_tree_foliage` as first
                          # written duplicated story 5.4's sabotage anchor and broke a row in ANOTHER story's
                          # table; the gate's mutation audit caught it and the helper was rewritten. The inert
                          # seam moved out one level and SHRANK but did not vanish: `run()`'s own three lines
                          # (plugins, configure_client_app, app.run()) still need a socket and a window. STILL
                          # OPEN: Task 6 / AC12, vehicle-bound; gui.exe MUST be rebuilt (the 08-25 build stamp
                          # predates every patch). AC10/AC11 have still never executed in any process anywhere.
                          # PRIOR: STORY 8.1 CODE-REVIEWED -> in-progress. 4 layers + 1 narrowed re-run, NO coverage
                          # holes. 5 decisions ruled by Wolf, 12 patches LEFT AS ACTION ITEMS for a FRESH SESSION
                          # (patch-in-a-fresh-session rule held deliberately, not by omission). Convergence 4-in-22,
                          # the first favourable R1 data point; R1 STILL has no M2-crate mapping and was hand-adapted
                          # for the SECOND story running -- owed a ruling at the Epic 8 retro. Blind Hunter timed out
                          # at ~18min, was SALVAGED not killed bare, and its partial carried the run's one scare: an
                          # oracle disagreeing with first_visible_hit on 24/36000 rays. Its one permitted re-run
                          # SETTLED it -- all 24 are ORACLE artifacts (fixed 0.005 sampling step vs corner grazes
                          # 0.000065-0.0018 wide). The DDA is PROVEN correct, not assumed. CORRECTION TO THE RECORD:
                          # the --capture path CANNOT run in any devpod (bevy_winit, no DISPLAY) -- so AC10 and AC11's
                          # instrument has NEVER EXECUTED IN ANY PROCESS ANYWHERE, and joins AC12 as vehicle-bound.
                          # AC5 is NOT met: the hover slab is invisible on every tile with a drawn tile above it
                          # (cliff faces, corridor walls, shaft sides) AND near the campfire -- both DEFERRED by Wolf
                          # awaiting final gfx, but 8.2 designates by pointing at exactly those faces, so it lands
                          # BEFORE 8.2 ships. PRIOR: STORY 8.1 HEADLESS HALF -> REVIEW. Mutation round caught --cursor inert at run(); fixed, 6/6 KILLED, gate green, 382 tests. Task 6/AC12 vehicle-bound and open. PRIOR: STORY 8.1 CREATED, epic-8 opened. Picking approach ruled by Wolf: the ray comes from the rendering camera (Camera::viewport_to_world), not from a second hand-rolled projection. All four of 8.1's epic premises re-verified against source and all four hold; render_to_world exists but had zero production callers until now. PRIOR:  # M2-TO-DATE RETROSPECTIVE HELD (Epics 5+6+7 in one, filed as epic-5-retro-2026-08-23.md). epic-5's stale in-progress key flipped to done; retros for 5, 6 and 7 all marked done against the one record. 12 action items opened, E4-P1 and T4 closed, E2 dropped as obsolete.
                          # 2026-08-20 VEHICLE SESSION -- 6.1, 6.2 and 7.1 all watched running on gingerspice and SIGNED OFF by
                          # Wolf ("i think we are done with these stories"). Epic 6 CLOSED. Wow beat 2 is signed.
                          # THREE defects found by eye that the whole suite was green on, all fixed and sabotaged:
                          # (1) 6.1 stone items spawned at scale 1.0 -- a terrain-sized block standing in the tile it
                          # was dug from, so a dug tile visually REFILLED and a worked face read as untouched rock;
                          # it also geometrically enclosed AC8's debris chips, which could never be seen. Fixed at
                          # STONE_ITEM_SCALE 0.4 (the largest scale keeping all four chips outside the cube).
                          # (2) 7.1 a FAILING capture wrote no PNG at all -- save_to_disk and the range checks were
                          # two observers on one event and Bevy ran validation first, so the run whose frame most
                          # needed looking at produced no frame. Now sequenced in one observer.
                          # (3) 7.1 the 5.4 range band was judging a scene it was never calibrated against: at a cut
                          # the sample window shows interior rock, not sky-lit snow, so z 9 read 67 vs the 70 floor
                          # and panicked on a picture Wolf confirmed fine. Band now scoped to the world top; the
                          # black/uniform gates stay unconditional. This is the SAME correction the 08-19 review made
                          # one assertion higher (lanterns) and stopped short of -- the hole relocated one level down.
                          # Sabotage 14->17 (6.1), 13/13 (6.2), 14->16 (7.1), ALL KILLED; gate GREEN cold on each branch.
                          # CARRIED OPEN past sign-off, recorded not blurred: the CAMPFIRE still reads blown at full
                          # depth (diagnosis: 04e6de5 raised its amplitude 0.11->0.40, peaking at 44.8M, 40% above the
                          # value 5.4 sized against the approved artifact -- not 6.2's lanterns); 7.1's AC10
                          # surface/underground ruling never given; ,/. controls still PROVISIONAL and the mousewheel
                          # confirmed unclaimed in code. 6.1's AC15 is closed on PRE-FIX evidence (2,557 px measured
                          # on the binary where the item was a full block) -- a fresh pair was offered and declined.
                          # A SABOTAGE ROW NAMING A TUNED LITERAL DIES SILENTLY WHEN THE KNOB MOVES: 6.1's torch
                          # flicker row had been dead since 04e6de5; 6.2's lantern row was retargeted with the drop.
                          # NEXT = 7.2 (ready-for-dev, BLOCKED on Task 0, Wolf's artifact).
                          
                          # Prior 2026-08-19: 7.1 code-reviewed (4 layers, no coverage holes) -> in-progress, 13 patches applied, 13/13 mutations killed.
                          # VEHICLE RUNBOOKS WRITTEN for all three in-progress stories (6.1, 6.2, 7.1). They share ONE binary --
                          # 7.1's branch is stacked on 6.2 on 6.1 -- so all three live sessions run in ONE sitting off one gui.exe
                          # and one simd. Spine: _bmad-output/implementation-artifacts/vehicle-session-runbook.md. Order 6.1 -> 6.2
                          # -> 7.1 (6.1 designates the dig the other two need). NOTHING ELSE blocks any of the three.
                          # hole; the feature was watched running live and AC6 is genuinely closed,
                          # but three one-line deletions each killed wow beat 2 with the suite GREEN —
                          # run one's defect class, one level below where AC6 looks. 1 decision + 8
                          # patches applied, gate green cold, mutations 8 → 14 all killed, 16 LOW
                          # deferred. Tasks 6 + 9 and ACs 7/12/13/15/16/17 still OPEN — nothing has
                          # been observed on the vehicle. Prior same day:
                          # 6.1 HEADLESS HALF DONE → review (two Codex runs; four seam ACs were
                          # ticked-but-undelivered on run one and falsified by sabotage, closed for
                          # real on run two; 8/8 mutations, gate green). Task 0 approved earlier the
                          # same day. Tasks 6 + 9 and ACs 13/16/17 remain owed to the vehicle and to
                          # Wolf. Beat 2 signs without UX-DR14's carried stone; dig site not
                          # re-picked; the dig face reads at working zoom (measured: 0.30% of frame).
                          # Prior same day: 6.1 STORY CREATED (ready-for-dev) and Epic 6 opened. Dig site named and
                          # verified live on the shipped seed: rect [58,68,9]-[64,69,9] = 8 mineral
                          # tiles, all sky-exposed and unoccluded at the boot framing, dug in 52 ticks,
                          # 8 stone items left standing; wander motion baseline 47% of ticks. Task 0
                          # carries a BLOCKING clarification for Wolf: UX-DR14's "picks something up
                          # and carries it" is not on the wire (a carried stone keeps its pickup tile),
                          # so beat 2 is signed without it or a sim story is spun. Prior:
                          # 5.4 dev STARTED (in-progress, delegated to Codex). Earlier same day —
                          # Task 0 gate OPENED: Wolf approved the sign-off artifact
                          # (5-4-signoff/candidate-artifact-2026-08-15.png) after a four-pass iteration on the
                          # parked 08-14 candidate — draft-2 framing, spruce trees with visible trunks, FULL
                          # wire-true tree density (thinned variant offered, NOT taken → no worldgen change).
                          # Implementation may start. Prior: 5.3 DONE 2026-08-14 (AC13 falsified+fixed, AC26's
                          # live half handed to 5.4, gate green, 5/5 mutations killed).
project: frostvein
project_key: NOKEY
tracking_system: file-system
story_location: /workspace/projects/frostvein/_bmad-output/implementation-artifacts

development_status:
  epic-1: done
  1-1-a-seeded-frozen-world-exists: done
  1-2-the-daemon-serves-the-world: done
  1-3-behold-the-frozen-world: done
  epic-1-retrospective: done
  epic-2: done
  2-1-the-world-runs-on-its-own-clock: done
  2-2-dwarves-wander-the-frost: done
  2-3-master-of-time: done
  2-4-the-world-endures: done
  epic-2-retrospective: done
  epic-3: done
  3-1-give-the-order: done
  3-2-the-dig: done
  3-3-the-haul-and-the-skeleton-walks: done
  epic-3-retrospective: done
  # CLOSED EARLY 2026-08-08 (sprint-change-proposal-2026-08-08.md). 4.1a delivered but
  # deliberately NOT merged; 4.1b dropped; FR24 withdrawn from phase one. Wolf judged the
  # live depth view "quite far from wow effect" — 3D-in-TUI is abandoned. MILESTONE 1 CLOSES
  # HERE with all four phase-one success criteria met. The ambition moves to a Bevy client
  # in Milestone 2, which needs its own product + architecture pass before any story exists.
  epic-4: done
  # 4.1 SPLIT IN TWO at the Epic 3 retrospective (Wolf, 2026-08-08). The single story carried the
  # raycast renderer AND the sub-voxel creature models, and was larger than 3.2 -- which shipped at
  # 17 ACs and exhausted a full week of Codex quota. FR23/FR24 sign-off lands on 4.1b. The keys
  # below are provisional until action item E1 rewrites epics.md; sprint-planning owns the final shape.
  # done but DELIBERATELY NOT MERGED — kept on branch `4-1a-behold-the-fortress-in-depth`,
  # `main` stays 2D-only (Wolf, 2026-08-08). Review was clean (4 layers, gate green, honest
  # evidence); the story answered its question in the NEGATIVE — Wolf judged the live depth
  # view "quite far from wow effect". 3D-in-TUI is abandoned. Replan via correct-course.
  4-1a-behold-the-fortress-in-depth: done
  # DROPPED 2026-08-08, never started, no story file was ever written. It carried sub-voxel
  # glyph dwarf models AND FR23's deferred icy-grim sign-off — both inside a terminal renderer
  # Wolf has now judged, live, as unable to reach the effect he wants. FR23's PHASE-ONE
  # obligation was already MET at 3.3; only the 3D ambition moved to Milestone 2. Do NOT read
  # this drop as FR23 going unmet. Full record: epics.md + sprint-change-proposal-2026-08-08.md
  4-1b-dwarves-in-depth: dropped
  # DELIBERATELY SKIPPED, not forgotten (Wolf, confirmed 2026-08-23). Epic 4 was ditched once 4.1a
  # proved the wow effect was not reachable in a TUI, and the retro was skipped with the epic. Its
  # one orphaned action item, E4-P1, was closed on M2's evidence at the 2026-08-23 retrospective.
  # Do NOT re-flag this as process drift.
  epic-4-retrospective: optional

  # ============================================================================
  # MILESTONE 2 — THE BEVY CLIENT. Planned 2026-08-09; readiness-checked and its
  # nine findings applied the same day (implementation-readiness-report-2026-08-09.md).
  # Four epics, 11 stories, inside the PRD's 10-14 counter-metric. Two new crates:
  # client-core (mirror, protocol-only) and gui (Bevy). Six crates total.
  #
  # ORDERING IS NOT TASTE — it is two hard constraints. client-core exists before
  # either client consumes it (AD-13), and the gui envelope is proven before
  # anything builds on it (5.3). Wow beat 1 lands at 5.4, story 4 of 11, which
  # completes at 36% of the milestone: CM2's first-third mandate is met AT THE
  # EDGE. Splitting 5.2 or 5.3 pushes it to 42% and breaches CM2, so a split is
  # the trigger to re-check CM2 on the record, never a free move.
  #
  # CUT ORDER, decided in advance (PRD counter-metrics): FIRST 6.2 (lanterns) —
  # torches and the campfire still carry the warm/cold wow. THEN 8.1+8.2 shrink
  # away, leaving 8.3 with speed control only and the TUI keeping designations;
  # if that fires, 8.3's walking-skeleton AC MUST BE REWRITTEN, not reinterpreted
  # (the dig is designated from a TUI client and watched in gui). 5.2 is on NO
  # cut list — AD-13 is load-bearing on it.
  # ============================================================================
  # CLOSED 2026-08-23 at the M2 retrospective. All four stories were done; the epic key was
  # never flipped and sat stale for a week -- the same `encoded != recorded` drift Epic 2 named.
  epic-5: done
  # 5.1 carried the vista-mountain-silhouette decision (readiness fix #5), the last of the
  # spine's three decisions owed. ANSWERED 2026-08-09 (Wolf): YES — in-grid terrain IS shaped
  # for a skyline, tuned inside 5.1 to a headless-testable target (height span >=16 levels).
  # 5.4 builds on that answer and does not re-open it.
  # Second decision taken at the same time: THE CAMP IS REAL — 5.1 clusters all five dwarves
  # into a deterministic camp site near the map centre, because a campfire with no dwarves
  # around it does not deliver UX-DR5's warm/cold read that 5.4's wow rests on. Consequence:
  # the pinned seed-42 spawn positions and the terrain fingerprint both change, deliberately.
  5-1-the-world-grows-things-that-glow: done
  # Split line if it overruns one session: the crate/mirror boundary. On no cut list.
  5-2-one-mirror-two-clients: done
  # ALLOWED TO BE UGLY — grey boxes orbiting at speed is a pass; visual bars are 5.4's.
  # Carries M2's only unproven-until-run risk: a Bevy window rendering on WSLg. If the
  # envelope does not hold that is the story's FINDING, reported, never worked around
  # in production code — fallback ladder recorded in Epic 5 (force GL via WGPU_BACKEND,
  # then the spine's deferred native Windows build). Eight stories sit downstream.
  # Split line: envelope+lifecycle vs projection+instruments.
  5-3-a-window-onto-the-valley: done
  # WOW BEAT 1. Sign-off gate BOTH halves (UX-DR22): Wolf approves a "here is what you
  # will see" artifact BEFORE implementation starts, and the story is done only when he
  # has viewed the built result live against it. CANNOT BE CLOSED BY A DEV AGENT.
  # Story file created 2026-08-14. Task 0 APPROVED 2026-08-15 (candidate-artifact-2026-08-15.png,
  # full wire-true tree density — no worldgen change) so the opening half is met and
  # implementation may start. Closing half still owed: Wolf views the built boot frame live
  # against the artifact. Live half runs on the proven native-Windows vehicle; NFR6's WSLg
  # figure stays owed to the retro. Inherits 5.3's AC26 debt.
  # Headless half implemented (Codex/terra, 2 sessions) + orchestrator-verified: gate green,
  # 5/5 mutations killed. Moves to code review. Vehicle-bound remainder (Tasks 6-9 live
  # halves, AC14/16-18) and Wolf's AC19 closing sign-off stay OPEN — review does not close
  # this story; only Wolf does.
  # Code review 2026-08-15 (fresh session, 4 layers): headless substrate solid, but six HIGH
  # findings predict the composed frame fails the artifact comparison (light scale, atmosphere
  # placement, fog-vs-zoom, ice-hiding cap). 3 decisions ruled, 11 patches left as action
  # items for a fresh patch session, 5 defers. Back to in-progress.
  5-4-the-cold-boot: done
  # ONE M2-to-date retrospective covering Epics 5, 6 and 7 -- Wolf's scope ruling 2026-08-23,
  # taken because the repeating defect classes only read as patterns across epics. Filed as
  # Epic 5's record: epic-5-retro-2026-08-23.md. Epic 4's retro was DELIBERATELY SKIPPED when
  # Epic 4 was ditched; nothing here relitigates it.
  epic-5-retrospective: done
  epic-6: done
  # WOW BEAT 2 — the beat the PRD calls the magic; a client achieving only beat 1 has
  # FAILED the milestone. Sign-off gate applies. Also cannot be closed by a dev agent.
  # Its dig must be at a surface-visible face designated from a TUI client (readiness
  # fix #4) — z-slicing does not arrive until 7.1.
  # Story file created 2026-08-16. Named dig site [58,68,9]-[64,69,9] (8 mineral tiles, verified
  # sky-exposed + unoccluded + fully dug live). Task 0 sign-off artifact is the blocking gate and
  # is a VEHICLE session on the shipped 5.4 binary — the dig and its rubble already work, so the
  # artifact is the renderer's own before/after pair plus a "what you will NOT see" list.
  # Dev STARTED 2026-08-16. TASK 0 CLOSED — WOLF APPROVED THE ARTIFACT 2026-08-17, AC1 MET, THE
  # GATE IS OPEN. Two rulings, both recorded in 6-1-signoff/what-you-will-see.md: (1) wow beat 2 is
  # signed WITHOUT UX-DR14's carried stone — no sim story, no wire change, clause FORMALLY NOT
  # DELIVERED IN M2, and it is unobservable here anyway since 6.1 places no stockpile so no haul job
  # and no carrying ever occurs; (2) the named dig site is NOT re-picked.
  # THE PAIR PRODUCED A FINDING and it is the gate working at the price of two screenshots. Wolf's
  # first reaction was "did not see the difference" and he was RIGHT: the dig site is 64x43 px =
  # 0.30% of a 1280x720 boot frame. Measured — 2,255 pixels differ between the pair and 1,625 (72%)
  # fall inside the window CameraRig predicted, so the dig is correct AND correctly located, merely
  # sub-legible at the vista. 6-1-digsite-inset.png (marked frames + 7x crops) makes it legible, and
  # line 8 was added to "what you will NOT see": THE DIG FACE READS AT WORKING ZOOM, NOT AT THE BOOT
  # VISTA. Consequence for AC7/AC8/Task 4 — rubble and debris chips are sub-legible at boot framing,
  # so the opening frame's visible weight rests on dwarf motion and light flicker (camp-scale).
  # AC15's windowed-comparison design is vindicated before a line of it is written: whole-frame
  # inequality here is satisfied by snowfall alone.
  # Dev now delegated to Codex — Tasks 1-5, 7, 8. Tasks 6 (live vehicle) and 9 (Wolf's closing
  # sign-off) stay vehicle- and human-bound and must never be faked.
  # HEADLESS HALF DONE 2026-08-17 → review. Codex dev in two runs. FIRST RUN: gate green, 4/4
  # mutations killed, scope exact — but FOUR SUBTASKS WERE TICKED WITHOUT BEING DELIVERED, and they
  # were the seam ACs the story exists to protect. AC6 was falsified by the orchestrator BY SABOTAGE:
  # deleting both blend_projection and flicker_projection from the live tuple — the entire headline
  # outcome — left the suite 54/54 GREEN, because projection_systems was called only by run() and
  # never by the headless tests. AC4/AC5/AC11 had no app-level test and no mutation. Tasks 1-3
  # reopened; a continuation handoff carried the RED evidence and required the sabotage as a pasted
  # deliverable. SECOND RUN closed all four in five commits (cadence floor met): the same sabotage
  # now turns 4 TESTS RED, the mutation table went 4 → 8 with all KILLED, gate green, 306 workspace
  # tests. Codex REFUSED to claim a green gate or fabricate a mutation table when its sandbox
  # detached output — the correct call, and the inverse of run one's false ticks, from the same agent
  # on the same story. LESSON: a checkbox is worth only what its verification is worth.
  # The self-gate is a COVERAGE HOLE for this story — it produced no conclusion on either run.
  # STILL OPEN and NOT closable by any agent: Tasks 6 (live vehicle) + 9 (Wolf's sign-off),
  # ACs 13/16/17. Review does not close this story; only Wolf does.
  # CODE REVIEW 2026-08-18 (4 layers, fresh context, none a coverage hole) -> back to in-progress.
  # The Feature Auditor ran the real pipeline against a live daemon and WATCHED IT WORK (592 distinct
  # sub-tile positions per dwarf, 32 client-local chips for 8 dug tiles with zero mis-marked, 5,582
  # distinct intensities per emitter), and AC6 is genuinely closed. BUT THE HEADLINE FINDING IS THE
  # SAME DEFECT CLASS AS RUN ONE, ONE LEVEL DOWN: AC6 proves a system is in the tuple and says nothing
  # about the tuple's INPUTS. Three one-line deletions -- observe_tick, delta_secs, elapsed_secs --
  # EACH KILLED WOW BEAT 2 WITH THE SUITE 57/57 GREEN, re-verified by the orchestrator on a /tmp copy.
  # The continuation run's four seam tests all hand-drive TickClock::advance, so they pass whether or
  # not production ever drives the clock. Closed by three production-drive tests + three mutations.
  # 1 decision + 8 patches applied and verified in one pass. Also fixed: the motion line printed AFTER
  # its assertion (a failing vehicle run gave a panic and none of the five numbers); the mid-blend
  # counter read only the clock, never a Transform, so it was true in a frozen world and still logged
  # 23 "strictly-between" frames with the blend sabotaged; the instrument read TickClock with no
  # ordering edge (raised by THREE layers; Bevy's ambiguity detection defaults to Ignore); the flicker
  # band assertion was a TAUTOLOGY (table-derived bound on a formula normalised to the same amplitude
  # -- the 1.1/1.2/1.3 self-referential antipattern again); a same-frame delta burst collapsed the tick
  # interval to its floor; item_count overwrote instead of taking a running max; AC15's window compare
  # was a near-vacuous >0 (measured 1,651 real vs ~5 from atmosphere) and now has a 200-pixel floor.
  # WOLF RULED the capture recipe: --frames 600 at 10 Hz against >135 fps is ~44 ticks against the
  # instrument's >=100 floor, so TASK 6'S FIRST COMMAND WOULD HAVE PANICKED BEFORE WRITING ANY PNG.
  # Raised to 1,500; the Task 0 recipe deliberately left untouched (that pair ran on the 5.4 binary,
  # which has no motion assertions -- rewriting it would falsify the record), and the Task 6 outputs
  # renamed to 6-1-motion-*.png so they cannot overwrite the approved pair they are compared against.
  # Gate GREEN on a cold rebuild, 311 workspace tests, MUTATIONS 8 -> 14 ALL KILLED. 16 LOW findings
  # deferred with file:line, including one the orchestrator raised rather than a layer: this story
  # closed two deferred-work.md entries without updating the register.
  # FOR THE RETRO: (1) R1's layer-territory split HAS NO MAPPING FOR THE M2 CRATES -- it names
  # sim-core / simd+tui+protocol, none of which a gui-only diff touches, so a literal reading leaves
  # both hunters idle; adapted for this story (Blind -> pure logic, Edge -> shell) and needs a real
  # mapping. (2) Convergence was measurable: 3 layers on the unordered instrument read, 2 each on the
  # self-referential band and the capture-recipe defect.
  # STILL OPEN and NOT closable by any agent: Tasks 6 (live vehicle) + 9 (Wolf's sign-off), and
  # ACs 7/12/13/15/16/17 -- NOTHING IN THIS STORY HAS BEEN OBSERVED ON THE VEHICLE. Only Wolf closes it.
  # LIVE VIEWING 2026-08-18 — TWO RULINGS BY WOLF, BOTH FIXED, both needing one more vehicle pass.
  # Dwarves SLIDE: AC2's headline outcome confirmed live. ACs 12, 13, 14 CLOSED ON MEASUREMENT --
  # before-run 107 ticks / 49 position changes / 628 mid-blend with zero commands; after-run 259 /
  # 158 / 1364 with max 3 working and 8 items; NFR6 >143 fps at BOTH working zoom and full vista
  # (gingerspice / native Windows / NVIDIA, ~2.4x and ~4.8x the bars). Three of those figures
  # corroborate story-creation measurements taken months earlier: 45.8% of idle ticks carry a
  # position change vs a 47% baseline, max working is exactly 3, items exactly 8. Ground-median held
  # at 123 across both runs = 5.4's converged look untouched. AC15 passed on that pair.
  # RULING 1: THE FLICKER READ AS STATIC. The mechanism was never wrong (5,587 distinct intensities,
  # 22% peak-to-peak measured in a real app) -- at ~0.1 stop it is simply below the perceptual
  # threshold. Amplitude raised torch 0.07 -> 0.30, campfire 0.11 -> 0.40 on Wolf's ruling; band
  # literals and the peak assertion moved with it so the test stays non-tautological. This was the
  # risk the code review flagged IN ADVANCE and carried into the runbook.
  # RULING 2: THE DIG SITE IS RE-PICKED to [55,62,9]-[56,65,9]. Wolf saw "walls still in place" and
  # was right: the original rect straddled a SLOPE, and slope tiles are Tile::Ramp, which is NOT
  # diggable -- 4 of the 14 were contiguous ramps at x=58-61,y=69 standing as a wall through the
  # excavation. THE STORY KNEW 8 OF 14 WERE DIGGABLE AND RECORDED IT AT CREATION; nobody drew that
  # the leftovers were CONTIGUOUS. 8th instance of the AC-text-defect class, and the FIRST caught
  # only by a human eye on the vehicle -- which is what the sign-off gate is for.
  # A first replacement was killed before it cost a session: raytrace from the boot camera found 5
  # of its 6 tiles behind TREE FOLIAGE, strictly worse than the site it replaced. The chosen site is
  # the ONLY 8-tile rect near the camp that is solid + sky-exposed + unoccluded + in frame (19 tiles
  # qualify at all), re-verified LIVE end to end: 8 designations, all 8 dug in 52 ticks (IDENTICAL
  # to the original), 8 items, max 2 working, NOTHING left standing. Tile count unchanged so AC7
  # needs no amendment; AC9's framing assertion moves. New key sequence in the story + runbook.
  # STILL OPEN: AC15 must be RE-RUN (its window moved with the site), AC7's eyeball, AC16's TUI
  # cross-check, and AC17 — Wolf's sign-off, which no agent can close. Gate green, 11 commits,
  # NOTHING PUSHED.
  6-1-the-world-moves: done
  # FIRST ON THE M2 CUT LIST. Cutting it leaves Epic 6's wow intact.
  # STORY CREATED 2026-08-18 (ready-for-dev). THE EPIC'S CENTRAL WIRE CLAIM IS STALE AND WAS
  # FALSIFIED AGAINST SOURCE: protocol::LightKind::Lantern, sim_core::LightKind::Lantern and
  # Entity.light ALL ALREADY EXIST, so AD-16's sanctioned M2 wire diff was spent at 5.1 and this
  # story adds NO protocol change -- AC3 pins that and it materially shrinks the story. The work is
  # switching two hardcoded `light: None` sites (bridge.rs:27 snapshot, :82 delta -- fixing only one
  # lights dwarves on connect then drops them, which looks like a flicker bug) and making the world
  # say every dwarf carries one. gui needs ~zero change: reconcile already spawns a PointLight for
  # any entity with a wire light, 6.1's flicker animates it, and 6.1's blend makes the pool slide
  # with the dwarf for free. TRAP FOUND: do NOT give dwarves the existing Emitter component --
  # emitters() would double-emit them AND drive them into entity_kind()'s unreachable!(), panicking
  # the daemon. TWO EXISTING GUARDS must be re-ruled rather than tripped over: that unreachable!,
  # and load_world_from's lantern rejection whose test name (`..._before_the_wire_bridge`) asserts a
  # world state this story changes. THE REAL LOOK RISK IS THE GROUND-LUMINANCE CEILING, NOT NFR6:
  # 6.1 measured >143 fps (4.8x headroom) but 5.4's frame measured ground-median 123 against a
  # ceiling of 180, and this adds five 11M-lumen lights inside the camp. Verified live off the wire:
  # all 5 dwarves carry light: None today. BLOCKED on Task 0 -- Wolf's artifact approval.
  6-2-lanterns-in-the-dark: done
  # CODE REVIEW 2026-08-19 (4 layers, fresh context, none a coverage hole) -> back to in-progress.
  # THE WIRE HALF IS PROVEN AND THE EVIDENCE APPARATUS WAS NOT. Both bridge arms verified by reading
  # bytes off a live daemon (10 entities, dwarves 0-4 all lantern, 3 consecutive deltas); AC3/AC19's
  # negative claims confirmed by command; both guards still honest; gate green. Two HIGHs, both in the
  # capture instrument: an empty first observation LATCHED an empty region, after which moved() was
  # permanently true and a stationary world passed AC15's central assertion; and accumulate_motion --
  # the production code deriving the lit region -- had ZERO test callers and had never executed once
  # anywhere, so deleting the whole extraction block left the suite green. 12 patches applied, one
  # verification pass: gate GREEN, 52 gui unit + 30 headless tests green.
  # AC14's BAND WAS NOT WIDENED (floors/ceiling byte-identical) -- the one thing review was told to
  # assume had been quietly done, was not done. But it was never re-measured with lanterns lit.
  # THE SABOTAGE TABLE CAUGHT THE REVIEW'S OWN PATCH: first run 10 KILLED / 2 SURVIVED / 1
  # APPLY-FAILED. Both survivors were new tests passing for the wrong reason -- one held its dwarf
  # still so needs_observation's early return swallowed the observations it meant to make. Tests
  # repaired, table re-run. Mutations 9 -> 13.
  # WOLF RULED AT REVIEW: AC1 APPROVED (written-only artifact stands; part (a) before-capture WAIVED,
  # no devpod can render one) -- artifact header corrected, it had contradicted the story for a day.
  # AC13, AC14, AC17 and the rendered halves of AC9/AC15 STAY OPEN pending a gingerspice session.
  # Deferred: 3 LOW AC-text defects (AC11 tautologies, AC4/AC5 unfalsifiable-by-construction) + the
  # per-layer build isolation having no reaper (~92GB/review).
  # Covered by the M2-to-date retrospective filed as Epic 5's: epic-5-retro-2026-08-23.md
  epic-6-retrospective: done
  epic-7: done
  # Resolves the addendum's open control collision BY TESTING, on the record: the
  # mousewheel is already claimed by the zoom continuum and one wheel cannot drive both.
  # ^ STORY CREATED 2026-08-18 (ready-for-dev) AND THAT PREMISE IS STALE. Falsified against source:
  # `gui` binds NO mouse input of any kind -- no MouseWheel, no MouseButton, no CursorMoved -- and
  # camera zoom sits on the Q/E KEYS (camera_controls). The wheel is UNCLAIMED IN CODE; the
  # collision is planned (UX-DR2 intends the wheel) rather than implemented. That does not dissolve
  # the decision, it changes it: the wheel is available today but costs a migration when zoom
  # arrives, while `<`/`>` costs nothing and matches tui --z. AC3 requires the ruling against this
  # reality. CUT-FACE TRAP FOUND: a naive `z <= level` filter over the existing draw set yields a
  # HOLLOW SHELL, because a buried tile is not `is_exposed` -- the `z == level` arm that draws the
  # floor of the cut IS the feature. Also flagged: the pinned 53,365-cube draw-set oracle is a
  # FULL-DEPTH figure that slicing necessarily changes, so the startup line must name its level or
  # every inherited recipe reads as broken; and entity visibility above the slice is a decision to
  # rule and test, not to leave undefined. This is also the story that gives 6.1's one-voxel dig
  # actual DEPTH. BLOCKED on Task 0 -- Wolf's artifact approval.
  7-1-slice-into-the-mountain: done
  # CODE REVIEW 2026-08-19 (4 layers, fresh context, NONE a coverage hole -- a first this epic).
  # THE CUT ITSELF IS PROVEN: Blind Hunter fuzzed the real projection over ~1.1M position checks and
  # 4,000 ECS steps with 0 failures; the Feature Auditor drove a LIVE simd and measured 15,316 of
  # 16,071 cut-face tiles supplied only by the z==level arm. The hollow-shell trap named at creation
  # is genuinely closed and all 12 live-path hops are WIRED -- no dead capability.
  # EVERY DEFECT WAS IN THE OBSERVABILITY LAYER, NOT THE FEATURE. 19 findings: 3 decision (all ruled
  # by Wolf same session), 10 patch, 5 defer, 1 dismissed. Two HIGHs, both the 6.1 untested-drive-line
  # class reproduced: the ENTIRE on-screen readout (AC9's mechanism) and the --z PIN could each be
  # deleted or made inert with a green suite. The capture instrument was LEVEL-BLIND -- `terrain_tiles
  # > 0` cannot see a hollow cut, measured 209 vs 258 with 49 floor tiles missing, so the one story
  # whose headline defect is the hollow shell had an instrument that could not detect it.
  # WOLF'S RULINGS: label keys off terrain above the cut, not position; `--z` no longer requires
  # `--capture` (AC11's own recipe line would have errored as written); lantern assertions now ask the
  # mirror whether a dwarf sits at or below the cut rather than trusting an empty observation.
  # ALL 13 PATCHES APPLIED same session, batched, ONE verification pass. Suite 84 -> 90. Sabotage
  # table 7 -> 13 mutations, 13/13 KILLED. GATE GREEN, run and observed.
  # AC-TEXT-DEFECT CLASS, 10th INSTANCE: AC11/AC17's stated proof `git diff main..HEAD` returns 6
  # files on this stacked branch. The review's FIRST correction was also wrong (6-2..HEAD still
  # catches 6.2's post-7.1 patch commit) -- on a stacked branch only the story's own commit range
  # answers it. Worth generalising: every M2 story is stacked, so every such AC is wrong by default.
  # BACK TO in-progress, NOT done. Unit-green is not feature-proof: AC8 (dug corridors seen), AC9/AC10
  # legibility, AC14 (no fps figure exists at any slice level), AC12's live capture, and BOTH halves of
  # Wolf's gate (AC1 opened on a written-only fallback, AC15 untouched) remain OPEN pending the
  # vehicle. The `<`/`>` ruling is still PROVISIONAL -- Wolf has never chosen it.
  # SCOPE LESSON: the review baseline (the story's own baseline_commit) HID a coupled hunk -- 6.2's
  # 75f04a2 made the lantern assertion read slice.level(). Recovered only because every layer is told
  # the diff is scope, not source. On stacked branches, diff-scoping alone is not enough.
  # Renders designations/zones issued from a TUI client — gui issues nothing here. That
  # placement is deliberate: the rendering survives if Epic 8's input work is cut.
  # STORY CREATED 2026-08-19 (ready-for-dev). THE EPIC'S OWN AC IS UNMEETABLE AND WAS FALSIFIED
  # AGAINST SOURCE: it requires mark entities be "created, updated and despawned by sim `Id`", but
  # designations are `BTreeMap<Pos, DesignationKind>` (sim-core:447) and zones `BTreeSet<Pos>` (:451) --
  # neither is an ECS entity and neither carries an Id at sim, save or wire level, while Entity and Item
  # both do. Meeting it literally means a wire change AD-16 forbids (M2's sanctioned diff was spent at
  # 5.1). AC10 is written KEYED BY POSITION and the AD-14 amendment is recorded as owed to the spine --
  # AD-14 names designations and zones as world-projected AND says reconciliation is keyed by sim Id, and
  # those two halves cannot both hold. 9th instance of the AC-text-defect class, caught at creation.
  # THE STORY IS SMALLER THAN IT LOOKS: every noun is ALREADY on the wire and ALREADY mirrored --
  # Snapshot/Delta carry designations+zones, Mirror exposes them and already does absence-is-deletion,
  # so client-core needs ZERO change and gui has simply never read them. Channel designations are real
  # end to end (distinct sim validity rules, distinct TUI glyph/hue, daemon test sends literal "channel").
  # Items ALREADY RENDER, so they are in the legibility bar but are not new work. gui-only story.
  # RECIPE EXECUTED LIVE AT CREATION and it found the trap: DESIGNATIONS ARE CONSUMED BY THE DWARVES.
  # 6.1's 8-tile site is fully dug in ~52 ticks against a ~110-tick capture window, so the obvious recipe
  # photographs an EMPTY SITE and exits 0 -- the 3.2/3.3 false-failure class again. Measured: an 8x12 rect
  # yields 79 marks (17 of 96 silently dropped by the diggability filter), decaying 68/59/51 at t+40/60/100
  # and PLATEAUING AT 50 from t+120 as the rest become unreachable (FR8 queues, never drops). The recipe
  # uses that rect. Also measured: a TUI-dragged 2x2 stockpile yields TWO zone tiles (the is_standable
  # filter), and a zone tile sits one level ABOVE the rock -- digs at z=9, zones at z=10 -- so a capture
  # pinned to --z 9 hides the zone entirely. Pin --z 10. TUI key sequence lands 8/8, verbatim from 6.1.
  # Baseline 328 workspace tests green. Task 0 (Wolf's artifact) is the blocking gate; Tasks 6/7 are
  # vehicle- and human-bound. BLOCKED on Task 0.
  # HEADLESS HALF DONE 2026-08-21 -> review. Two Codex runs (terra/high). RUN ONE COMMITTED NOTHING --
  # Tasks 1-3 implemented and left entirely STAGED when its window closed, so a dead run had no recovery
  # point at all; orchestrator verified green and committed it. Run two held cadence over 8 commits, ran
  # exactly 3 self-gate passes and fixed all 5 findings (2 P1), the third pass's included -- reported, not
  # answered with a fourth. Orchestrator-verified: gate GREEN cold, 359 tests (baseline 348, NOT the
  # story's stated 328), AC3 empty over all five non-gui crates, File List exact, sabotage 10/10 KILLED.
  # THREE DEFECTS FOUND BY THE ORCHESTRATOR, NONE BY THE AGENT. (1) The two HEADLINE sabotage rows --
  # designation and zone projection deleted -- swapped the mirror source for an empty slice, orphaning the
  # filter/map/collect chain, so NEITHER COMPILED and neither pinned anything. (2) STALE-SABOTAGE-LITERAL,
  # 3rd INSTANCE and the first where the mutation and the code that outdated it were written in the SAME
  # session: the agent's own last refactors moved the lines two rows target, the table was not re-run
  # after them, and the record asserted "all eight KILLED ... did not rerun the already-complete table" --
  # true when it ran, false when written. Re-running measured 2 APPLY-FAILED. A table is evidence only as
  # of its last run: re-run after the last REFACTOR, not the last FEATURE. (3) AC4/AC5 WERE MET ONLY
  # VACUOUSLY -- the colour test asserted mere inequality for an AC demanding VISUALLY distinguishable.
  # Measured, channel sat 16 RGB units from Material::Snow and zone 22 from foliage_snow_color, which the
  # terrain list did not even include; two of three marks were near-neighbours of the surfaces they are
  # drawn on and would have reached the live viewing labelled distinguishable. Now a 40-unit separation
  # floor, proven able to go RED. THE RETUNED COLOURS HAVE NEVER BEEN SEEN RENDERED.
  # WOLF'S TASK 0 RULINGS 2026-08-21: marks are a FLOOR SLAB (channel and zone tiles have no cube, so a
  # tinted replacement could express only dig); AC9's vista capture moves to FULL DEPTH because
  # range_band_applies SKIPS the warm/ground assertions below the world top and BOTH prescribed captures
  # pinned --z 10, so AC9's own recipe proved nothing; mark colours break with the TUI's amber on purpose.
  # AC9's Verification text was left UNEDITED (dev-story may not touch that section) -- the artifact and
  # Dev Agent Record carry the corrected recipe and Task 6's runbook must too.
  # REPORTED NOT FIXED: the slice readout's em-dash has no glyph on the vehicle and draws as a box
  # (slice.rs:59), visible in 7-1-slice.png and every capture since.
  # STILL OPEN and NOT closable by any agent: Tasks 6 (live vehicle) + 7 (Wolf's sign-off), ACs 8 and 17
  # and the rendered halves of AC9. NOTHING IN THIS STORY HAS BEEN OBSERVED ON THE VEHICLE.
  # CODE REVIEW 2026-08-21 (4 layers, fresh context, NONE a coverage hole) -> back to in-progress.
  # 17 findings: 3 decision-needed (all ruled by Wolf, all patched), 11 patch, 4 defer, 0 dismissed.
  # 14 patches applied in ONE verification pass; gate GREEN cold, 364 tests (was 359), sabotage
  # table 16 rows 16/16 KILLED with zero APPLY-FAILED after being re-run post-refactor.
  # THE REVIEW'S BIGGEST FINDING WAS INVISIBLE TO EVERY GREEN TEST: the prescribed working-zoom
  # capture photographed an EMPTY SITE and exited 0. A dig slab sits at z+0.54, but the slice draws
  # every solid tile AT the cut as a full cube spanning [z-0.5, z+0.5] regardless of exposure, so a
  # dig with rock above it was sealed inside opaque geometry -- and the dwarves dig the REACHABLE
  # tiles first, so the marks surviving a capture window are exactly the buried ones. Measured live:
  # 25 of 79 visible at t+2, 9 at t+46, 2 at t+64, 0 of 50 from t+102 on, while the instrument
  # correctly printed designations=50. AC13's counter could not catch it -- all 50 WERE projected.
  # Wolf ruled: promote a buried dig onto the top face of the rock covering it.
  # TWO SEAMS WERE SABOTAGE-PROVED INERT with the whole suite green, both by more than one layer:
  # (1) --distance parsed, validated, then never reached the camera rig, while its only test was
  # NAMED for reaching the camera setup and asserted parse_args_from alone; (2) AC10's restyle was
  # pinned on a bookkeeping component, not the style -- deleting the update-path MeshMaterial3d
  # insert left 106/106 green including the test whose name claims that coverage, and the mutation
  # row sabotaged the same wrong branch. VACUITY RELOCATES: the colour-table vacuity fixed before
  # review reappeared in the restyle assertion, the --distance seam and AC11's rebuild oracle.
  # CROSS-CLIENT COLLISION nobody had a rule for: gui's DIG blue shipped BYTE-IDENTICAL to the TUI's
  # CHANNEL blue, on the two windows Wolf reads side by side. Palette retuned; the colour test now
  # carries a cross-client floor as well as a terrain floor.
  # AC3 verified on the story's OWN range (8d85259..HEAD), empty over all five non-gui crates.
  # STILL OPEN and NOT closable by any agent: Tasks 6 (live vehicle) + 7 (Wolf's sign-off), ACs 8
  # and 17 and the rendered half of AC9. NOTHING IN THIS STORY HAS BEEN OBSERVED ON THE VEHICLE and
  # the retuned colours have never been seen rendered. The sign-off artifact carries a dated
  # amendment naming what changed since approval and exactly what to look at.
  7-2-read-the-working-zoom: done
  # Covered by the M2-to-date retrospective filed as Epic 5's: epic-5-retro-2026-08-23.md
  epic-7-retrospective: done
  epic-8: in-progress
  # M2's hardest input work and the main story-count driver, isolated on purpose.
  # IF PICKING PROVES EASY, 8.1 AND 8.2 COLLAPSE INTO ONE AND M2 LANDS AT 10.
  # HEADLESS HALF DONE 2026-08-25 -> review. Codex dev in one run, six commits, CADENCE FLOOR MET
  # WITHOUT A FOLLOW-UP for the first time -- one commit per task, no squash. Codex left Task 5 and
  # Task 7 unticked and refused to claim a gate its sandbox never showed it: the correct call.
  # ORCHESTRATOR MUTATION ROUND 1 CAUGHT THE STORY'S FINDING -- 5/6 KILLED, one SURVIVED. `--cursor`
  # parsed, validated, then SILENTLY DROPPED by run(): replacing the ScriptedCursor insert with a
  # discard left the WHOLE SUITE GREEN, because the only test wrote the resource by hand and so
  # pinned the resource->pick half and never the flag's own path. This is the 7.2 `--distance` lie
  # recurring verbatim, and M2-11's "named for a seam it does not cross". Fixed by the same remedy:
  # extract run()'s wiring into insert_capture_resources so a test can execute it on a real parsed
  # Args. ROUND 2: 6/6 KILLED, zero APPLY-FAILED. Gate GREEN full tier on a cold rebuild, 382 tests.
  # Scope verified structurally: ZERO files changed outside crates/gui, so AC9's client-local claim
  # is proved rather than asserted. D2/D4/D5/D7/D8 all honoured on reading; D6 raised no finding.
  # TWO ITEMS LEFT FOR REVIEW, both recorded in the Dev Agent Record: (1) the instrument's oracle is
  # a screen-space nearest-within-32px match, not a ray -- depth-blind, so it can disagree with a
  # correct pick at the vista; it fails LOUD, so it cannot manufacture 7.2-style false evidence, but
  # a Task 6 mismatch may be the instrument's defect, not the pick's. (2) AC8's mutual-inverse pin
  # landed in tests/headless.rs, not by extending transform.rs's round-trip pin as the story's
  # structure table specified -- substance met, location not.
  # STILL OPEN and NOT closable by any agent: Task 6 / AC12. NOTHING HAS BEEN OBSERVED ON THE
  # VEHICLE; NFR6-with-picking is unmeasured and no fps figure was fabricated. Rebuild gui.exe and
  # record its build time + source commit first -- the stale-binary trap fired three times in 5.4.
  8-1-point-at-the-world: done
  8-2-designate-with-the-mouse: done
  # DONE 2026-08-28 -- MERGED, PR #35 (a949835). M2 at 10/11. The readout pass ran on the
  # vehicle and AC18 is MET ON ALL FOUR MODES, each read back from the sim by a second client
  # over its own connection: dig 8 designations @ z8, stockpile 12 zones @ z9, channel 16
  # designations SPLIT 9/7 ACROSS z9 AND z10 (channel follows the ground, so ONE drag lands on
  # several levels -- sum them, never read one cut), clear removed every zone and every channel
  # mark across two drags while leaving all 8 digs untouched (the negative control). AC19's
  # takes-effect half MET, AC15/16 MET, NFR6 read at ~140 fps against floors of 60 and 30.
  # CARRIED TO THE GFX PASS, UNCLAIMED, where Wolf ruled them: AC13's rendered half and AC19's
  # reads-clearly half. Neither inferred, neither marked met.
  # THE THING TO CARRY FORWARD: three consecutive vehicle reads showed designations=0 for a
  # channel that WORKED PERFECTLY, and this story's own history -- two genuinely dead modes
  # found in review -- made the wrong reading the plausible one. Settled not by more
  # code-reading (every link was already tested green) but by putting it to the REAL DAEMON:
  # 16 channel designations accepted in full, ALL consumed within ~25 ticks (~2.5 s), 16 blocks
  # below left as Ramp. A channel job is worked from the cell the dwarf already stands on, so
  # it needs no travel; a dig sits at a solid cell a dwarf must path to, which is why dig marks
  # linger and channel marks do not. AN INSTRUMENT THAT READS STATE AFTER THE FACT CANNOT SEE
  # AN OBSERVABLE WHOSE LIFETIME IS SHORTER THAN THE GAP BETWEEN THE ACTION AND THE READ --
  # pause first, and look for the consequence the work leaves behind.
  # TWO RUNBOOK DEFECTS FED IT, both agent-authored, both fixed: the card named a gui.exe ONE
  # COMMIT STALE (the stale-binary trap relocating into the INSTRUCTIONS, its sixth variant),
  # and it ordered the clear drag BEFORE the only read -- clear cancels on both the picked rect
  # and the standable rect, so it erased the evidence it was meant to check.
  # DEFERRED with a reopen trigger: clear cannot reach a mark in a column holding a SECOND
  # standable cell when the two drags are anchored at different heights (measured: near_z=2 ->
  # z2, near_z=5 -> z5). Needs a cave or an overhang to fire.
  # STILL AN ABSENCE, NOT A PASS: `codex review --base main` NEVER RAN on this story (killed
  # twice, quota-blocked once). PRIOR: 8.2 in-progress after code review --
  # CODE REVIEW 2026-08-26 (4 layers, fresh context, NONE a coverage hole) -> back to in-progress.
  # First four-layer full house with per-layer CARGO_TARGET_DIR isolation: nothing starved, no
  # layer killed on silence. 26 findings; 4 decisions ruled by Wolf, 19 patched, 6 deferred, 1
  # dismissed. THE VERDICT THAT MATTERS: the feature is GENUINELY WIRED -- all 15 hops trace to
  # real live callers and the daemon end was proved with the real binaries (a dig rect landed as
  # 16 designations, read back over an independent connection). The D2 inert-seam curse that beat
  # 7.2 and 8.1 is BROKEN here. What replaced it is the same defect displaced ONE LEVEL OUT:
  # (a) INTO THE INSTRUMENTS -- `--at-tick` exhaustion wrote AppExit::error() but `app.run()`'s
  #     return was discarded, so a run that captured NOTHING exited 0 (AppExit is not #[must_use],
  #     so clippy -D warnings stayed green); and a `--drag` that designated nothing evaluated
  #     `0 == 0` and passed, because AC15's non-zero check sat behind `--expect-work`, which the
  #     story's own recipe omits and which a dig-only drag CANNOT pass (it also demands zones > 0).
  # (b) INTO THE TESTS -- the DDA march face, the slab rotation, three of the four modes in the
  #     story title, both Esc paths, the hint bar and the drag preview were each sabotage-provable
  #     GREEN. Only Digit1 was ever pressed by any test.
  # Mutation table 9 rows -> 27. ALL 27 KILLED, NO SURVIVOR (19 new/re-pointed + 8 original
  # re-verified, since the patches touched files the originals anchor into). Tests 149 -> 165,
  # workspace gate GREEN on a full run.
  # TOOLING: scripts/audit-mutations.py was BLIND to 7 rows -- it only inspected literals bound to
  # a NAME, so rows passing text inline to s.replace() were skipped entirely, not even counted as
  # unguarded. One had already rotted (2-1's tui delta arm, dead since that match arm became a
  # block) and the script printed a clean all-clear over it. Now fixed and cumulative (3-1 swaps
  # through a temporary sentinel and must not be called broken). The fix immediately caught 6-1's
  # and 7-2's rows, which THIS session's capture.rs edits had just broken -- they would have
  # shipped rotten. 351 -> 369 rows, all applying.
  # STILL OWED, UNCHANGED BY REVIEW -- none of it closable by an agent:
  # (1) TASK 7 / AC19, AC13's rendered half, AC15/16 end-to-end and AC18 are VEHICLE-BOUND and
  #     remain OPEN AND UNOBSERVED. No fps figure fabricated. The march-face and rotation findings
  #     RAISE the stakes: the live session is the only thing between a wrong face and a shipped
  #     defect, and neither could ever have been caught in a devpod.
  # (2) M2-7's build stamp missing for the FIFTH time -- still no automation in scripts/.
  # (3) `codex review --base main` NEVER RAN. An ABSENCE, not a clean pass; weighed and recorded.
  # ACCEPTED RISK (Wolf): send_commands blocks the Bevy Update schedule on a 30 s write timeout.
  # Daemon is localhost so the stall shape is not real today; REOPEN if simd ever runs off-box.
  # DEV DONE 2026-08-26 except the vehicle half. 14 commits, all Völundr, no squash. Full gate
  # GREEN on a cold rebuild (orchestrator-run). Sabotage table 9 rows, 9 KILLED, NO SURVIVOR --
  # rows 1/2/7 attack the send seam directly, which is the D2 inert-seam shape that beat 7.2 and
  # 8.1 twice. gui's write half is now open: CommandSink + bounded PendingCommands, rects only
  # via client_core::rect_on_level, protocol/simd/client-core never opened.
  # THREE THINGS OWED, none closable by an agent:
  # (1) TASK 7 / AC19 -- NOTHING OBSERVED ON THE VEHICLE. No devpod can open a window; the four
  #     hand-drags, the cliff-face highlight and both fps readings need gingerspice. No fps
  #     figure was fabricated. AC13's rendered half rides on the same session.
  # (2) M2-7's build stamp missing for the FIFTH time -- still no automation in scripts/.
  # (3) `codex review --base main` NEVER RAN: killed twice by the harness, quota-blocked once.
  #     An ABSENCE, not a clean pass -- weigh it in review.
  # DELEGATION WENT BADLY: 4 Codex sessions, 3 ended involuntarily (2 harness kills at ~3 min,
  # cause never identified; 1 usage-limit at 291k tokens). Orchestrator hand-recovered each.
  # QUOTA EXHAUSTED 0%->100% ON THIS ONE STORY. 4 dev rows = 98pp ($6.37 benchmark; dollars
  # cannot see it). CODEX IS UNAVAILABLE TO 8.3 UNTIL THE WEEKLY WINDOW RESETS. 12pp bought
  # NOTHING (the two killed sessions). The self-gate never ran, so the 3-pass cap was never
  # approached -- it all went on dev turns. Pool is account-wide: nidavellir's court brain is
  # starved too. This is 3.2 repeating, to exhaustion rather than to a third.
  # Story created 2026-08-26. FOUR DECISIONS RULED BY WOLF AT CREATION, so the dev pass inherits
  # them rather than re-opening them: press-drag-release (the epic left the pattern to "testing in
  # this story", which no devpod can do); mode keys are DIGITS 1/2/3/4 because the TUI's `d`
  # collides with camera yaw-right; 8.1's buried hover slab is fixed by moving it to the HIT FACE
  # (the DDA already knows the crossed axis); and M2-15's `--capture-at-tick` ships HERE, because
  # the ack AC needs tick-anchored evidence and `--frames` is a render-rate quantity.
  # PREMISE CHECK CHANGED THE SCOPE: the epic's "simd validates the incoming rect and logs-and-drops"
  # is ALREADY BUILT AND TESTED (`rect_is_valid` main.rs:714, applied :677, pinned by serve.rs:1322
  # over both the inverted-corner and two-z cases). The story inherits it and forbids opening
  # crates/simd/ -- only the client half is owed.
  # THE ONE STRUCTURAL CHANGE: `gui` is receive-only today (the TcpStream is eaten by BufReader at
  # ingest.rs:104), so this story opens the write half. That is the seam this project has now got
  # wrong three times running -- 7.2's --distance, 8.1's --cursor, and 8.1's untested call site --
  # which is why AC5 requires a test that starts at a mouse press and ends at BYTES ON A SOCKET.
  # Baseline cca118a, gate green at creation. Nine sabotage rows named. Split line at Task 5 if it
  # overruns; every --capture AC is vehicle-bound, since no devpod can open a window.
  # THE MILESTONE GATE: the walking skeleton runs end to end in the Bevy client, and
  # Wolf signs off BOTH wow beats in one sitting plus the six anti-requirement words.
  8-3-master-of-time-and-the-skeleton-walks-in-3d: backlog
  epic-8-retrospective: optional

  # ---- the gfx pass (epics 9 + 10, added to epics.md 2026-08-28) ----
  # EXECUTION ORDER IS NOT NUMERIC ORDER. RULED 2026-08-28 (Wolf): Epic 9, then Epic 10,
  # THEN 8.3 closes the milestone. AMENDED 2026-09-08 (Wolf): Epic 11 runs after 10.8 and
  # BEFORE 8.3 as well. 8.3 asks Wolf to sign off all six anti-requirement bars,
  # a judgement he has twice refused to make against placeholder material -- so the look work
  # runs first and 8.3 stays `backlog` behind it, not because it is blocked by code.
  # SCOPE, STATED HONESTLY: M2 was planned at 10-14 stories and sits at 11. These nine are
  # added scope ruled by Wolf, not cap slippage -- the cap governed the path to the walking
  # skeleton, which 8.1-8.2 delivered.
  epic-9: in-progress
  # Every story here is gated by a CONCRETE MEASURED DEFECT already on the record (deferred-work.md
  # or named by Wolf at the 8.2 vehicle sessions) -- this is what the standing art rule (2026-08-22)
  # requires, and none of it is taste-driven tuning. Closes the visual halves of FR31/FR34 via
  # UX-DR15-UX-DR20, plus 8.2's deferred AC13-rendered and AC19-reads-clearly halves.
  # UX-DR22 applies to every story, BOTH halves; the four closing halves share ONE vehicle session.
  # 9.1 goes first -- nothing else can be judged with an emitter that bright in frame. 9.2-9.4 are
  # order-free after it.
  # STORY CREATED 2026-08-28 (ready-for-dev). TWO SPEC DEFECTS FOUND AT CREATION, both in the epic's
  # own 9.1 text, both falsified against source.
  # (1) THE PREMISE IS STALE: the epic says the campfire peaks at 44.8M, ~40% over what 5.4 sized
  #     against. It has not since 2026-08-22 -- commit 57e468e applied Wolf's ruling (d) (base
  #     32M -> 25M, amplitude kept) landing the peak at 35.0M, UNDER the APPROVED_PEAK = 35_520_000.0
  #     pin that now guards it. Wolf then re-confirmed the blow-out on 2026-08-27, FIVE DAYS LATER,
  #     on a binary carrying the 25M base. The decision stands and the REASON is wrong -- the cause
  #     is somewhere ruling (d) could not reach.
  # (2) THE EPIC'S AC1 IS VACUOUS, and this was MEASURED rather than inferred: it names the 70-180
  #     valley-floor median band as 9.1's instrument, and 6.2 recorded that band reading 123 -- the
  #     approved artifact's own figure to the digit -- ON THE VERY FRAME Wolf called too blown out,
  #     with the note "no range check can see that". Re-measured at creation: the ground median is
  #     123.4 on 5.4's approved frame, on 6.1's, AND on today's. Constant across every frame anyone
  #     has approved and every frame anyone has rejected. AC1 passes today, unchanged.
  # WOLF RULED THREE THINGS AT CREATION so the dev pass inherits them: (W1) add a local blow-out
  # measure the median cannot see, band demoted to a non-regression guard; (W2) the ONLY lever this
  # story may open is PointLight shadows -- project.rs:416-424 sets colour/intensity/range and
  # nothing else, so shadows_enabled defaults FALSE and the campfire lights straight through solid
  # terrain, the one structural cause the peak ruling never touched (filed at 5.4's review,
  # deferred-work.md:619-622, never acted on). No intensity, amplitude, range or emissive change;
  # (W3) NO Task 0 -- UX-DR22's opening half is met by the approved 5.4 artifact.
  # THE INSTRUMENT WAS CALIBRATED AT CREATION on committed pixels, and the first draft was WRONG and
  # corrected before saving: calibrating against the approved artifact would have measured the
  # RENDERER, since that artifact is a software mock -- whiter than every Bevy frame at threshold 250
  # and darker at 200. The bar is Bevy against Bevy. Measure = largest connected near-white region:
  # 0.6651% on boot7.png (the frame Wolf approved) against 0.9883% today, up 49%, while whole-frame
  # near-white area moved only 18% and the median moved not at all. RED at baseline.
  # STATED TENSION, deliberately not hidden: shadows and the blown-pool ceiling may not be coupled --
  # shadows remove light passing THROUGH geometry, not the unoccluded pool. If the ceiling stays red
  # with shadows correct and NFR6 met, the story STOPS AND REPORTS. Opening another lever silently is
  # forbidden. A story that proves shadows insufficient, with numbers, has done its job.
  # NFR6 IS THE REAL RISK: shadow-casting point lights are the one M2 change with a real chance of
  # spending the ~140 fps headroom. No figure may be fabricated.
  # Baseline 15b3635 after a rebase onto the merged PR #38 (build-cache reaper); created against
  # b1cd5f9 and full gate GREEN on both (run, not claimed). The rebase crossed NO crate source --
  # `git diff b1cd5f9 15b3635 -- crates/` is empty -- so every figure measured at creation stands.
  # Vehicle-bound: ACs 12-15.
  9-1-the-frame-stops-blowing-out: done
  9-2-the-cell-under-the-cursor-is-visible-on-every-face: dropped
  9-3-the-four-modes-read-apart: dropped
  9-4-trees-fewer-and-distinct-from-the-ground: done
  # 9.4 IS THE ONE STORY IN THIS EPIC THAT TOUCHES sim-core (worldgen density): every seeded world
  # changes, terrain-dependent tests and capture recipes need re-checking, and mutation rows anchored
  # near worldgen literals may APPLY-FAIL. It also pushes valley-floor luminance UP while 9.1 pushes
  # it DOWN -- the 70-180 band watches both.
  epic-9-retrospective: optional
  # EPIC 10 OPENS 2026-08-29 with story 10.1. Execution order stands: Epic 9 (closed), then
  # Epic 10, then 8.3 closes the milestone.
  epic-10: in-progress
  # The PRD's asset-pipeline trigger has fired -- it held authored assets until "a concrete case
  # forces the decision -- dwarves are the expected first case", and the PRD predicted right.
  # RULED 2026-08-28 (Wolf): the pipeline is Blender -> glTF via Bevy's NATIVE loader, SUPERSEDING
  # the addendum's MagicaVoxel/`bevy_vox_scene` path. Not a present incompatibility (bevy_vox_scene
  # 0.22 does match Bevy 0.19 today) -- the supersession is about iteration cost and maintenance
  # surface. Dated note lives in the addendum. Game first; the pipeline is an outcome, not the goal.
  # STORY CREATED 2026-08-29 (ready-for-dev). AC1's workload re-measurement was EXECUTED AT
  # CREATION rather than left to the dev pass, because the epic's own AC says the planning probe
  # "proved the venue, not the workload": a throwaway Blender bench was built against a real
  # exported snapshot and run twice. 44,984 exposed cells -- EXACTLY the client's own cube oracle
  # for the shipped seed, reproduced through a wholly independent path (wire JSON -> Python ->
  # Blender), which is the evidence that the bench renders OUR valley. 244,568 verts / 61,142
  # quads; Cycles 2.01 s, whole process 3.67 s; 0 OF 2,073,600 PIXEL VALUES DIFFER across two
  # full-scale runs. The frame was viewed: real terrain, 9.4's green spruce, ramps.
  # THEREFORE THE EPIC'S GINGERSPICE FALLBACK (its AC4) DOES NOT FIRE -- the devpod is the venue.
  # THE EXPORT FILE ALREADY EXISTED and is promoted, not rebuilt: 5-4-signoff/capture_snapshot.py
  # pulls a wire-true snapshot off the real daemon, which is exactly the epic's "explicit export
  # file, no repo internals". Its NEIGHBOUR artifact_render.py is the hand-made mock this story
  # retires -- and the recorded 5.4 failure (it drew spruce sprites nobody was tasked to build).
  # THREE EPIC PREMISES CHECKED, one FALSE: `bevy_gltf` IS enabled (transitively via
  # 3d_bevy_render, Cargo.lock:754) -- only file_watcher is off, which corrects 10.5's AC, not
  # this story. Eevee's blocker is a MISSING libegl1 (apt-installable), not llvmpipe -- decision
  # stands, reason was wrong. And the "1.0 s" probe figure conflates Cycles-internal with
  # whole-process time (0.54 vs 1.39 on the cube; 2.01 vs 3.67 at real scale).
  # STACKED ON 212fbcd, NOT main: 9.1/9.4 are done but UNMERGED and main is 49 commits behind,
  # still carrying the superseded foliage colour (55,73,84) against this branch's (44,100,58).
  # A bench cut from main would bake in a dead palette.
  # HARNESS GAP FOUND AND SCOPED IN: scripts/mutate.sh backs up only `git ls-files 'crates/*'`,
  # so a sabotage patching anything under scripts/ is NEVER RESTORED -- a latent trap regardless
  # of this story. Task 6 closes it and adds a `py` test tier so the bench can be sabotage-proven.
  # Full gate GREEN at creation on 212fbcd, clean tree (run, not claimed).
  # REVISED AFTER AN ADVERSARIAL CHECKLIST REVIEW at creation, which found FIVE criticals in the
  # first draft -- all fixed before saving, none inherited by the dev pass:
  # (1) THE HONESTY CHECK WAS INERT. AC6 named "non-black pixel fraction" as the empty-frame
  #     detector, but the client's sky is a flat (5,12,28) -- an empty frame renders 100%
  #     non-black and PASSES. The metric is now distance from the SKY colour, and the floors are
  #     re-measured on the delivered bench (creation's 79.3% came from a black-background
  #     throwaway and does not transfer).
  # (2) AC8 wanted an exit code while Task 2 required the tests to run without Blender, and no
  #     AC8 test was listed. Now: a Blender-spawning test over a MINIMAL synthetic export, skip-
  #     guarded, kept tiny because the pre-commit hook runs it.
  # (3) THE NEW `py` MUTATION TIER REOPENED THE FALSE-KILL CLASS mutate.sh exists to close: its
  #     KILLED test is "not `could not compile`, then rc != 0" [mutate.sh:82-94] and that guard
  #     is Rust-only, so a Python SyntaxError would print KILLED while proving nothing -- exactly
  #     story 5.3's recorded defect. The tier must call import/syntax errors SURVIVOR.
  # (4) The drift guard's Rust placement was justified circularly ("only cargo can reach it") by
  #     the same story that teaches mutate.sh Python. Re-justified, and two real blockers named:
  #     integration-test cwd is the PACKAGE root, and the boot-camera consts are private.
  # (5) Task 2 ordered "must read 44,984" -- pinning a moving content measurement inside the
  #     epic whose story 10.4 exists to move it. That pin would have turned 10.4's first commit
  #     into a red gate. No test may assert the count; it is a human-read observation now.
  # Also fixed: the exposed predicate's three coin-flips (6 orthogonal neighbours, OOB counts as
  # not-solid) were unstated though AC4 turns on them; the two presentation swaps that dominate
  # the frame (snow caps, spruce crowns) were missing from the palette; Blender's sensor_fit
  # defaults to HORIZONTAL fov against Bevy's vertical; and the promoted exporter carried a
  # hardcoded absolute path that fails on the second devpod mount.
  10-1-the-headless-bench: done
  # STORY CREATED 2026-08-30 (ready-for-dev). A SPIKE: the output is a recorded DECISION (what
  # the MCP-to-script handoff is, what it costs, whether MCP joins the standing workflow), not a
  # pipeline. Premises re-verified at creation: BlenderMCP's live-GUI requirement CONFIRMED
  # against upstream (addon socket server inside a running Blender, localhost:9876; Claude side
  # is `uvx blender-mcp`; upstream's own arbitrary-code caveat and the Windows GUI-PATH trap are
  # on the story's record). Blender-on-gingerspice is UNVERIFIABLE from the devpod -- budgeted
  # as Task 0, Wolf-side, per the epic. The receiving end was proven live by a CONTROL RUN at
  # creation (export -> valley_bench: range-check all non-zero, 44,984 cells, 4.7 s whole
  # process). Handoff candidates named for testing (script re-emission / data-file artifact /
  # hand re-expression), deliberately NOT decided. No gate wiring and no mutation table for the
  # spike script -- a STATED EXCEPTION to the tested-instrument rule: spike output that may be
  # superseded by its own decision; the executed two-run recipe with recorded figures stands in,
  # and if the decision keeps the script, hardening it is the follow-up's first task. Most of
  # this story is Wolf-side by construction; poor Codex-delegation fit, runs orchestrator-side.
  # STACKED on 10.1's tip d02f959 (PR #40 open at creation). An adversarial fresh-context
  # checklist review at creation found 3 criticals, all fixed before save: a FABRICATED
  # explanation for a luma discrepancy that does not exist (the control matches 10.1's recorded
  # range-check line exactly -- any future drift is real change, not sampling noise); an
  # AC2-vs-Task-3 contradiction on which artifacts land in 10-2-signoff/; and a deferral aimed
  # at 10.4 for bench-hardening work 10.4's text never contains.
  # CODE REVIEWED 2026-08-31 (fresh context, four layers, NO coverage holes) -> done. Baseline was
  # the story's OWN commit range 311e169..HEAD; the frontmatter baseline_commit was stale by three
  # merged PRs and would have dragged in ~1,342 lines of other branches' work. Both hunters'
  # chartered crates/ territories were EMPTY (zero Rust files changed) and were reassigned.
  # THE SPIKE'S CENTRAL CLAIM IS CONFIRMED: three layers independently regenerated all four
  # variants to SHA-256 byte-identical matches against the committed exports -- stable on repeat,
  # invariant under PYTHONHASHSEED and under a different cwd, no undocumented argument. Gate run
  # green independently by two layers, and again after patching.
  # ONE REAL CODE DEFECT, the project's signature shape: voxel_pine.py -- the declared artifact of
  # record -- exited 0 on EVERY failure but its own range check. Bad --seed/--voxel values, an
  # unwritable output dir, an output path that is a directory, and the devpod's OTHER Blender
  # (/usr/bin/blender = 4.3.2) all printed a traceback and reported SUCCESS having written nothing.
  # valley_bench.py documents that exact trap and spike_pine_render.py carries the guard; the
  # generator was the one file without it, while its own asset contract clause 6 says "Exit 0 with
  # no output is not a result". Plus --voxel 0 SELF-CERTIFIED: it collapsed the mesh to a point and
  # every closure check passed vacuously because expected_volume scaled by the same zero -- the
  # oracle was not independent of the input. Both fixed; seven failure paths re-verified at exit 1;
  # the four exports re-verified byte-identical afterwards (both-sides closure).
  # BIGGEST RECORD DEFECT: "Fidelity loss: zero, bit-exact" was FALSE AS STATED. The proven
  # bit-exactness is generator->generator across machines (a determinism result), not the
  # session->script fidelity AC5(b) asks about. tree.glb (the 08:59 hand export, 5,130 tris,
  # 7.6 m, centre X -0.100) and export/SM_VoxelPine_Tree02.glb (5,894 tris, 8.0 m, centre X
  # +0.000) carry THE SAME glTF mesh AND node name but are different assets -- 14.3% of pixels
  # differ. NOT handoff loss: three deliberate corrections made THROUGH CLAUDE sit between them
  # (off-centre canopy, double-sRGB dark texture, thick 5x5 trunks). The defect was pointing Wolf
  # at revision N's screenshot beside revision N+1's render and calling the delta zero. There is
  # no committed session-side image of the delivered revision.
  # tree.glb was the one stale artifact actually committed and was marked stale NOWHERE, while
  # ASSET_NOTES' "Repo state" listed three stale .blend files that exist only on the vehicle.
  # Also corrected: a circular "no viewport edits" proof (the GLBs it reproduces were the
  # generator's own output, so it proved determinism, not the absence of mouse edits -- now
  # recorded as Wolf's assertion, which is sufficient); the falsified Blender-4.3.2 receiving-end
  # premise (PR #54 moved the venue to 5.2.1 inside this same range); bit-exactness now named as
  # bound to Blender 5.2.1 / exporter v5.2.40; a "MEASURED on Tree02" bench comment matching NO
  # committed asset; an unreproducible 1.75 s figure; and empty File List / Completion Notes /
  # 15 unchecked task boxes.
  # THREE AC DEVIATIONS, all ruled by Wolf at review and RECORDED AS DEVIATIONS, not filed as met:
  # AC2's transcript clause (the transcript is gone; the screenshots are the record), AC3's
  # scripts/bench/ location (voxel_pine.py STAYS in 10-2-signoff/ -- spike output, deliberate
  # split), and AC4 (CLOSED ON THE DOCUMENTED DIFFERENCE rather than an eye comparison, which AC4
  # explicitly sanctions).
  # THE THREE OWED ITEMS HAD NO DURABLE HOME -- action-item state lives on GitHub issues "and
  # nowhere else" and none of the 10 existing issues is 10.2's. Now recorded in deferred-work.md.
  # Wolf's ruling: the handover process/mechanism is real work but NOT this story's scope, so it is
  # recorded and NOT built, and NO issues were opened -- that call is his. Six further findings
  # deferred (the spike bench's floors, the AC2-conditional binaries, 3.7 MB of next-story dwarf
  # assets, two pre-existing tracked Zone.Identifier files).
  10-2-the-live-seat-blendermcp-on-gingerspice-spike: done
  # ---- EXECUTION ORDER IN EPIC 10 IS NOT NUMERIC ORDER (ruled 2026-08-31, Wolf) ----
  # 10.6 -> 10.3 -> 10.4 -> 10.5. The resolution bench measures what we can serve, THEN the
  # contract writes it down, THEN assets are authored against it. 10-6 is placed HERE, ahead of
  # 10-3, deliberately: this board's next-story rule reads top to bottom, and leaving 10-6 in
  # numeric position would reproduce exactly the ordering bug that put 8.3 up first today.
  #
  # STORY CREATED 2026-08-31 (ready-for-dev). Baseline 0b8b673, gate GREEN at creation.
  # WHY IT EXISTS: 10.3 was authored with its grid-scale clause picked from three UNMEASURED
  # options, and 10.4/10.5 author assets against whatever it picks. Wolf caught it. Measured at
  # creation on the real world: 44,984 exposed cubes but only 61,142 faces are actually on the
  # surface against 269,904 submitted as whole cubes -- 77% of the geometry we draw is interior
  # and invisible, and greedy meshing takes 539,808 triangles to 38,528. There is a 14x triangle
  # headroom BEFORE any resolution increase, which is why "can we go finer" was never obviously
  # a no. The suspected real bottleneck is the ~63k per-cell ECS entities, not the triangles.
  # The sweep must run to the WALL, not to a comfortable number, and report per class -- terrain
  # (44,984), trees (~265) and dwarves (5) do not share one budget. fps is Wolf-side: lavapipe
  # renders here but does not clock.
  # DEV DONE 2026-08-31, delegated to Codex across TWO runs (the first was killed by the harness
  # mid-verification; Task 3 was resumed separately on the same branch, 18 commits total).
  # Gate GREEN and all four mutation rows KILLED on the ORCHESTRATOR's own re-runs -- the dev run
  # never achieved a green gate itself and never completed a self-gate pass. Open for review:
  # AC7 (fps) is Wolf-side on gingerspice by construction; the sweep's k=8 wall is a DEFERRED
  # venue constraint, not a measured limit (Wolf, shared WSL host); and the bench and the gui
  # disagree on k=2 triangles (155,080 vs 218,832), unexplained.
  #   -> ALL THREE ANSWERED BY THE 2026-09-01 RE-MEASUREMENT. The unexplained k=2 gap was three
  #   compounding defects (44% of live faces buried in rock, an analytic baseline no pit obeys,
  #   and a RED control that could not fail) plus two tie-break/foliage divergences found while
  #   fixing them; bench and gui now agree to the unit on cells and chunks, and the residual
  #   +3.9% at k=2 is chunk/rim partitioning, which is bounded and explained. The k=8 wall was a
  #   mis-sized guard, not a venue limit: k=16 completes in 30.9s and the wall is k=32.
  #   AC7 remains genuinely Wolf-side and is the only thing still owed.
  10-6-how-fine-can-we-go: done
                          # 10.6 DONE 2026-09-01. AC7 SETTLED ON THE VEHICLE, clean build 23172f4:
                          # k=4 >130 fps, k=8 100-140, both clearing NFR6's 60 and 30 bars. k=16 EXCLUDED
                          # -- not on frame rate but on INTERACTION COST: Wolf sees halts when digging,
                          # which are the measured 67-187 ms per-chunk rebuild. THE DECIDING AXIS TURNED
                          # OUT TO BE THE DIG, not fps: ~5 ms at k=4, ~40 at k=8, ~70-110 at k=16 per
                          # rebuilt chunk; boot terrain build 328 / 1,269 / 5,266 ms. Adopted k=4 (1.6 m
                          # per cell, 0.4 m per voxel) now rests on that rather than on triangle counts.
                          # CAVEAT RECORDED, NOT HIDDEN: every figure came from a nearly-idle world (tick
                          # 21, ten entities), so fps is an UPPER BOUND and dig frequency an under-count;
                          # re-benchmark under load is deferred. Opened M2-19: a frame-performance overlay
                          # on industry-standard percentile FRAME TIMES -- the current smoothed-fps overlay
                          # structurally cannot show a hitch, which is why Wolf found k=16's halts by eye.
                          # 10.6 CODE-REVIEWED ROUND 2 (2026-09-01) -> in-progress. FOUR LAYERS, ALL LIVE,
                          # NO COVERAGE HOLES; full gate GREEN on a review layer. 3 decisions / 14 patch /
                          # 4 defer / 1 dismissed, plus 2 patch items generated by Wolf's rulings = 16 patched.
                          # BOTH-SIDES CLOSURE ON ROUND 1: 18 of 23 CLOSED, 5 HALF-CLOSED -- every one the
                          # same shape, fixed for the direction it was written for and relocated into the
                          # other. Convergence 5-in-20 against round 1's 1-in-8; both 3-way convergences came
                          # from the two UNTERRITORIALISED Opus auditors, which is the R1 territory evidence.
                          # WOLF'S RULINGS: adopted k stays 4 (the k=16 reopening is withdrawn); the
                          # MAX_FINE_FACES raise 4M->48M is confirmed retroactively; the 60-90 fps k=16
                          # reading PREDATES bace455/c8675fc and is VOID. That makes THREE void fps readings
                          # in this story, so the vehicle card now carries a commit-SHA column. AC7 IS OPEN
                          # AT EVERY BAR.
                          # THE PATCH PASS FOUND A REAL DEFECT NO LAYER DID: dirty_chunks reached one cell
                          # where a dig reaches two, so a dug tile near a chunk seam left STALE geometry --
                          # invisible because the fixture was 40x4x4 and could not cross a y or z boundary.
                          # Widening it to seams on all three axes failed immediately. Mutation table 21->23
                          # rows; the first run had 2 SURVIVORS, both defects in the patch work itself
                          # (an inequality oracle, and a census fixture with no buried cell). After fixing:
                          # 23/23 KILLED, full gate GREEN twice.
  #
  # GATED ON 10.6 -- do not start it first even though it is numerically earlier.
  # STORY CREATED 2026-08-31 (ready-for-dev). Baseline 0b8b673, gate GREEN at creation.
  # THE BOARD'S NUMERIC ORDER WOULD HAVE PICKED 8.3 HERE and Wolf caught it: the 2026-08-28
  # ruling twelve lines above puts the gfx pass FIRST, so 10.3 is next and 8.3 stays parked.
  # Two of the epic's premises were WRONG at re-verification and are corrected in the story.
  # GRID SCALE, corrected twice in one session: the first draft called it a three-way divergence
  # (1.600 / 1.767 / 1.846 m per cell). Wolf pointed at the reference sheet and it collapses --
  # Section A labels the dwarf "12 Voxels", Section B labels trees in cells, and at a 1.20 m
  # dwarf that is 0.1 m/voxel, 1.6 m/cell, 16 voxels/cell, consistently. It was never three
  # numbers fighting; it is ONE stale constant, `scale: 0.65` -> 0.75, owed to 10.5. What the
  # sheet cannot say is what we can RENDER -- that is 10.6's job, hence the gate.
  # Second premise: the 1.20 m dwarf anchor every 10.2 asset was built against is ratified
  # NOWHERE outside that story's ASSET_NOTES.
  # Vehicle for issue #57 (M2-20) -- `Closes #57` goes in the PR body. Explicitly NOT taking
  # #58 (handover runbook) or #59 (spike-bench hardening); review already found 10.3 does not
  # pick those up on its own. Instrument is a NEW `scripts/bench/check_asset.py`; its RED
  # specimen is the already-committed stale `tree.glb`, measured at creation as genuinely red
  # (centre X -0.100000 against the four deliverables' 0.000000) -- and note that EVERY OTHER
  # clause passes on it, so origin centring is the only discriminating one.
  10-3-the-rules-of-the-look: done
  10-4-the-trees-look-right-the-pilot: done
                          # 2026-09-03 MERGED as PR #63 (merge commit 276dbd8) -> done. Verified
                          # on the remote, not the log: state=MERGED, every commit an ancestor of
                          # origin/main, remote branch deleted, no stacked PR left pointing at it.
                          # POST-MERGE BRANCH TRAP FIRED AGAIN (6th): the merge put the working
                          # copy on main. Re-check branch before any commit.
                          # PRIOR: AC12 CLOSED ON THE VEHICLE -> review. ALL ACs met. Wolf ran
                          # build 3b0c43f on the RTX 4080. NFR6 PASSES: >100 fps typical, brief ~60
                          # against 60/30 floors -- FIRST fps ever taken against the mesh trees, at
                          # 67% of the scene's triangles. Lone gui.exe, no assets/, 265 pines from
                          # embedded://; windowed --capture wrote its PNG where it used to panic.
                          # NEW FINDING, CAUSE FOUND: THE SUN IS UNDER THE MAP. The only
                          # DirectionalLight sits at the aurora curtain's MIDPOINT, Y=-58.5, while
                          # the surface is Y=9 -- it shines upward from below and lights nothing.
                          # Deleting it entirely moves the frame LESS than run-to-run noise;
                          # lifting it moves mean luminance 13.3 against a 0.08 noise floor.
                          # EVERY look decision so far (9.1 blowout, 9.4 colours, 10.3 rules, 10.4
                          # trees, the near-white ceiling) was tuned with the sun off. Not 10.4's,
                          # needs its own story + bench + Wolf's eye. deferred-work.md.
                          # ALSO DEFERRED: fps varies while dwarves move (Wolf reads lighting; two
                          # modes fit, pausing the daemon separates them), and near-white reads
                          # 2.2071% on the GPU vs 9.1's 1.5630% ceiling -- no baseline GPU capture
                          # exists, so venue and content stay confounded. Do NOT raise the ceiling.
                          # PRIOR: THE VEHICLE CARD EXISTS: 10-4-signoff/task-6-vehicle-runbook.md.
                          # AC12's closing half is ALL that is left in this story. Every command in
                          # the card was run here on 12da79d first. The build.rs stamp defect
                          # reproduced, so `touch crates/gui/build.rs` is a mandatory build step.
                          # The fps question restated on measurement: the pines are 67% of the
                          # scene (576,972 terrain triangles at the shipped subdiv 1, ~1.19M for
                          # 265 pines). The story's own "~479k terrain" was NEVER MEASURED --
                          # it matches no reading and is nowhere in the repo.
                          # PRIOR: CODE-REVIEWED 2026-09-02 -> in-progress. FOUR LAYERS, ALL LIVE, NO
                          # COVERAGE HOLES; full gate GREEN on a review layer and again after the
                          # patch pass. 3 decisions / 14 patches / 5 deferrals / 1 dismissed.
                          # THE HEADLINE: the story's central claim was TRUE and its evidence for it
                          # was FALSE. The embedding fix works -- two layers ran a lone binary from
                          # an empty dir and got 265 of 265 meshes from embedded:// -- but BOTH
                          # committed AC5 captures were renders of the PRE-MESH build. One treatment
                          # photographed twice, which is the failure AC3 was written to name,
                          # landing on AC5's own artifacts. Confirmed 3 ways: by eye, by rebuilding
                          # 9eba31f (exit 101, no PNG at all), and by measurement -- a real HEAD
                          # capture differs from the committed one by 257,952 px at d>=4 against a
                          # 6,620 noise floor. Regenerated from real builds: 261,952 at d>=4 vs a
                          # WORST-CASE same-code noise of 46,050 (5.7x; 22.6x at d>=16).
                          # THREE DECISIONS WERE ONE DEFECT. tree_meshes REJECTS a column it cannot
                          # represent while both spawn paths filter every tree cell out of the
                          # terrain -- so a rejected column was drawn by NEITHER path and vanished
                          # silently, with the cut-face oracle rejecting it on both sides at once
                          # and reporting a match. Wolf ruled: revive the cube path as the FALLBACK
                          # (AC6 stops being vestigial), add the contiguity check TreeMesh's doc
                          # comment always promised (a dug-through trunk now shows its hole instead
                          # of redrawing an unbroken pine), and promote authored_bench.py onto the
                          # gate reading the SAME GLBs the client embeds.
                          # THE SUBDIV-2 BREAK WAS OURS, not pre-existing: dc79164 removed the only
                          # TerrainTile entities at subdiv>1 and the lantern sweep queried that
                          # component alone -- lit=0, panic BEFORE the PNG. Now lit=1870, PNG
                          # written. It was filed as pre-existing because a genuinely pre-existing
                          # panic (the near-white ceiling) sat next to it; filing them together is
                          # what let it hide. A WINDOWED --capture also always panicked (0 == 265),
                          # which is the exact command epic-9-shared-sitting-card.md tells Wolf to
                          # run. 10 mutation rows, ALL KILLED, anchors re-audited after cargo fmt.
                          # METRICS TRAP 4 AGAIN: the dev transcript's cursor sat at 12:09 while the
                          # embedding work ran to 12:51 -- 86 turns / $20.80 belonged to no row.
                          # OPEN: AC12's closing half (real window, real fps) still needs the
                          # vehicle; ~1.2M triangles vs ~479k terrain is an unanswered fps question.
                          # --at-tick is unusable on this venue (its floor wants as many OBSERVED
                          # ticks as requested; software rendering observes ~a third).
                          # STORY CREATED 2026-09-02 on branch 10-4-..., baseline 2ef194d, full gate
                          # GREEN at creation. THREE OF SIX EPIC PREMISES WERE WRONG: "Blender 4.3.2"
                          # is stale (bare `blender` is 5.2.1 and figures are NOT comparable across
                          # the two, which is why the bench stamps blender= into every line); "no
                          # devpod can open a window" is HALF false (no window, but lavapipe renders
                          # and `gui --headless --capture` produces real client pixels -- only fps is
                          # unmeasurable); and Eevee's exclusion is right for the WRONG reason
                          # (missing libegl1, not llvmpipe). The epic's AC also names `SPRUCE_SNOW`,
                          # a value superseded at round 7.
                          # A SEVENTH DEFECT WAS FOUND IN THE JUST-MERGED GUIDELINES: the foliage
                          # taper reads 0.72/0.86/full where the client ships 0.62/0.78/0.95. Same
                          # round-7 commit 10c06e1 moved the taper, the snow cap AND the crown, and
                          # a later commit documented only the crown -- the third value the partial
                          # update hid. Task 0 corrects it. Evidence for issue #62.
                          # VERIFICATION RECIPE EXECUTED AT CREATION, RED FIRST: stripping all 5,582
                          # tree cells moved every bench figure (exposed_cells 44,984->40,148,
                          # distinct_colors 59,191->27,999, terrain_luma 105.853->125.883) -- AND THE
                          # RED STILL EXITED 0 AND PASSED ITS RANGE CHECK, because the floors are far
                          # too loose to fail a treeless world. Judgement rests on the printed
                          # figures, never the exit code. Control + RED artifacts committed to
                          # 10-4-signoff/.
  10-7-the-sun-lights-the-valley: done
                          # 2026-09-04 MERGED as PR #66 (4256b15 -> merge 87f3bdc), 43 commits,
                          # 56 files, +3,757/-202. Full gate GREEN at the pushed commit and green
                          # again on the pre-push hook. All twelve ACs met, UX-DR22 signed both
                          # halves, mutations 14/14 killed.
                          # MERGED WITH A STATED GAP, not a resolved one: the draw-set fix, the
                          # guard's oracle replacement and two mutation rows landed AFTER the
                          # four-layer review, so that code has had no fresh-context review. It is
                          # in the PR body. Worth remembering that the review which DID run passed,
                          # and Wolf's sitting falsified AC12 anyway.
                          # 2026-09-04 ALL TWELVE ACs MET -> review. UX-DR22's closing half is
                          # SIGNED for ACs 10-12: Wolf at the vehicle on subdiv 1, 2 and 4 ("holes
                          # are fixed now even on terrain", "16 is ok"), and on the toggles ("light
                          # toggles are working properly") -- his first look at F5-F9, the readout
                          # and the all-off state since the torch and emissive fixes landed.
                          # Full gate GREEN 10/10 with both pixel guards; mutations 14/14 killed.
                          # OPEN QUESTION BEFORE MERGE: the four-layer review of 2026-09-03
                          # predates the draw-set fix and the guard oracle replacement, so roughly
                          # 30 lines of production code and a rewritten guard have had NO
                          # fresh-context review. Wolf's call whether that is worth a pass.
                          # Nothing pushed; no PR.
                          # 2026-09-04 AC12 TRUNK BASES CLOSED, and the cause was the DRAW SET,
                          # not face emission: is_visible_at_slice reads a Solid(TreeTrunk) cell as
                          # ordinary cover, so the ground under a trunk never reached the mesher and
                          # emitted nothing whatever the emission decisions said. occludes ->
                          # occludes_terrain, one level up. Found by running the REAL world through
                          # the mesher (232 owed faces missing, EVERY one on a cell absent from the
                          # draw set -> 0) after TWO hand-built fixtures failed to reproduce it,
                          # because both made the cell exposed for their own reasons.
                          # subdiv 2: 2,177 px / 54 blobs -> 2,042 / 16. subdiv 4: 1,959 / 67 ->
                          # 1,825 / 19. subdiv 1 EXACTLY unchanged at 1,650 / 15, its whole-frame
                          # diff inside its own noise band despite 212 newly drawn cells.
                          # WOLF SAT AT subdiv 1, 2 AND 4: "holes are fixed now even on terrain",
                          # "16 is ok". THE RESIDUAL IS NOT HOLES -- the face oracle reports zero
                          # missing faces, so it cannot be a missing face; it is sky past the
                          # world's outer edge, framed by pines standing beyond the last terrain
                          # cell. Issue #65 CLOSED as not-a-defect and retitled. The guard's
                          # ceilings are a property of the framing, not a debt; comment corrected.
                          # Guard oracle REPLACED not re-calibrated: interior_sky counted OPEN sky
                          # (its per-column silhouette never engages -- the sky is a gradient) ->
                          # enclosed_sky, a border flood fill. Blob count is the primary bar: the
                          # 38 trunk holes were 135 px between them.
                          # Mutation table 12 -> 14 rows, 14/14 KILLED, zero survivors.
                          # TRAP RECORDED: the first subdiv-4 capture measured a MUTANT binary --
                          # mutate.sh restores the SOURCE and leaves the last mutant BUILD on disk,
                          # and that row removes this very fix. Caught only by M2-7's compiled-in
                          # stamp reading -dirty. Rebuild before any capture after a mutation run.
                          # 2026-09-04 AC12 FALSIFIED AT THE PRE-MERGE SITTING, full gate green.
                          # Wolf: "tree trunk bases has still black quads and there is 2-3 black
                          # holes on top of terrain cubes". BOTH reproduce headless at 6cd6f8d.
                          # TWO FAMILIES: (a) trunk bases, this story's, PARTIALLY fixed -- 82 blobs
                          # -> 54, ~200 px left, --subdiv 2 only; (b) four ridge holes ~1,650 px at
                          # BOTH subdivs, byte-identical in control-shipped-a-e930d07.png from
                          # before the story, so NOT 10.7's -- filed in deferred-work.md.
                          # WHY THE GUARD WAS GREEN: holes.py and pixel_guard.rs resolve the
                          # silhouette as a column's topmost non-sky pixel, and the sky is a
                          # GRADIENT, so it lands at y<=19 in all 1,280 columns and they count OPEN
                          # SKY (11,174 of 18,889). Only their DELTA tracked holes; the story read a
                          # delta as a level, and AC12 asks for gone. New oracle
                          # 10-7-signoff/enclosed.py: border flood fill, RED-first, 0 px noise floor.
                          # NEXT: Wolf's call on scope -- the trunk-base remainder is 10.7's, the
                          # ridge family is not. Nothing pushed; no PR.
                          # 2026-09-03 CODE REVIEW -> in-progress (NOT done). Four layers, no
                          # coverage holes, every layer ran cargo. THE CODE IS SOUND on every
                          # count that was executed: sun +13.2 mean luminance reproduced on two
                          # fresh builds, five toggles wired on the live path, no third residual
                          # emitter (all-off frame measures warm-lit=0), subdiv-2 holes closed
                          # (12,285/12,313 vs 12,722 before). The one HIGH was EVIDENCE, not code:
                          # the committed `probe-subdiv2-after-occluder-fix*.png` were the REJECTED
                          # first fix -- 13,606 px measured against 12,314 published, and
                          # 12,722 + 884 = 13,606 is exactly the regression the story's own change
                          # log attributes to the wrong turn it abandoned. AC12's proof contradicted
                          # the fix while the fix was fine. Renamed to say what they are; the shipped
                          # fix re-captured under its own name. Same shape as M2-7's stale binary
                          # and 10.2's duplicate mesh: the NAME authenticated the artifact.
                          # 7 patches applied, 7 LOW deferred, 1 dismissed. Mutation table 7 -> 12
                          # rows, 12/12 KILLED. Full gate GREEN.
                          # SCOPE CHANGE #5, Wolf's ruling at triage: the no-CLI-flag decision is
                          # LIFTED. `--lights-off` lands, which gives LightSource::from_name the
                          # production caller it never had, and with it crates/gui/tests/
                          # pixel_guard.rs -- the FIRST tests in this workspace that assert pixels.
                          # AC11's is Wolf's own complaint encoded: all five sources off must leave
                          # warm_lit_pixels at exactly 0. They cost ~2 min and run in the FULL tier
                          # only; the fast tier names them skipped beside serve.rs.
                          # WHY NOT `done`: UX-DR22's closing half covers the SUN only. Wolf's
                          # recorded sittings predate the torch fix, the emissive fix and the
                          # subdiv-2 fix. An observable-outcome AC stays open until observed, so
                          # AC9's closing half for ACs 10-12 is OPEN and the card is written:
                          # 10-7-signoff/review-vehicle-runbook.md. Nothing pushed; no PR.
                          # 2026-09-03 ALL 12 ACs MET -> review (2nd time). Light toggles on F5-F8
                          # + readout; AC11's guard is the renderer-read component test (Wolf's
                          # ruling) with frame measurements as evidence. AC12: the subdiv>1 black
                          # quads were HOLES, not shadows -- rgb(5,12,28) is SKY_RGB -- caused by the
                          # mesher letting a mesh-drawn tree cell suppress a face nothing then drew.
                          # interior-sky 12,722 -> 12,314 on a 12 px floor; subdiv 1 UNCHANGED at
                          # 11,174 exactly. Mutation table 7/7 KILLED, full gate GREEN.
                          # TWO WRONG TURNS RECORDED: the first hole metric used a y-threshold and
                          # reported the regression as an IMPROVEMENT (published before its noise
                          # floor -- the 10.4 AC5 mistake again); and the first fix also routed the
                          # CARVING decision through the tree-aware occluder, closing 486 px at the
                          # trunks and opening 1,370 at the world edges, invisible to a unit oracle
                          # that forced carving parity. The narrow face oracle + a stone control is
                          # what steered it.
                          # PRIOR: REOPENED from `review` on Wolf's instruction after the
                          # vehicle sitting. ACs 10-12 added: per-source light toggles as a KEY
                          # COMMAND in gui (NOT a cli flag -- Wolf), an instrument test asserting
                          # the FRAME moves rather than the flag, and removal of the subdiv>1 black
                          # quads with a PIXEL-level guard. Toggle lands FIRST because it is the
                          # instrument the defect is diagnosed with.
                          # 2026-09-03 TASKS 1-7 DONE -> review. WOLF RULED +17.66 deg against the
                          # shipped -6.42 control and +8.62/+25.87 alternatives, on a side-by-side
                          # of four bench frames; UX-DR22 OPENING half signed, CLOSING half still
                          # open (card: 10-7-signoff/task-7-vehicle-runbook.md). AC4 measured
                          # 131.8x its own noise floor (mean 87.87 -> 101.19, worst same-build
                          # spread 0.101). Mutation table 3/3 KILLED, re-run independently; no row
                          # anchors a tuned literal. Full gate GREEN 9/9 on the orchestrator's own
                          # run. AC1 clean on 47139fa..HEAD: five source files, which is the
                          # story's table PLUS scripts/tests/test_valley_bench.py -- a GATE-BLOCKING
                          # file the table omitted that PINNED THE DEFECT (it asserted the key light
                          # travels UPWARD).
                          # BOTH DELEGATED RUNS WERE HARNESS-KILLED, Run A mid-self-gate and Run B
                          # mid-verification-recipe. The commit-cadence floor cost nothing both
                          # times; the orchestrator finished Task 7 directly.
                          # INSTRUMENT DEFECT FOUND AND CLOSED: lumstats.py and pixel_diff.py
                          # hardcoded bpp=3 and never read the PNG colour type, so on the bench's
                          # RGBA frames they DID NOT DIE, THEY LIED -- returning a plausible
                          # NON-MONOTONIC luminance ladder for frames whose true ladder is clean.
                          # It misled the orchestrator first. They now raise; a third RED is on file.
                          # AC7 HELD: near-white 1.82% -> 2.25% against a 1.5630% ceiling, recorded
                          # and NOT moved. Breach PREDATES the story on both venues. Filed in
                          # deferred-work.md with the real finding: the ceiling was calibrated on
                          # boot7.png, a frame rendered WITH THE SUN BELOW THE HORIZON, so it needs
                          # a new reference frame rather than a bigger number.
                          # NOT THIS STORY'S, flagged: session_tokens.py reads rate_limits.primary
                          # and calls it the weekly window. On this account primary is the 5-HOUR
                          # window and secondary is the weekly one, so every quota_pp in the ledger
                          # is the 5-hour figure. Run A really moved weekly 67% -> 70%.
                          # CREATED 2026-09-03 out of 10.4's vehicle sitting, on Wolf's
                          # instruction. THE SUN IS UNDER THE MAP: the only DirectionalLight sits
                          # at the aurora curtain's MIDPOINT, Y=-58.5, surface is Y=9 -- it shines
                          # upward from below and lights nothing. Deleting it moves the frame LESS
                          # than run-to-run noise; lifting it moves mean luminance 13.3 vs a 0.08
                          # noise floor (~170x). PLACED ABOVE 10.5 ON PURPOSE -- it must run first.
                          # Judging authored dwarves under an ambient-only scene repeats 10.4's
                          # own failure (approving a frame the client does not draw), one level up.
                          # CONTEXT-FILLED 2026-09-03; ACs firmed 6 -> 9; added to epics.md the
                          # same day (execution order there now reads 10.6->10.3->10.4->10.7->10.5).
                          # ALL THREE RULINGS ANSWERED (Wolf): elevation is BENCHED not pre-picked;
                          # the sun is DECOUPLED from the aurora; NOTHING ELSE is re-tuned.
                          # baseline_commit corrected e930d07 -> 47139fa (5 commits stale, and AC1
                          # grades the diff from it).
                          # TWO TRAPS MEASURED AT CREATION, both new. (1) It is a DIRECTION defect,
                          # not a position one: Bevy reads a directional light's forward vector and
                          # IGNORES its translation (bevy_light-0.19.0/src/directional_light.rs:25),
                          # so the sun is at -6.42 deg elevation and the draft's own suggested
                          # height guard WOULD CERTIFY A BROKEN BUILD. (2) `gui --headless
                          # --capture` ALREADY EXITS 101 on main -- "near-white area is 1.8757%,
                          # above the 1.5630% ceiling" -- so the ceiling is breached on BOTH venues
                          # before the story starts and a red capture check is NOT evidence about
                          # the sun. The PNG is still written (save-then-validate). AC7 forbids
                          # raising the constant; the breach is filed as its own defect.
                          # Also found: the existing "aurora light must arrive from the curtain
                          # side" test is SELF-REFERENTIAL and passes with the sun underground.
                          # VERIFICATION RECIPE EXECUTED AT CREATION, RED FIRST: lumstats.py dies
                          # loudly on a truncated frame and reports an all-black frame as
                          # mean=0.000/dark=100%, so a blank capture cannot pass as a lit valley.
                          # Same-build noise floor re-measured on 47139fa: 0.107 mean (the Y=200
                          # probe's 13.3 is 124x it). Full gate GREEN, 9/9, no skips.
  10-5-dwarves-worth-looking-at: done
                          # 2026-09-07 JOINT REVIEW WITH PART B, four layers, full house, no
                          # timeouts and no coverage holes. Four of the review's findings were
                          # Part A's: --static-world was ignored under --at-tick with zero test
                          # coverage anywhere; the below-the-cut capture in its AC10 test has
                          # PANICKED on every gate run since Part A (issue #77, hidden because the
                          # helper discarded the exit status); the story still specified a pine
                          # stand-in and forbade committing a dwarf model with Task 2 ticked
                          # against it; and its Verification named `gui build 4b01a58` for control
                          # frames replaced under the same filenames at cc66a49. All patched.
                          # Full gate re-run by the reviewer, GREEN 597s -- not inherited.
                          # 2026-09-06 PART A IMPLEMENTED, 14 commits, pushed as
                          # `10-5-dwarves-worth-looking-at` at cb45817. NOT merged, NO PR, and
                          # NOT yet reviewed -- status moved backlog->review 2026-09-07 because
                          # `ready-for-dev` on an implemented story reads as an invitation to
                          # re-dev it. Full gate GREEN (432s, re-run 2026-09-07, not inherited);
                          # mutation table 7 rows all KILLED. AC7 AMENDED on Wolf's ruling: the
                          # partition hole predates the story by an epic (4 unmarked scene
                          # children per tree, 3 per dwarf; 3,240 at the adopted --subdiv 4) and
                          # is issue #73. Part A OVER-DELIVERED: it shipped the REAL authored
                          # dwarf, not the pine stand-in its own Task 2 specified.
                          # WOLF'S RULING 2026-09-07: Part B runs FIRST, then ONE review covers
                          # Part A and Part B together. Do not open a PR for A on its own.
                          # 2026-09-04 STORY CREATED (ready-for-dev). Baseline 834f105.
                          # THE EPIC'S OWN SPLIT LINE IS TAKEN (epics.md:1591-1593): this story is
                          # PART A, the seam -- a stand-in glTF rendering on the entity path. PART B,
                          # the authored dwarf plus generalising check_asset.py plus UX-DR22's two
                          # halves, is a separate story and CANNOT START until Wolf has a model,
                          # because UX-DR22's opening half needs an approved artifact of the real
                          # asset before implementation. Part B's first input is Wolf's .blend.
                          # NINE PREMISES RE-VERIFIED, SIX WRONG OR ABSENT FROM THE EPIC:
                          #  1. bevy_gltf is ALREADY enabled via 3d_bevy_render -- half of the
                          #     epic's AC1 is a no-op. Only file_watcher is off.
                          #  2. The lantern is NOT client-table-driven: entity_light_kind(Dwarf) is
                          #     None and the client refuses to light a dwarf by kind; it comes over
                          #     the WIRE from sim-core DWARF_LIGHT.
                          #  3. HOT-RELOAD HAS NO VENUE. embedded_watcher resolves the source by
                          #     COMPILE-TIME path, so the Windows vehicle (which can show a dwarf)
                          #     has no source tree and the devpod (which has one) cannot open a
                          #     window. This is the exact failure GUI_WORKSPACE_ROOT was deleted
                          #     for. WOLF'S RULING REQUIRED -- three costed options in the story.
                          #  4. An asset-contract dwarf (min Y = 0) FLOATS half a cell: entities are
                          #     placed at cell CENTRE, trees at cell FLOOR.
                          #  5. scale 0.65 is pinned TWICE, one a source-text grep that also
                          #     requires valley_bench.py edited in the same commit.
                          #  6. WorldAssetRoot spawns CHILDREN, which classify_client_local
                          #     (PostStartup-only) never reaches -- two partition tests are a live
                          #     trap for a scene-drawn dwarf.
                          # check_asset.py is deliberately NOT touched here: it rejects an authored
                          # dwarf on its first clause BY DESIGN (V1 voxel assets only), and
                          # generalising it without the real asset means guessing.
  10-5b-the-art-iteration-loop: done
                          # 2026-09-07 JOINT REVIEW OF PART A + PART B (Wolf's ruling: one review
                          # covers both). Four layers, FULL HOUSE -- second on record. 32 findings
                          # triaged: 3 decision-needed (all ruled by Wolf and implemented), 18
                          # patched, 10 LOW deferred, 1 dismissed. Full gate GREEN 597s, run by
                          # the reviewer.
                          # THE THREE THAT MATTERED, and all three are the same shape -- an
                          # instrument reporting success while measuring nothing, under a green
                          # gate: (1) `dirty_tiles` was STRUCTURALLY always 0 -- drained in
                          # `Update`, read in `Last` -- so AC10's steady/edit split could never
                          # fire and the column written to catch 10.6's missed edit cost was blind
                          # to it; its justifying comment cited a function that does not exist.
                          # (2) AC6's mandated test drove a binary that PANICKED every run,
                          # passing because `dwarf_report` read only stderr and never the exit
                          # status. (3) AC9's provenance-header clause was NOT IMPLEMENTED AT ALL
                          # -- `rg -i vsync` returned nothing in the repo -- while the story read
                          # 12 of 12.
                          # THE REVIEW ALSO SUPPLIED THE STORY'S MISSING EVIDENCE: the headline
                          # outcome (change the dwarf, see it without a rebuild) had no AC, no
                          # test and no recorded observation. It was watched working headless --
                          # asset swapped under a running client, `Reloaded` logged 2s later, the
                          # dwarf window converging on the pine control to 24 of 1,200 px. THE
                          # WINDOWS LEG (ReadDirectoryChangesW) HAS STILL NEVER BEEN SEEN TO FIRE.
                          # Mutation table 12 -> 18 rows; the 13th had shipped at 703bb94 with no
                          # recorded verdict while three places said 12.
                          # NOT MERGED, NO PR YET -- awaiting Wolf.
                          # 2026-09-07 ALL 12 ACs MET. AC8/UX-DR22 closed on the vehicle: Wolf
                          # "dwarves look like the model in blender", judged against the glTF
                          # read from the CHECKOUT (source=disk:), not the embedded copy.
                          # Task 4's launcher WALKED: happy path + MISMATCH (on a genuinely
                          # stale binary, not staged) + unrecognised stamp + diverged checkout
                          # + -dirty. Issue #74 seen and stays open; the campfire blow-out is
                          # NOT this story's -- the CUBE blows out identically, so the authored
                          # dwarf is exonerated and the decomposition went to #75.
                          # AWAITING the joint review with Part A (Wolf's ruling: one review).
                          # 2026-09-07 DEV DONE except AC8. Full gate GREEN 475s (pixel
                          # guards in, flaky #72 did not fire); mutation table 12 rows ALL
                          # KILLED at dev time -- a 13th was added later at 703bb94 with no
                          # recorded verdict, and the review added 5 more, so the table is now 18. 11 of 12 ACs met -- AC8 is UX-DR22 and needs Wolf at the
                          # Windows vehicle, as does Task 4's launcher, which is written but
                          # UNRUN (no Windows, no display here).
                          # SIX FINDINGS BEYOND THE ACs:
                          #  1. My own watcher test pinned NOTHING -- it asserted the flag
                          #     going in, not the decision coming out, so forcing the
                          #     override left it green. Only the mutation table said so.
                          #     4th self-referential test in this repo (1.1, 1.2, 1.3).
                          #  2. Perf-log batching lost 60 of 300 frames: the capture path
                          #     exits by PANIC, which runs no destructors. Per-frame writes
                          #     now; 153 rows from a run that still exits 101.
                          #  3. A column named `chunks` read 40,148 -- that is TerrainTile,
                          #     one per exposed CELL. Renamed, and it now sums both terrain
                          #     modes; --subdiv 1 and N>1 draw different components.
                          #  4. This story broke two of M2-1's mutation rows (they matched
                          #     an ADJACENT PAIR of systems) and the gate refused the
                          #     commit. Re-anchored on one symbol each, both re-verified.
                          #  5. AC2 nearly shipped a false finding: a no-dwarf window showed
                          #     72 changed px against a floor of 74 IN THAT SAME WINDOW.
                          #  6. `meshes=5` cannot see a scene that loads and draws nothing,
                          #     so the frames were LOOKED AT: five pines where five dwarves.
                          # 2026-09-07 STORY CREATED (ready-for-dev). Baseline cb45817 -- the
                          # PART A BRANCH TIP, not main; Part A is unmerged so `main..HEAD` is
                          # the wrong range for its gate AC (every M2 story is stacked).
                          # SCOPE CHANGED BY PART A OVER-DELIVERING: the authored dwarf is
                          # already shipped and embedded, so "Part B cannot start until Wolf has
                          # a model" is SPENT. B is the LOOP (--assets disk loading +
                          # file_watcher, Task 1's option A), the CHECKER, and the SIGN-OFF.
                          # THREE PREMISES CORRECTED THE RECORD, all executed not read:
                          #  1. check_asset.py does NOT reject the dwarf -- it ACCEPTS him and
                          #     SILENTLY UNDER-READS his palette by 3 cells (reads the pines'
                          #     7; the dwarf has 10). The unread three include the flame
                          #     #F0A63C, the one colour a pixel guard cares about.
                          #  2. notify-debouncer-full 0.7.0 CROSS-COMPILES AND LINKS to
                          #     x86_64-pc-windows-gnu -- 13MB .exe, exit 0. Part A's one
                          #     unclaimed risk is closed.
                          #  3. `source=embedded` is a HARDCODED literal in both instrument
                          #     lines, so the line B must change is itself the
                          #     text-that-does-not-move-when-the-maths-does shape (10.1).
                          # Also settled from vendored source: an absolute AssetPlugin.file_path
                          # REPLACES the base via Path::join, so option A stamps nothing into
                          # the binary; and file_watcher turns watching on GLOBALLY unless
                          # overridden, a cost every headless test would pay (AC5).
                          # TASK 7 RULED 2026-09-07 AND IT IS NOT A "PROBE": it is a performance
                          # LOG, so a live run can be READ AFTERWARDS instead of watched on
                          # screen. --perf-log <path> CSV, one row per frame, content counters
                          # (draws/tris/remesh_chunks) BESIDE the time so a flat number cannot
                          # pass as a measurement (140fps once survived a 39% triangle cut);
                          # summary = p50/p95/p99/p99.9 + hitch count, STEADY-STATE and EDIT
                          # frames split apart. Plus an on-demand key at the seat. CPU/GPU
                          # split considered and NOT bought -- llvmpipe cannot exercise GPU
                          # timestamps here. AC9/10/11 added; mutations moved 9 -> 12.
                          # WOLF RULED THE STORY RUNS WHOLE, Tasks 1-7, against the advice to
                          # split 6-7 off. Still open: issue #74's routing.
  10-8-lighting-and-atmosphere-re-judged-under-the-sun: done
                          # 2026-09-09 MERGED as PR #85 (merge dee3819), verified with
                          # `git merge-base --is-ancestor a6494f0 origin/main`. Full gate GREEN
                          # 485s at the tip; 54 commits.
                          # DONE WITH ONE AC UNMET, AND THAT IS DELIBERATE: **AC16 is NOT
                          # satisfied.** Its five consecutive full-tier runs were taken (5/5
                          # green, zero "wrote no PNG"), but issue #72's RACE WAS NEVER
                          # REPRODUCED -- reverting to the async `save_to_disk` and running the
                          # all-off guard three times left the PNG on disk every time. The fix is
                          # held by a deterministic ordering test and those five runs; the failure
                          # it prevents has not been observed in this venue. The PR body said so
                          # plainly and Wolf merged it, which is what accepted it. #72 stays OPEN.
                          # RULINGS 1-7 recorded in the story with Wolf's words. DEFERRED BY WOLF,
                          # not dropped: Ruling 3's fog and rim defects, the hover-slab defects,
                          # and the snow layer's thickness -- the last gets ITS OWN TERRAIN STORY
                          # ("I think we need to have a separate terrain story anyway"), because
                          # the "too thick" layer is `worldgen.rs:100` making every surface tile a
                          # FULL CELL of Snow or Ice, not the cap and not the renderer.
                          # Snowfall at any view angle -> Epic 11.2: it needs a camera-anchored
                          # field, a MECHANISM, and AC15 authorised constants only.
                          # NEXT IS **NOT 8.3** -- Epic 11 runs first (2026-09-08 ruling), and the
                          # terrain story is now also ahead of it. The board's numeric order has
                          # overridden a prose ruling three times; do not let it a fourth.
                          # ADDED 2026-09-08 on Wolf's instruction ("lighting and overall
                          # atmosphere/style story first") and RUNS BEFORE 8.3 -- the 2026-08-28
                          # ruling (look work ahead of the six-bar sign-off) still governs, and
                          # the board's numeric order does NOT: 8.3 stays backlog behind this.
                          # Defect on record: issue #75 ("lighting is still way off", 2026-09-06)
                          # plus NEAR_WHITE_AREA_CEILING red on every clean main since before
                          # 10.7. Re-measured at creation on main 3ed269c, debug build stamped
                          # 3ed269c, boot framing: near-white 2.1686% vs 1.5630% ceiling, exit 101.
                          # Every look constant older than 10.7 was tuned with the sun under the
                          # map; 10.7 re-tuned nothing by ruling. Epic text in epics.md.
                          # 2026-09-08 STORY CREATED -> ready-for-dev, then EXTENDED on Wolf's
                          # rulings the same day: --subdiv 4 becomes the shipped default and lands
                          # FIRST; snow/rock flank rule, atmosphere constants (on named defects),
                          # #62, #72 and #77 are IN. Day/night + post stack split to Epic 11.
                          # DEV CANNOT START until Wolf answers Ruling 1 at the opening sitting:
                          # SUN or NIGHT KEY, and which knob (intensities or one Exposure).
                          # Creation probes on 3ed269c AT --subdiv 1: torches off ALONE -> exit 0
                          # (near-white 1.3342%); sun off stays red (1.7378%). THAT DIED WITH THE
                          # k=4 DEFAULT -- Task 2, 2026-09-08: torches off alone now reads 1.6046%
                          # and stays RED; only campfire+torches together clears (1.3102%). And the
                          # AMBIENT is the real illuminant: -46.9 frame mean, 404x noise, against
                          # the directional's -6.8 / 59x. See 10-8-signoff/AC3-marginals.md.
  10-9-the-land-reads-natural: done
  10-10-take-the-camera-where-you-want-it: done
                          # DONE 2026-09-18: WOLF SIGNED OFF FROM THE SEAT after the review patch
                          # -- "perfect .. I am happy. Works like it should. So PR." That closes
                          # the seat round the patch was waiting on, and the two feel items he had
                          # HELD stand AS SHIPPED (wheel notch count, wheel direction), judged
                          # rather than argued. It also closes the gap no devpod could: MMB orbit,
                          # shift+MMB pan, a real wheel notch, C printing a line, clicking a dwarf
                          # and Escape have now been exercised by a person with a window.
                          # The THIRD held item is NOT closed and is not claimed as closed --
                          # discoverability (no --help, the hint bar still reads only
                          # "1 dig 2 channel 3 stockpile 4 clear") is invisible to an operator who
                          # already knows the controls, which Wolf does. Deferred, not ticked.
                          # 10.10 REVIEW-PATCHED 2026-09-18. ALL TWELVE patch items
                          # closed; the 3 deferred stay deferred and the 3 seat items Wolf HELD
                          # stay held, so what the story still owes is a SEAT ROUND and sign-off,
                          # not code. Two decisions went to Wolf first, neither having a ruled
                          # value: pan's fast modifier (CTRL, over alt, which Linux window managers
                          # claim before the client ever sees the drag) and the selection distance
                          # (20 -- a dwarf reads ~49 px against 10.8 at the boot 90).
                          # FULL GATE GREEN 557s at 2078e5a (RUST_TEST_THREADS=6, foreground),
                          # INCLUDING the pixel-guard tier at 380s -- the tier the review's own
                          # --fast run skipped and called a coverage hole. That hole is closed.
                          # BOTH ORACLE DEFECTS FIXED AT SOURCE, not corrected for: the pick ranks
                          # the DRAWN translation (blend lag and draw offset included) through the
                          # LIVE camera's world_to_viewport, the same seam update_pick already used,
                          # with the radius in pixels off logical_viewport_size. The two new tests
                          # STATE THEIR OWN DISCRIMINATION rather than leaving it to a mutation: the
                          # wire cell sits 105 px from the cursor against a 64.8 px radius at
                          # distance 20, and at 900x1200 the 16:9 projection puts the dwarf 227 px
                          # from where he is drawn. install_pick_camera takes a viewport now --
                          # every picking test had used 1920x1080, the one aspect where the
                          # hardcoded constant is right.
                          # THE THREE UNTESTED SEAMS ARE PINNED: shift+MMB pan (nothing across all
                          # 551 rows), the readout key with its LastCameraReadout, and the live
                          # rig->framing string the capture reports -- the last through the REAL
                          # BINARY in pixel_guard.rs, because that is the only place the live query
                          # exists. Sabotage table re-run WHOLE (the code moved; "the anchors did
                          # not change" is not evidence): 31 rows, 31 KILLED, and one OLD row came
                          # back NO-COMPILE -- it injected a `DetectChanges` import the module now
                          # has of its own, so it read as a row in the file while pinning nothing.
                          # Three rows re-pointed in all; the pre-commit hook's mutation audit
                          # caught two of them before the commit landed, which is what that probe
                          # is for.
                          # ISSUE #99 IS OVERTURNED ON ITS HEADLINE, re-verified here rather than
                          # relayed: --camera 0.7,0.45,45,64,64,9 exits 101 on the NEAR-WHITE
                          # assertion with the ground median at 91, comfortably inside [70,180].
                          # The ground median does not climb smoothly with the zoom -- 81 at d=90,
                          # 83 at d=80, 91 at d=45, 202 at d=20 -- which is what a two-sample
                          # generalisation missed. #99 stays OPEN for its second finding (the
                          # recorded --distance 80 trigger is not repeatable).
                          # FOR THE SEAT ROUND: the pan feel Wolf judged on 2026-09-17 was the
                          # always-on 4x at the boot zoom, which is now CTRL+shift+MMB. Plain
                          # shift+MMB runs at the documented 0.12 rate, which nobody has felt yet,
                          # and every pan is distance-scaled now.
                          # ACs 2, 3 and 8 AMENDED on Wolf's rulings -- outside the dev workflow's
                          # normally permitted sections, logged in the story's Change Log.
                          # 10.10 CODE-REVIEWED 2026-09-17 -> in-progress. FOUR LAYERS, ALL LIVE.
                          # 12 patch items + 3 deferred + 3 dismissed; patches LEFT AS ACTION ITEMS
                          # for a separate review-patch session (Wolf's choice). Wolf ruled three
                          # decisions on the spot: clamp what the READOUT PRINTS (not the aim
                          # point); 10.10 OWES THE SELECT-ZOOM that AC8 dropped at creation and
                          # `epics.md` still specifies; patch only the two OBJECTIVE seat items
                          # (dead shift conditional in the pan branch, distance-scaled pan) and
                          # HOLD wheel direction / notch count / discoverability for a seat round.
                          # VERIFICATION ACTUALLY RUN BY THE REVIEW: `gate.sh --fast` GREEN 58s at
                          # 339a07f -- a COVERAGE HOLE, not a full gate: it skips serve.rs (61
                          # daemon tests) and pixel_guard.rs (2 frame guards), and NOTHING has run
                          # the full gate at 339a07f (the record's GATE GREEN 511s is 4b3f75b, two
                          # commits back). Sabotage table re-run independently: 16/16 KILLED, so
                          # AC12 no longer rests on the dev's word. Repo binaries rebuilt after the
                          # mutation run -- `gui build 339a07f`, no -dirty.
                          # THE TWO FINDINGS THAT MATTER MOST: (1) the dwarf pick's oracle disagrees
                          # with what is DRAWN, two ways at one site -- it projects the raw wire cell
                          # (not the blended, -0.5Y-offset figure) through a projection hardcoded to
                          # BOOT_ASPECT_RATIO (while the render camera's aspect follows the live,
                          # never-locked resizable window). Both hide inside the 0.06 radius at boot
                          # and exceed it at distance <=40 -- i.e. clicking a walking dwarf fails at
                          # exactly the close zoom this feature exists for. Every picking test uses
                          # 1920x1080 AND derives its cursor from the same projection the pick uses,
                          # so oracle and subject share the error term. (2) TWO EVIDENCE CHANNELS ARE
                          # UNOBSERVABLE: `LastCameraReadout` was built as "the seam so a test can
                          # assert what the operator saw" and NOTHING reads it; the capture's live
                          # rig->framing seam is the same shape. Same class the story's own Change
                          # Log records closing for the wheel three commits earlier.
                          # ALSO: shift+MMB pan is pinned by no test and no row across all 551;
                          # `epics.md:2043` still publishes AC1's STRUCK luminance clause and its
                          # falsified figures (0.0048, 76.1236, "766x"), so the phantom regression is
                          # still re-derivable from the epic; and ISSUE #99 IS WRONG -- near-white IS
                          # reachable via zoom at d=45 (ground median 89, near-white 2.6951%, ceiling
                          # trips and names the framing), so AC11 is live-demonstrable in one command.
                          # CONVERGENCE, measured not inferred: 5 in 15 findings (4-way on the
                          # unclamped focus, 3-way on --distance overriding --camera, 2-way on the
                          # pan hole / readout key / capture framing) against Epic 3's 1-in-8.
                          # REVIEW INCIDENT, and it changes a standing instruction: the acceptance
                          # layer sabotaged the SHARED WORKING TREE mid-review. It tried to check
                          # whether the mutation anchors were stale by `exec`-ing each payload with
                          # `pathlib.Path` monkeypatched to swallow writes -- but every payload opens
                          # with `import pathlib`, and `import X` inside `exec` REBINDS the name to
                          # the real module, so all 16 payloads wrote to five tracked source files at
                          # 17:07:32 in one 5ms burst, no restore, while three siblings compiled
                          # against that tree. Restored on Wolf's authorisation; diff archived; the
                          # blind layer was re-run from a rebuilt cache and CHANGED its headline
                          # finding on clean source (its "boots at 800x600" premise was false --
                          # Bevy 0.19 defaults to 1280x720). A mutation payload reports whether it
                          # applies BY APPLYING IT: every future layer prompt must ban executing the
                          # payloads, not merely `scripts/mutate.sh`.
                          # DONE 2026-09-11 at the closing sitting: Wolf signed off the `df0f722`
                          # frame ("cool ... I think we are done with the story"), which met AC12 --
                          # the last AC outstanding. MERGED 2026-09-11 06:09Z as PR #91, merge
                          # commit `6e5f472`; `origin/main` is `6e5f472` and `git merge-base
                          # --is-ancestor ab6e7d7 origin/main` passes, so the story tip really is in
                          # main. The merge landed the working copy on `main` mid-session for the
                          # SIXTH time -- this record was written from a `10-9-closure` branch off
                          # main for that reason. Nothing was stacked on #91, so no base pointer
                          # needed retargeting.
                          # TWO RULINGS CAME OUT OF THE SITTING, both on the card. Ruling 2 (the
                          # lake does not read as ice): "keep the lake don't touch it" -- accepted
                          # as it stands. Ruling 3 (the scour lattice): the patches read as
                          # ARTIFICIAL PLATES, and the cause was that ice is the one material drawn
                          # dead flat (`material_detail_depth` depth 0 -- right for a lake, wrong on
                          # a slope). Scour now emits `Stone` (depth 1) with the snow cap LEFT ON,
                          # Wolf's choice between two built-and-captured variants. Side effect worth
                          # carrying forward: ice is now the LAKE's exclusive material, the
                          # uniqueness AC9 claimed and had to retract at review.
                          # WHAT THE RULING DID NOT FIX, so Epic 11 does not inherit a false
                          # premise: the lattice is still a fixed 16-cell grid on 19.0% of sloped
                          # columns, and with the cap on NOTHING in the frame reads as exposed rock
                          # -- the patches read as snow-covered broken ground. Neither is filed.
                          # Ruling 1 (AC1's `x=127` / `y=0` off-screen) was closed by the sign-off
                          # WITHOUT a framing change, so AC1 stands on its projection mapping. That
                          # is the orchestrator's reading of "we are done", recorded so it can be
                          # contradicted rather than assumed.
                          # ADDED 2026-09-10 out of 10.8's closing conversation, and it runs
                          # BEFORE Epic 11. THE TERRAIN STORY the 10.8 entry promised -- but note
                          # the scope MOVED during creation: the "too thick snow" that named it is
                          # NOT here. That is a full-cell worldgen fact needing a cover depth on
                          # the tile model, i.e. AD-19's terrain-STATE epic. What IS here is the
                          # terrain LOOK: Wolf 2026-09-10, "with subdiv 4 the whole landscape is
                          # blocky all around.. small blocks all the places like noise".
                          # CAUSE FOUND AND MEASURED: `detail_depth` (gui/src/project.rs:1165) is
                          # uncorrelated per-fine-voxel hash noise whose OWN docstring calls it "a
                          # MEASUREMENT STAND-IN for 10.4's authored terrain look, not a visual
                          # decision ... 10.4 owns the real look and this is the copy it will
                          # replace". 10.4 never replaced it and 10.8 made --subdiv 4 the shipped
                          # DEFAULT -- so a measurement placeholder is the game's default look.
                          # This is 10.4's unpaid debt, which is why it is Epic 10 and not 11.
                          # MEASURED AT CREATION on main c54b793: subdiv 4 triangles=927,622;
                          # deliberate RED with detail_depth forced to 0 -> triangles=31,968.
                          # THE NOISE IS 96.6% OF THE TRIANGLE BUDGET. The RED also corrected the
                          # instrument: faces= moved only -33% and does NOT discriminate;
                          # triangles= moved -96.6% and does. Restore was re-measured to
                          # triangles=927622 exactly, so no mutant binary survived.
                          # SEED DOES NOT CHANGE: layered_terrain is the LAST consumer of
                          # STREAM_WORLDGEN, so the material rewrite leaves heights, ramps, camp,
                          # trees and dwarves bit-identical; material has no gameplay meaning
                          # (is_standable never reads it). Ridges and the frozen lake DO move
                          # heights and are LOCAL POST-PASSES, never a change to height_field.
                          # Frozen lake is in on Wolf's explicit call -- "I want to test biomes
                          # and introduce concept" -- so it is a biome PILOT and AC10 makes the
                          # biome decision prove it is CONSUMED, not merely produced.
                          # --subdiv 1 STAYS (Wolf ruled it; I had over-read an earlier remark).
                          # NOT here and deliberately: the rim dissolve and fog (10.8 Ruling 3
                          # defect (d), still deferred -- same defect, different lever, so they
                          # must be judged one at a time).
  epic-10-retrospective: optional

  epic-11: in-progress
  # THE ART-SHOT LOOK. Added 2026-09-08 (Wolf) out of 10.8's creation. FEATURES, not re-tuning:
  # ambient occlusion + bloom + exposure (11.1), depth of field + volumetric haze (11.2), a
  # day/night cycle that BOOTS AT NIGHT (11.3). EXECUTION ORDER IS NOT NUMERIC ORDER for the
  # milestone: Epic 10 (10.8) -> Epic 11 -> 8.3. Inside the epic the numbering IS the order
  # (post stack first, cycle last) and Wolf may reorder at 10.8's closing sitting.
  # Every mechanism is already compiled in via 3d_bevy_render (verified in the pinned 0.19.0
  # source) -- no bevy feature change. Whether any renders under lavapipe is UNKNOWN; each
  # story's first task is that probe. NFR6 re-read with --perf-log after every effect.
  # 11.1 SPLIT INTO 11.1a AND 11.1b (Wolf ruled 2026-09-18, at 11.1's creation). Verifying the
  # epic's own premises against the pinned 0.19.0 source found three things.
  # (1) SSAO IS INERT WHILE MSAA IS ON, AND FAILS SILENTLY. extract_ssao_settings
  #     (bevy_pbr-0.19.0/src/ssao/mod.rs:479-494) logs an error! and then RETURNS OUT OF THE LOOP --
  #     not continue -- so EVERY camera is skipped. Msaa's default is Sample4 and this client sets
  #     Msaa nowhere, so AO added as 11.1 described it renders a clean frame and exits 0 with no
  #     occlusion in it. Bloom's Hdr and SSAO's prepasses ARE automatic (#[require(...)]);
  #     Msaa::Off is the only prerequisite a human must remember and the only one that fails quietly.
  # (2) Msaa::Off and Hdr each move EVERY PIXEL before any effect is judged, so the live-frame
  #     guards re-baseline against that change alone, not against it plus two effects.
  # (3) THE EPIC'S BLOOM AC WAS UNSATISFIABLE HEADLESS and is struck in epics.md. Two independent
  #     reasons: deferred-work.md:1237-1246 already forbids judging a headless area figure against
  #     ceilings calibrated on a GPU frame (llvmpipe under-reads ~16%); and measured over FOUR
  #     same-build boot captures at creation, near-white has 0.1284 pp of headroom against a
  #     0.2388 pp spread and blown-pool 0.0154 pp against 0.1568 pp -- a margin ONE TENTH of its own
  #     noise, so a capture can trip a ceiling by luck with nothing changed. That is issue #90 at
  #     four samples instead of two, extended to blown-pool. REPLACED BY a headless on/off DELTA
  #     plus a vehicle-judged ceiling. NO CEILING WAS RAISED -- 10.8's rule holds.
  # FXAA, NOT TAA (Wolf ruled). TAA needs MotionVectorPrepass + jitter and accumulates across frames;
  # falling snow, flickering light and a 160-frame settle make that unproven here. SMAA rejected: it
  # needs the smaa_luts feature, absent from the root list, and crossing a bevy feature boundary
  # rebuilds ~400 crates and has nearly OOMed this devpod.
  # THE "ALREADY COMPILED IN" PREMISE HOLDS: 3d_bevy_render expands to include bevy_anti_alias,
  # bevy_pbr and bevy_post_process, and all three plugins are in DefaultPlugins -- a component alone
  # is enough, no add_plugins owed, no feature change.
  # AND THE EPIC'S LAVAPIPE UNKNOWN IS ANSWERED FOR SSAO: its only GPU gate is
  # max_storage_textures_per_shader_stage >= 5 (ssao/mod.rs:61-70); this devpod's llvmpipe reports
  # 48, so the plugin loads. That proves it LOADS, not that it looks right. Bloom/DoF/fog unprobed.
  # Creation control (4 same-build captures on 41b3f02) filed at 11-1-signoff/task-0-control.md,
  # with the crease instrument's own GREEN (p10/median identical across a same-build pair -- a ZERO
  # noise floor) and RED (--lights-off ambient moves crease p10 30->0 while open snow's median moves
  # 117->104). The camp window is DIAGNOSTIC ONLY: its mean moves 1.19 between same-build runs.
  # 2026-09-19: CODE REVIEWED -> in-progress. FOUR LAYERS, ALL LIVE, NO COVERAGE HOLES (every layer
  # ran cargo and executed binaries; reviewed in an isolated worktree because the repo tree sits at
  # 11.1b, which is stacked on this story and changes the same render path). 4 decisions + 8 patches
  # + 3 deferred, 2 dismissed. Patches on `e89af02`; 7/7 mutation rows KILLED; FULL GATE GREEN 434s.
  # Status is in-progress, NOT done: three ACs are vehicle-bound and only Wolf's seat can close them.
  #   TWO RULINGS came out of the review and bind beyond this story.
  #   (1) GROUND_LUMINANCE_FLOOR 70 -> 55 (crates/gui/src/capture.rs). Nothing was failing when it
  #       moved. It was placed with 53 levels of headroom under a 123-level approved artifact; 10.7
  #       lifted the sun and 10.8 re-ruled the lighting, the shipped frame drifted to 80, and the
  #       headroom fell to 10 without anyone touching the floor -- so a guard built to catch an
  #       UNLIT frame had quietly become the binding constraint on the camera exposure, which is the
  #       art decision this story exists to hand to the seat. Every failure class it was built for is
  #       still caught: --lights-off ambient reads 45, all-off reads 0.
  #   (2) Exposure ruled to 10.5 EV100. 9.7 was byte-identical to Bevy's Exposure::default(), so
  #       deleting the component changed nothing and its sabotage row could only kill inside the
  #       MinimalPlugins harness. EV100_OVERCAST (12.0) is unreachable at ANY floor -- that frame
  #       reads ~32, darker than a broken --lights-off ambient frame, so no floor both admits it and
  #       catches an ambient regression. 10.5 is the darkest value that survives; the binding path is
  #       --fx-off fxaa at 58 against the 55 floor.
  #   AC7 restated from "25 levels" to "75% of the window's own p10" -- the absolute form is
  #     arithmetically unreachable at 10.5, where the crease window's own p10 is 23. No bar lowered:
  #     the instrument drives p10 to 0, a 100% fall, and the old measurement passes the new form too.
  #   AC8 CORRECTED: its "residual 2,042, headroom only 258 px" premise was already false before the
  #     story began -- the untouched 41b3f02 baseline measures 43 px / 12 blobs. The recorded cause
  #     was backwards too: Msaa::Off RAISED the count 43 -> 61, only FXAA lowered it.
  #   AC11 AMENDED: its first clause was recorded CLOSED on evidence that did not meet it. The
  #     MSAA-4x control it named cannot be built from this branch at all (Msaa::Off is unconditional,
  #     no flag restores it), so the comparison was unfalsifiable and no pair was ever filed.
  #   HIGH finding folded into #108 by Wolf's ruling: Msaa::Off takes the enclosed-sky blob count
  #     12 -> 18 against a ceiling of 20, and the shipped 7 holds only because FXAA blends those
  #     edges down -- FXAA erases 76% of blobs and 81% of the <=4 px ones, which is exactly 10.7's
  #     trunk-hole family, so the guard would go green on the defect it was built for.
  #   TWO instrument HIGHs were still live at 11.1b's tip and 11.1b measured its own figures with
  #     them: creases.py reported a confident GREEN (+0 everywhere) when two captures shared a label,
  #     and never checked that two captures share a resolution. Both patched here.
  #   STILL OWED, vehicle-bound, on 11-1-signoff/task-5-vehicle-card.md: AC10 (its recipe was also
  #     corrected -- it omitted --headless, so vsync would have clamped both p50s and measured the
  #     monitor), AC11's exposure clause (10.5 was ruled from lavapipe numbers; nobody has SEEN it),
  #     and AC4's on-screen clause, which the story did not name as vehicle-bound but is: zero pixels
  #     of the readout's colour appear in a real capture, and no human has pressed F10.
  # 2026-09-19 (LATEST, after the gingerspice sitting on merged build `20b9005`):
  #   AC10 MET. p50 3.47 ms FXAA-on vs 3.55 ms with --fx-off fxaa, against NFR6's 16.67 ms bar --
  #     4.7x headroom, so FXAA does not cost the bar and no ruling is owed. The reading rests on the
  #     SIGN, not the size: FXAA is faster on EVERY percentile, which a post-process pass cannot be,
  #     so the delta is run-to-run variance and the cost is below the floor. NO cost figure is
  #     claimed -- one run per condition is not a floor (#98). `--headless` was load-bearing and the
  #     preamble confirms `vsync=off`; without it both p50s would have clamped to the refresh rate.
  #   AC11 CLOSED IN FULL. Edges closed 2026-09-18 (`d3ecdff`); exposure closed 2026-09-19
  #     (`20b9005`) -- Wolf judged the frame and confirmed 10.5 EV100. Until this sitting the value
  #     was RULED from lavapipe ground-medians and had never been seen on the delivery GPU.
  #   ONLY AC4's ON-SCREEN CLAUSE REMAINS, and it needs a WINDOWED run: neither readout renders
  #     headless (zero pixels of its colour in a real capture), so the AC10 captures could not carry
  #     it and nobody has yet pressed F10 on a machine.
  #   NOT established by these runs, recorded so nobody over-reads them: the p99/p99.9 tail is 1-2
  #     frames of a 0.7 s run and is start-up, not steady state; `edit frames: none recorded`, so
  #     they say nothing about the re-mesh cost that is the expensive frame in this game.
  #   NEW RISK FILED, #111: the bloom guard's near-white clause is FLAKY at the ruled exposure --
  #     2 failures in 4 full-gate runs, passing alone and 8/8 under the gate's own --ignored flags.
  #     Its bar is a 0.10 pp fall; calibrated at 9.7 EV100 the fall was 0.32 pp over a 0.019 floor,
  #     but 10.5 halved near-white (5.17 -> 3.53 %) and halved the signal: measured 0.1137 and
  #     0.1679 pp, so the worse run clears by 0.0137 against a 0.019 noise floor -- THE MARGIN IS
  #     SMALLER THAN THE NOISE. Third guard the exposure ruling has taken headroom from (all-off
  #     drop 21.707 -> 8.707; ground floor's binding path 58 vs 55) and the first to go under.
  #     NOT patched: lowering a bar until runs pass is what 10.8's rule forbids, and the floor at
  #     10.5 is unmeasured. #111 carries the measurements and the ratio-vs-absolute option.
  # 2026-09-19 (FINAL): AC4 CLOSED AT THE SEAT -> STORY DONE. All twelve ACs met or closed.
  #   AC4 needed a WINDOWED run and nothing before it could carry the clause: neither readout renders
  #   headless (zero pixels of its colour in a real capture), so the string had been proven only as a
  #   Text component inside a MinimalPlugins app. Wolf observed BOTH halves: F10 visibly re-roughens
  #   the polygon edges and NOTHING ELSE in the frame moves -- the correct signature of an AA pass --
  #   and the readout text flips between "F10 fxaa on" and "F10 fxaa off".
  #   A WRONG DIAGNOSIS was raised and withdrawn en route, recorded because the error is reusable:
  #   from Wolf's first "cannot see any difference" a source reading concluded all three runtime
  #   toggles were inert, since they remove the component and `extract_components` queries
  #   `Query<(RenderEntity, &C)>` so a removed component stops matching its own cleanup branch. True,
  #   but not the whole plugin -- `ExtractComponentPlugin::build` ALSO installs `SyncComponentPlugin`,
  #   whose `on_remove` hook does the render-world removal. One system was read in isolation instead
  #   of the plugin that assembles them. The SEAT observation was right and the source reading wrong;
  #   no issue was filed. Wolf's "no exposure difference" was also correct and not a fault: both
  #   captures are 10.5, and against the 09-18 frame Bloom returns ~39% of the darkening while the
  #   sky never moves at all (#113, the sky is a ClearColor and bypasses Exposure entirely).
  11-1a-a-chosen-exposure-and-a-clean-edge: done
  # 11.1b inherits the render path and the instrument 11.1a builds, and owes the AC that proves AO's
  # output is CONSUMED rather than silently skipped -- the seam 11.1a can only place, not exercise.
  # 2026-09-19 (LATER): REBASED ONTO 11.1a's REVIEWED TIP (122271c). 38 commits replayed, only two
  # conflict resolutions needed (tech-art-guidelines.md's Lights table, deferred-work.md's appended
  # sections) -- 11.1b touches neither the Exposure line nor GROUND_LUMINANCE_FLOOR, so ingest.rs and
  # capture.rs auto-merged. FULL GATE GREEN 525s at `b60a6f5`.
  #   ONE CONTROL RE-BASELINED by the exposure ruling: CONTROL_OPEN_SNOW_MEDIAN 116 -> 93
  #     (pixel_guard.rs). It is an EXPECTED POST-STACK VALUE, not a bar -- the assertion is equality,
  #     so 93 is neither weaker nor stronger than 116 -- and it moved because Wolf ruled the exposure
  #     9.7 -> 10.5, a deliberate change to the frame rather than drift. Measured on the guard's own
  #     flags, fresh daemon per capture, FOUR same-build captures: open-snow-LL and -LR both
  #     93/93/93/93, spread 0. AO's terrace darkening is untouched at 0.540 against its 0.30 floor.
  #   THE VEHICLE CARD's review corrections were reverted by the replay and restored: an earlier
  #     11.1b commit had written 11.1b's card over 11.1a's `task-5-vehicle-card.md`, and a later one
  #     correctly restored 11.1a's -- which also restored the pre-review version. The load-bearing
  #     part is `--headless` on both `--perf-log` runs; without it vsync clamps both p50s to the
  #     refresh interval and AC10 measures the monitor, not FXAA.
  #   FLAKE OBSERVED, not a regression: `bloom_lifts_the_camp_halo_without_brightening_open_snow`
  #     failed once under the full gate and then passed alone (130s) AND passed 8/8 under the gate's
  #     own `--ignored` settings, before the full gate went green. Same shape as #72 and #88. The
  #     assertion text was not captured before the run was superseded, so no issue was filed on a
  #     failure nobody can reproduce from the record -- if it recurs, capture the panic first.
  #   STILL OWED and unchanged by the rebase: 11.1b's own recorded figures (camp spread, near-white,
  #     bloom deltas) were measured at 9.7 EV100 and floor 70. The GATE does not pin them, so they
  #     are stale PROSE rather than a red test, and they describe a build that no longer exists.
  11-1b-the-air-has-depth: done
  # 2026-09-20: DONE, merged as PR #115 (main c7f67f5). The sitting was held on the vehicle against
  #   build 5cd523c: AC8 MET (worst p50 3.55 ms against NFR6's 16.67 ms bar), AC9 signed off by
  #   Wolf, and AC5's deferred near-white ceiling CLOSED -- it held with +0.4449 pp of headroom,
  #   where the card had predicted a trip. Evidence: 11-1-signoff/task-5b-vehicle-evidence.md.
  #   All 13 [Review][Patch] items are ticked: ELEVEN were already fixed in the tree and only the
  #   boxes had not moved, two were genuinely open, and three MORE stale notes were found off the
  #   review list. Every one was the same failure -- a correction written BESIDE the stale text
  #   instead of replacing it -- which is why the line below is struck rather than deleted.
  #   The "STILL OWED" note above is partly discharged: the near-white figures were re-measured at
  #   the current exposure (n=18) for the bloom floor re-baseline in PR #116.
  # SUPERSEDED 2026-09-20 -- the vehicle-bound work below was done at the sitting:
  # 2026-09-19: AC1 and AC4 are now MET and the two blocking rulings are recorded on #105/#107.
  # Branch story-11-1b-the-air-has-depth.
  # CODE REVIEWED 2026-09-19 -- see the review block at the bottom. Stays in-progress: the
  # remaining work is vehicle-bound (Task 5, AC8, AC9) and only Wolf's seat can close it.
  #   AC1 MET, as written, no bar lowered (#105). The residual was never flicker: --static-world
  #     is documented as "freeze the sim" (README.md:201) and froze NOTHING -- it set a bool that
  #     silenced the capture's motion assertions while the daemon kept ticking. Wired it to the
  #     SetSpeed { Paused } command.rs has sent since 10.5. crates/gui ONLY; no wire, no sim.
  #     Camp spread collapsed: median 4 -> 0, p90 17 -> 0, near-white 1.7658 -> 0.0017 pp.
  #   AC4 MET, amended on Wolf's ruling (#107). Bloom::default() is EnergyConserving, which
  #     redistributes energy OUT of bright cores, so the bright tail the AC asked to RISE cannot.
  #     AC4 now measures the HALO its own prose already named: median +13, mean +6.98 against
  #     floors of 0 and 0.035. Preset UNCHANGED -- the story still tunes no look.
  #   AC2 amended: p10 -> terrace MEAN (Hdr killed p10; it reads 38 both ways), open-snow control
  #     named as 116. ATTRIBUTION CORRECTED: the 117 -> 116 shift is BLOOM's, not Hdr's. The two
  #     arrived in one commit (Bloom #[require]s Hdr) and --fx-off bloom is the first control to
  #     separate them -- it reads 117/117 with Hdr still on. Three documents had it wrong.
  #   AC2/AC3 re-measured on the paused world: terrace mean AO-on 69.485 vs AO-off 70.087 against
  #     a 0.001 floor -- a 600x margin, where it used to be a fraction of the floor.
  #   AC10 VERIFIED against this story's OWN range (d3ecdff..HEAD, never main): protocol,
  #     sim-core, client-core, simd and tests/capture.rs all empty.
  #   Task 6: TWELVE rows, ALL KILLED, incl. both pause-path failure modes (never sent; sent as
  #     Normal). git status clean after the run (#104).
  #   Task 5 stays UNCHECKED: vehicle-bound, card at 11-1-signoff/task-5-vehicle-card.md.
  #   #108 RULED by Wolf 2026-09-19: RETIRE the enclosed_sky guard, rebuild the oracle as its own
  #     story. It was INERT, not vacuous -- const SKY = [5,12,28] is an EXACT match and Hdr moved
  #     the sky to [7,15,31], so ZERO pixels classified as sky (8,434 did pre-Hdr). It returned 0
  #     holes because it could not SEE sky and would have passed with the terrain absent. Test and
  #     helper removed; a ledger comment in pixel_guard.rs names what is still covered (the
  #     geometric mask tests, whole-world face oracle) and what is NOT (a hole opened where nothing
  #     was pointed). #108 carries the rebuild.
  # CODE REVIEW 2026-09-19 (four layers, no coverage holes, no layer timed out; the earlier
  # "two open items" line promised an item that #108's ruling had already closed):
  #   NO SELF-GATE EVER RAN ON THIS STORY: every codex attempt across four handoffs died on a
  #     read-only /tmp mount-registry lock and the three-pass cap is spent (#49). The code review
  #     carried the adversarial load alone, which is why it was run at four layers.
  #   19 patch items, 3 deferred, 0 dismissed. Wolf ruled six decision items at the sitting; all
  #     six became patches and all were applied in-session.
  #   BOTH 2026-09-19 AC AMENDMENTS HELD under independent reproduction -- terrace p10 reads 38
  #     with AO on AND off, and Bloom::default() is EnergyConserving, so neither original clause
  #     was satisfiable. They were corrections, not lowered bars.
  #   THE BIG ONE: --static-world froze the world at whatever tick it had reached at connect, so
  #     captures meant to be compared froze worlds up to 35 ticks apart. ONE DAEMON PER CAPTURE is
  #     the fix and it is measured: fresh daemons land on tick 40 every time and the camp spread is
  #     0.0070 pp; a shared daemon spreads 0.3619 pp and FAILS AC1's bar by 2.05x. AC1 stands, with
  #     a precondition nobody had written down. It is now in README, the vehicle card, and both
  #     rendered guards.
  #   Also fixed: the pause outlived the client and froze every later one (the daemon's speed is
  #     one global); Space silently resumed a --static-world run; AC3 asserted a LEVEL that bloom
  #     was propping up, now a delta; AC4 had NO rendered guard despite the spec promising one;
  #     two mutation rows were weak kills that died on an unreachable! arm before any key
  #     assertion ran; a re-pointed 11.1a row had stopped sabotaging the readout's on/off state.
  #   CORRECTED IN FLIGHT: the review's "the AC3 guard passes with SSAO absent" was OVERSTATED --
  #     --fx-off ao,bloom clears the old terrace ceiling at 68.403 but reads open-snow 117 against
  #     an == 116 assertion, so the guard still reddened. The stated mechanism had stopped
  #     discriminating; the open-snow "control" was silently doing the work.
  #   AND: "600x margin" divided by a TWO-sample floor. Three layers read that statistic across
  #     0.035 on one build, so the real margin is ~36x. Nothing was failing; the number was.
  #   #106's RULING NOW LIVES IN THE STORY, not only on the issue: AO is accepted as WIRED and
  #     PROVED, is NOT visible from the seat, and its look judgement moves to 11.3. #106 stays open
  #     for the AC-bar half, which the review showed applies to AC4 as well as AC2.
  #   Task 5's card had OVERWRITTEN 11.1a's card, whose own Task 5 is still unchecked; 11.1a's is
  #     restored and 11.1b's now sits at task-5b-vehicle-card.md as the spec always said.
  # STILL OPEN, and only the vehicle can close it: Task 5, AC8 (perf read), AC9 (Wolf's sign-off).
  11-2-the-miniature: done
  #   Created 2026-09-21. BOTH mechanisms probed LIVE on 7442174 and reverted (creation-probe.md):
  #     DepthOfField and VolumetricFog both render under lavapipe -- the epic's unknown is answered.
  #     DepthOfField::default() is a SILENT NO-OP here (coc = 0.09 px at f/1.0); focal_distance =
  #     rig.distance misses the camp by 46% (the boot composition push makes it 61.7, not 90);
  #     VolumetricFog at defaults DARKENS (ground median 69 -> 30) and panics capture.rs:1498.
  #   HAZE SOLVED AT CREATION 2026-09-21, and the floor collision is STRUCK: it was three wrong
  #     settings, not a guard bounding the art. (1) ambient_intensity is relative to Bevy's
  #     AmbientLight default of 80, not this scene's 1500 -- derive 0.1*1500/80 = 1.875;
  #     (2) fog_color stays WHITE, the tech-art "fog = sky colour" rule is a DistanceFog rule and
  #     volumetric fog ADDS light (sky-coloured fog took the ridge 49->31 instead of 49->69);
  #     (3) a bounded FogVolume box shows its TOP FACE as a hard band across the sky, which every
  #     pixel window PASSED -- fix is a vertical density_texture. Proven recipe reads at ground
  #     median 69, the control's own value. NO SPLIT.
  #   APERTURE RULED 0.05 by Wolf at creation, against the measured bracket and the filed frame
  #     (ridge -36.1%, camp -3.2%, 32 of 78 star cores, sky peak 157). NO DECISIONS OUTSTANDING --
  #     the story is ready to implement. Both look values are re-judged on the vehicle at the
  #     sitting like every other, AC14.
  11-3-night-falls-day-breaks: in-progress
  #   2026-09-25 PATCH PASS: all 4 review patches applied (docs + mutation row, no code change). Next = AC13 vehicle sitting, then full gate + PR.
  #   2026-09-24 CODE REVIEW run 1 on d917314: 0 HIGH. Four patches were LEFT AS ACTION ITEMS (the mutation row, two vehicle-card
  #     warnings, the dusk disclosure). The predawn floor trip is DEFERRED to #125. AC13 stays open until the vehicle sitting.
  #   2026-09-24 DEV STARTED -> in-progress, delegated to Codex (gpt-6-sol/high), sitting 1 = Tasks 0-4a, STOP at 4a for Wolf's day pick.
  #   Created 2026-09-24 on 0fbb6ce. RULED at creation (Wolf): TICKS_PER_DAY 24,000 (8 min per day at
  #     Fast); the moon MOVES and passes the approved key direction exactly at BOOT_HOUR 22, every
  #     capture pinned (--clock, else the boot hour by default); the day look is picked by Wolf from
  #     Task 4's candidates BEFORE the day table is final (story runs in TWO sittings, one story);
  #     sky is TABLE-DRIVEN and flat, Exposure fixed at 10.5 -- Bevy's Atmosphere was probed live in
  #     an isolated worktree and NOT adopted ("none of those athmosphere shots are convincing"), but
  #     Wolf "might change my mind", so nothing may foreclose it. #113 and #106 land here.
  #   Control on 53ba44e: two boot captures cmp-IDENTICAL, ground median 69; RED --lights-off sun -> 50.
  #   NOTE the board picks 8.3 first by number; the 2026-08-28/09-08 rulings put 11.3 before it.
  epic-11-retrospective: optional

# ACTION ITEMS HAVE MOVED (2026-08-31, forge-process 1.3.2 — Wolf's ruling).
#
# There is no `action_items:` block here any more. Its absence is CORRECT: it is not a missing
# required field, not a validation failure, and not something to re-add.
#
#   state     -> GitHub issues on jeicei75/frostvein labelled `action-item`, and nowhere else.
#                Read them with:
#                  gh issue list --label action-item --state open \
#                    --json number,title,labels,assignees --limit 100
#                Every one carries a `route:` label naming its vehicle — route:skill-rule,
#                route:story, route:forge-process, route:undecided. Surface `route:undecided`
#                as a RISK: items rot because nothing routes them to a vehicle, not because
#                they are hard to find.
#   reasoning -> _bmad-output/implementation-artifacts/action-items.md — the prose archive of
#                all 60 items, verbatim. History wins there; STATUS WINS ON THE ISSUE. Never
#                read status from that file.
#
# A mirror was rejected deliberately: two parsers agreeing with each other is a failure mode this
# project has already paid for. If the `gh` call fails for any reason, say so out loud and never
# report zero open action items — a silent empty list is the false green this arrangement exists
# to prevent.
