# Token metrics rollup — 1

Per-story cost by phase (`est_usd`), summed across the epic. `$x` = recorded; `n/a` = a row exists but its cost is annotated unrecoverable in the ledger; `—` = no row at all (a silent gap to fix). Generated by `_bmad/scripts/session_tokens.py --rollup 1` on 2026-08-03 15:31 UTC; re-run to refresh.

| story | create | dev | review | total |
|---|---|---|---|---|
| 1 | $19.69 | $2.28 | $57.37 | $79.34 |
| 2 | $14.40 | $2.94 | $75.97 | $93.31 |
| 3 | $20.31 | $3.54 | $59.61 | $83.46 |
| **total** | **$54.40** | **$8.76** | **$192.95** | **$256.11** |

**No gaps** — every story carries a priced create/dev/review.

**These totals are old-Opus-rate and read ~3x high (annotated 2026-08-03).** Every row above
was recorded before `PRICES` was corrected: the table carried the Opus-4.1-era $15/$75, while
every current Opus is $5/$25. `--rollup` sums the `est_usd` already written into each ledger,
so re-running it will keep reproducing these figures — they are not retro-corrected, by
design, because silently rewriting recorded history is worse than labelling it. Rows written
from now on carry a `rates <version>` stamp so eras cannot be mixed unnoticed again.

Recomputed by the forge from these same token counts at correct rates:

| phase | above | corrected | share |
|---|---|---|---|
| create | $54.40 | **$18.13** | 20% |
| dev | $8.76 | **$8.75** | 10% |
| review | $192.95 | **$64.32** | **71%** |
| **total** | **$256.11** | **$91.20** | |

**The headline survives:** review really is ~71% of the epic, and the forge reproduced the
same shape independently on ep-11 (create 19% / dev 12% / review 69%, cache reads 65.6% of
review cost against frostvein's 61%). Two projects, two languages, one answer — **review is
expensive because it re-reads, not because it thinks.** What does NOT survive: "dev is 3% of
cost" was an artifact of the bug. It is 10%, so any reasoning resting on Codex dev being
nearly free needs re-deriving.
