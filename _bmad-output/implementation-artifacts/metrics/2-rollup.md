# Token metrics rollup — 2

Per-story cost by phase (`est_usd`), summed across the epic. `$x` = recorded; `n/a` = a row exists but its cost is annotated unrecoverable in the ledger; `—` = no row at all (a silent gap to fix). Generated by `_bmad/scripts/session_tokens.py --rollup 2` on 2026-08-03 11:23 UTC; re-run to refresh.

| story | create | dev | review | total |
|---|---|---|---|---|
| world | n/a | — | — | $0.00 |
| **total** | **n/a** | **n/a** | **n/a** | **$0.00** |

**Silent gaps (no row at all — fix):** 2-1-the-world-runs-on-its-own-clock `dev`, 2-1-the-world-runs-on-its-own-clock `review`

Each gap is either backfillable (transcript on disk) or must be annotated unrecoverable in that story's ledger — no silent blanks.

**Unrecoverable (annotated, not silent):** 2-1-the-world-runs-on-its-own-clock `create`

These ran in mixed multi-story/multi-phase sessions with no per-phase cursor baseline, so the delta can't be reconstructed post-hoc. The `on_complete` review hook (`_bmad/custom/bmad-code-review.toml`) records the review delta at review-time going forward, so this class of gap does not recur.
